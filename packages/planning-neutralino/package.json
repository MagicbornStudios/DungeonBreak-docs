{
  "name": "@dungeonbreak/planning-neutralino",
  "version": "0.1.0",
  "private": true,
  "description": "Neutralino desktop wrapper for the planning app; loads the Next URL.",
  "scripts": {
    "run": "neu run",
    "build": "neu build",
    "build:release": "neu build --release"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.4.0"
  }
}
