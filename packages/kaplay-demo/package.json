{
  "name": "@dungeonbreak/kaplay-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone KAPLAY single-file HTML game for Escape the Dungeon",
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build-standalone.ts",
    "dev": "tsx scripts/build-standalone.ts --watch",
    "dev:standalone": "pnpm run build && npx concurrently -n build,serve \"pnpm run dev\" \"npx serve dist -p 3001\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dungeonbreak/engine": "link:../engine",
    "kaplay": "^3001.0.19"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "esbuild": "^0.24.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
