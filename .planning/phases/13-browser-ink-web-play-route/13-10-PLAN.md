# Phase 13-10 Plan - Python Release Fulfillment and Publish Closeout

## Objective

Ensure the Python terminal binary release path is dependable and produce the first stable release (`v0.1.0`) after gates pass.

## Files

- `vendor/adventurelib` tracking fix
- `.github/workflows/terminal-game-release.yml` (if needed)
- `README.md`
- `AGENTS.md`
- `.planning/phases/13-browser-ink-web-play-route/13-07-SUMMARY.md` (finalized)

## Must-Haves

1. Vendor strategy is deterministic in CI (no broken gitlink state).
2. Browser and Python pipelines both pass before tagging.
3. Tag `v0.1.0` is pushed and release artifacts exist (linux/macos/windows binaries).
4. Scratch and concept artifacts are included in this loop's commit set.

## Verification

- `uv run --extra test pytest -q`
- `pnpm --dir docs-site run typecheck`
- `pnpm --dir docs-site run test:unit`
- `pnpm --dir docs-site run test:e2e`
- `pnpm --dir docs-site run build`
- GitHub Actions tag workflow outcome + release asset presence.
