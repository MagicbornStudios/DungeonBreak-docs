---
phase: 12-terminal-packaging-and-release-automation
plan: 01
wave: 1
depends_on: ['10-01']
files_modified: []
autonomous: true
gap_closure: false
must_haves:
  truths:
    - "CLI binary build command exists and works locally with configured tooling."
    - "CI workflow runs tests before building binaries."
    - "Cross-platform build artifacts are produced and uploaded."
    - "Tag-triggered releases publish binary assets."
  artifacts:
    - path: ".planning/phases/12-terminal-packaging-and-release-automation/12-01-SUMMARY.md"
      provides: "Execution summary for phase 12 plan 01"
      min_lines: 8
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/12-terminal-packaging-and-release-automation/12-01-SUMMARY.md"
      via: "phase 12 release pipeline traceability"
---

<objective>
Enable a predictable release loop for downloadable terminal game binaries with minimal manual overhead.
</objective>

<context>
@.planning/phases/12-terminal-packaging-and-release-automation/12-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Add local binary build scripts</name>
  <files>package.json, pyproject.toml, .gitignore</files>
  <action>Wire a command for local CLI binary builds and include build tooling dependency configuration.</action>
  <verify>`npm run build:terminal:bin` command resolves and can invoke configured builder.</verify>
  <done>Local binary build pipeline entrypoint exists.</done>
</task>

<task type="auto">
  <name>Create CI workflow for test/build/release</name>
  <files>.github/workflows/*.yml</files>
  <action>Add workflow that runs tests, builds binaries on matrix OS targets, and publishes assets on release tags.</action>
  <verify>Workflow YAML passes syntax checks and references valid paths.</verify>
  <done>Release automation workflow is committed.</done>
</task>

<task type="auto">
  <name>Document publishing expectations for contributors and agents</name>
  <files>README.md, AGENTS.md, .planning/*.md</files>
  <action>Describe release commands, workflow triggers, and planning traceability requirements.</action>
  <verify>Docs include command examples and phase references.</verify>
  <done>Publishing docs are clear and actionable.</done>
</task>
</tasks>
