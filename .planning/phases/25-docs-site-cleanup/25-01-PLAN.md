# Phase 25-01 Plan: Docs-site Cleanup Implementation

## Tasks

1. **Nav update** — Add Play, Reports, Downloadables, Engine, Roadmap, GRD to `layout.config.tsx` (replace or augment Getting Started).
2. **Planning markdown route** — Create `/planning/[[...slug]]/page.tsx` that maps slug to `.planning/*.md` files, reads with fs, renders with react-markdown.
3. **Downloadables** — Add `/play/downloads` page and `/api/reports/[filename]` route serving `.planning/test-reports/*` with `Content-Disposition: attachment`.
4. **Engine docs** — Add `/docs/engine` (via content/docs or MDX) with overview and link to `@dungeonbreak/engine`.
5. **Nav cleanup** — Remove or reduce template clutter; ensure nav items point to real content.

## File paths

- Nav: `docs-site/app/(fumadocs)/layout.config.tsx`
- Planning route: `docs-site/app/(fumadocs)/planning/[[...slug]]/page.tsx`
- Downloads page: `docs-site/app/(fumadocs)/play/downloads/page.tsx`
- Reports API: `docs-site/app/api/reports/[filename]/route.ts`
- Engine docs: `docs-site/content/docs/engine/index.mdx` (or planning-style if preferred)
