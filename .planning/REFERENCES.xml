<planning-references>
  <doc>
    <path>ROADMAP.md</path>
    <content><![CDATA[
# Roadmap: DungeonBreak

## Overview

Phases for planning-docs baseline and Narrative Engine delivery. Each phase has a goal, requirements, and plans; we run discuss → plan → execute → verify per phase.

## Phases

### Phase 01 : Planning docs baseline

**Goal:** PROJECT, ROADMAP, and REQUIREMENTS are aligned with DungeonBreak and Narrative Engine; Phase 01 has a clear context (desired outcome, constraints, non-goals) and at least one plan with concrete tasks; TASK-REGISTRY or phase plans reflect the task breakdown.

**Requirements:** [REQ-01, REQ-02, REQ-03]

**Plans:** Run `forge-loop discuss-phase 1` then `forge-loop plan-phase 1` to generate.

Tasks (breakdown for Phase 01):
- Capture Phase 01 context (discuss-phase): desired outcome, constraints, non-goals
- Ensure ROADMAP Phase 01 details and REQUIREMENTS traceability are correct
- Create or refine Phase 01 plan(s) with must-haves and task blocks
- Document any decisions in DECISIONS.md

---

### Phase 02 : Planning docs cleanup and narrative state-space simulations

**Goal:** PRD and key decisions live in .planning; general docs/ are not extended. Simulation notebooks in `notebooks/` visualize the 3D narrative state space, saliency, dialog triggering, and Verlet+constraints so we can understand and communicate how the system will behave—no game code.

**Requirements:** [REQ-06]

**Depends on:** Phase 01 complete.

**Plans:** Run `forge-loop discuss-phase 2` then `forge-loop plan-phase 2`.

---

### Phase 03 : Narrative Engine first slice (Verlet + constraints)

**Goal:** Verlet integration and constraint application (per-axis min/max, reproject old_p) are implemented and unit-tested so narrative state stays within bounds. See `.planning/DECISIONS.md` (Narrative Engine) and `.planning/implementation-roadmap.md`.

**Requirements:** [REQ-04]

**Depends on:** Phase 02 complete.

**Plans:** TBD (run `forge-loop plan-phase 3` when ready).

---

### Phase 04 : Narrative Engine – dialog selection and integration

**Goal:** Dialog/storylet selection uses entity position and dialog Location (saliency); integration point for DialogComponent and/or Yarn Spinner is defined and implemented to a first testable slice.

**Requirements:** [REQ-05]

**Depends on:** Phase 03.

**Plans:** TBD.

---

### Phase 05 : Docs fold into planning and notebook tooling

**Goal:** docs/ contains only image-catalog (and images). Game PRD, design decisions, and implementation roadmap folded into .planning. Notebook tooling: venv + check/install + JupyterLab from root; no single-notebook run commands. Notebooks are documentation (entities/vectors/dialogue in same space; threshold and options for follow-up).

**Requirements:** [REQ-07]

**Depends on:** Phase 02 complete.

**Plans:** Run `forge-loop discuss-phase 5` then `forge-loop plan-phase 5`.

---

### Phase 06 : Lab setup (uv, lab command) and notebook improvements

**Goal:** Single entry `npm run lab`; one main notebook (**dungeonbreak-narrative.ipynb**) that teaches axis semantics (Courage, Loyalty, Hunger), dialogue locations by *meaning*, how Kaiza reaches options (training, dialog, forces), saliency/threshold, Verlet, and time simulation—DRY, explicit naming, no arbitrary vectors. Doc images embedded; GDC/reference links in .planning. Fast, clone-and-run setup.

**Requirements:** [REQ-08]

**Depends on:** Phase 05 complete.

**Plans:** Run `forge-loop discuss-phase 6` then `forge-loop plan-phase 6`.

---

### Phase 07 : Escape the Dungeon (adventurelib base + modular architecture)

**Goal:** Define and implement **Escape the Dungeon** as a modular Python/Jupyter dungeon crawler starting at depth 12 as Kael, with level/chapter/act progression, NPC background simulation, quest + item systems, and embeddings-driven trait updates.

**Requirements:** [REQ-09, REQ-10, REQ-11, REQ-12]

**Depends on:** Phase 06 complete.

**Plans:** Run `forge-loop discuss-phase 7` then `forge-loop plan-phase 7`.

---

### Phase 08 : Vector-space rooms, dialogue ranges, and simulation clarity

**Goal:** Deepen Escape the Dungeon simulation with explicit `Dungeon/Level/Room` objects (50 rooms per level), room+item vectors that influence entities per turn, and dialogue clusters/options gated by vector distance and room state.

**Requirements:** [REQ-13, REQ-14, REQ-15, REQ-16, REQ-17]

**Depends on:** Phase 07 complete.

**Plans:** Run `forge-loop discuss-phase 8` then `forge-loop plan-phase 8`.

---

### Phase 09 : Cutscenes, action gating, skills, and livestream fame economy

**Goal:** Add a clear presentation and decision layer on top of the simulation: cutscene popups for major events, prerequisite-based action/dialogue filtering, skill vectors for emergent builds, and livestream gameplay with `Fame` gain and `Effort` spend (10 effort per turn).

**Requirements:** [REQ-18, REQ-19, REQ-20, REQ-21, REQ-22]

**Depends on:** Phase 08 complete.

**Plans:** `09-01` implemented; run `forge-loop verify-work 9 --strict` for final phase verification.

---

### Phase 10 : Entity parity, factions, and emergent pressure systems

**Goal:** Move from prototype AI behavior to full emergent simulation parity: same legal action model for player/NPCs, dense room composition (treasure/rune forge), spawn pressure, faction/reputation lethality gates, companion recruitment, rumor spread, and level curves.

**Requirements:** [REQ-23, REQ-24, REQ-25, REQ-26, REQ-27, REQ-28, REQ-29, REQ-30, REQ-31, REQ-32]

**Depends on:** Phase 09 complete.

**Plans:** `10-01` implemented; run `forge-loop verify-work 10 --strict` for final phase verification.

---

### Phase 11 : Content scale, archetype compass, and balancing

**Goal:** Expand authored content and balancing around emergent systems: large skill trees/evolution options, archetype headings/class textures, broader action/dialogue/item packs, and stability/performance tuning for long simulations.

**Requirements:** [REQ-68, REQ-69, REQ-70]

**Depends on:** Phase 10 complete.

**Plans:** `11-01` implemented (data-driven content packs, archetype compass runtime wiring, balancing harness/reporting).

---

### Phase 12 : Terminal binary packaging and release automation

**Goal:** Publish Escape the Dungeon as downloadable terminal game binaries with a repeatable CI pipeline for tests, build artifacts, and tag-triggered releases.

**Requirements:** [REQ-33, REQ-34, REQ-35]

**Depends on:** Phase 10 stable simulation baseline.

**Plans:** `12-01` implemented; run `forge-loop verify-work 12 --strict` for final phase verification.

---

### Phase 13 : Browser playable 3-column game UX (`/play`)

**Goal:** Ship a browser-playable TypeScript runtime in docs-site at `/play` with a 3-column button-first interface (actions/feed/status), Assistant UI feed presentation, and blocking cutscene queue.

**Requirements:** [REQ-36, REQ-37, REQ-38, REQ-39, REQ-40, REQ-41, REQ-42, REQ-43, REQ-44, REQ-45, REQ-46]

**Depends on:** Phase 10+ systems stable; docs-site build/deploy stable.

**Plans:** `13-01..13-06` initial browser runtime and gating. Recovery loops: `13-07` pivot docs/contracts, `13-08` UI rebuild, `13-09` parity presentation + tests, `13-10` release closeout.

---

### Phase 14 : Browser parity closure and content scaling

**Goal:** Close remaining parity and gameplay-clarity gaps with a no-grid combat simulation, deterministic 25-turn reference run (single canonical seed), misinformation-aware deed memory, and reusable cross-language data contracts in TypeScript runtime.

**Requirements:** [REQ-37, REQ-38, REQ-40, REQ-47, REQ-48, REQ-49, REQ-50, REQ-51, REQ-52, REQ-53, REQ-54, REQ-55, REQ-56, REQ-57, REQ-58, REQ-59]

**Depends on:** Phase 13 engine/runtime in production.

**Plans:** `14-01` planning + DoD draft, `14-02` contracts/formulas + simulation implementation, `14-03` deterministic replay and tests, `14-04` performance/reporting closure.

---

### Phase 15 : TypeScript Cutover and `DungeonBreak/engine` Package

**Goal:** Remove Python gameplay runtime and notebooks from active development immediately, retain `npm run lab` and install helpers for TypeScript workflows, and ship an installable package (`DungeonBreak/engine`) with bundled game data and an out-of-the-box React component.

**Requirements:** [REQ-08, REQ-60, REQ-61, REQ-62, REQ-63, REQ-64, REQ-65, REQ-66, REQ-67]

**Depends on:** Phase 13 complete; executes in parallel with late Phase 14 closure.

**Plans:** `15-01` planning/inventory, `15-02` removal cutover, `15-03` helper retargeting, `15-04` package extraction + CI/release wiring.

---

### Phase 16 : Publish hardening for browser package release (GitHub Releases)

**Goal:** Harden GitHub-release package/browser operations with stricter CI quality bars, semantic tag validation, automated release notes, smoke replay packs, and deterministic packaged-tarball integration checks.

**Requirements:** [REQ-41, REQ-42, REQ-66, REQ-67]

**Depends on:** Phase 14 and Phase 15 complete.

**Plans:** `16-01` implemented.

---

### Phase 17 : Content production and long-run balancing loops

**Goal:** Scale authored gameplay content (skills/dialogue/items/events/quests) while preserving deterministic behavior and performance under long-run simulations.

**Requirements:** [REQ-71, REQ-72, REQ-73, REQ-74]

**Depends on:** Phase 16 complete.

**Plans:** `17-01` contract lock + long-run suite/report baseline (complete), `17-02` content expansion (complete), `17-03` pressure/perf tuning (complete), `17-04` verification and summary (complete).

---

### Phase 18 : Deterministic MCP playthrough and interface

**Goal:** Provide a stable MCP machine interface and dense deterministic playthrough coverage for tooling, regression, and report generation.

**Requirements:** [REQ-75, REQ-76, REQ-77, REQ-78, REQ-79, REQ-80]

**Depends on:** Phase 17 baseline.

**Plans:** `18-01` baseline MCP interface and local installer wiring (complete), `18-02` dense deterministic playthrough + regression + presenter/MCP parity verification (complete).

---

### Phase 19 : Assistant Frame Window-Agent Support and Default Remote MCP

**Goal:** Add Assistant Frame support to browser `/play` so window agents can interact through a structured bridge, keep remote MCP (`/api/mcp`) enabled by default for signed-in users, and publish version-coupled play/test reports with each build.

**Requirements:** [REQ-81, REQ-82, REQ-83, REQ-84, REQ-85, REQ-86, REQ-87, REQ-88]

**Depends on:** Phase 18 complete.

**Plans:** `19-01` planning/contracts update (complete), `19-02` Assistant Frame integration + tests (complete), `19-03` default-on remote MCP auth/hardening/docs + versioned report publishing (complete).

---

### Phase 20 : Report schema normalization and streamable event ledgers

**Goal:** Keep report fidelity while reducing memory and payload pressure by defaulting to reference-oriented ledger artifacts and adding JSONL stream iteration for timeline viewers.

**Requirements:** [REQ-89, REQ-90]

**Depends on:** Phase 19 complete.

**Plans:** `20-01` policy/docs realignment (no LLM chooser scope), `20-02` JSONL external ledger stream format + viewer iteration support.

---

### Phase 21 : Tooling PRD, gameplay analysis, and report viewer

**Goal:** Tooling PRD; metric definitions (replayability, excitement, emergent); playthrough analyzer; report viewer on docs site.

**Requirements:** [PRD-tooling-gameplay-analysis]

**Depends on:** Phase 18 complete.

**Plans:** Tooling PRD + metrics, playthrough analyzer (`playthrough-analyzer.ts`), report viewer route `/reports` or `/play/reports`.

---

### Phase 22 : Action policies as static data

**Goal:** Action policies in `contracts/data/action-policies.json`; agent-play and engine NPCs use policies instead of hardcoded behavior.

**Requirements:** [PRD-tooling-gameplay-analysis]

**Depends on:** Phase 21 complete.

**Plans:** `action-policies.json`, schema + export from engine, agent-play reads policy; future: `simulateNpcTurns` accepts policy ID.

---

### Phase 23 : KAPLAY single-file HTML build

**Goal:** Standalone single-file HTML game with KAPLAY; first-person text mode + ASCII grid mode; screen-based UI (Navigation, Combat, Rune Forge, Inventory, Dialogue); bundle engine + game logic; GitHub release artifact.

**Requirements:** [Plan demo tooling and KAPLAY]

**Depends on:** Phase 22 complete.

**Plans:** `packages/kaplay-demo`, first-person scene, ASCII grid with screen swaps (Navigation → Combat → Action menu → Rune Forge, etc.); UI components per `.planning/UI-COMPONENT-REGISTRY.md`; `build-standalone.ts` → single HTML; GitHub release workflow.

---

### Phase 24 : Docs-site deploy reliability and Vercel-parity CI loop

**Goal:** Keep docs-site continuously publishable by adding parity build checks with retained logs, hardening local package bootstrap paths used by Vercel, and removing recurring build-time regressions.

**Requirements:** [REQ-41, REQ-42, REQ-66]

**Depends on:** Phase 20+ ongoing docs-site/report surface work.

**Plans:** `24-01` parity workflow with uploaded logs, `24-02` engine dist bootstrap on install, `24-03` engine contracts export regression fix, `24-04` clean build output + lockfile refresh, `24-05` llms route response hardening.

---

### Phase 29 : Engine/KAPLAY parity gaps (inventory action slice)

**Goal:** Close early gameplay parity gaps by adding explicit inventory actions in the engine (`use_item`, `equip_item`, `drop_item`) and wiring KAPLAY inventory UI controls directly to those engine contracts.

**Requirements:** [Plan demo tooling and KAPLAY]

**Depends on:** Phase 23 complete.

**Plans:** `29-01` inventory action contract + execution/availability, `29-02` KAPLAY inventory action wiring, `29-03` replay fixture/hash refresh.

---

### Phase 30 : Kaplay first-person UI simplification

**Goal:** Simplify first-person interface; fix unclosed tags; align layout reuse (header/left/right/center).

**Requirements:** [User feedback]

**Depends on:** Phase 23 complete.

**Plans:** `30-01` Quick View → Utility, remove tabs, default grid; `30-02` escape keyboard hints (unclosed tags fix), layout reuse doc; `30-03` Journal, Combat gate, contextual actions; `30-04` 3-column layout (left nav, center map, right journal); `30-05` first-person 3-column layout, shared layout constants.

---

### Phase 31 : Canonical identifiers and content structure

**Goal:** Engine as single source of truth for actionType and roomFeature; no magic strings; structured context (roomFeature in status) instead of prose parsing.

**Requirements:** [User feedback, PRD-tooling-gameplay-analysis]

**Depends on:** Phase 30 complete.

**Plans:** `31-01` engine exports PLAYER_ACTION_TYPES/ROOM_FEATURES, status(roomFeature), AGENTS.md convention, KAPLAY/engine-mcp consume; `31-02` Action vs Option mental model doc.

---

### Phase 32 : Next parity or polish

**Goal:** TBD — Phase 29 completion (Rune Forge, combat, dialogue) or reporting tooling.

**Requirements:** TBD

**Depends on:** Phase 29, 31 complete.

**Plans:** `32-01` TBD — run discuss-phase when ready.

---

## Planning Docs Lifecycle

To keep each phase discoverable and searchable, every loop should document what changed using the sections in `PHASE-DOC-TEMPLATE.md`.

- **Purpose:** Goal + success signal.  
- **Scope & Constraints:** UX/tech boundaries (controller-first, controller+mouse mix, etc.).  
- **Files of Interest:** List touched files and shorthand description/keywords.  
- **Search Keywords:** Terms like `ThreeColumnShell`, `KeyHintLegend`, `CommandPanelOrganism` to locate code quickly.  
- **Important Commands:** Build/typecheck/parity commands plus `rg` shortcuts.  
- **Verification:** Tests executed (typecheck/build/parity smoke).  
- **Next Steps:** Link to next phases (46–48) or follow-ups.

Use the same structure in `PLAN.md`/`SUMMARY.md` per phase folder, keep `TASK-REGISTRY.md` entries short but referencing keywords/commands, and let `STATE.md` cite the current phase plus the next action derived from the template.

---

### Phase 44 : KAPLAY atomic component library baseline

**Goal:** Establish the KAPLAY UI library as the default build path using atomic design (`atoms`, `molecules`, `organisms`) with tokenized theme usage and no scene-local color literals for new work.

**Requirements:** [UI-COMPONENT-REGISTRY, KAPLAY-INTERFACE-SPEC]

**Depends on:** Phase 30 shell simplification baseline.

**Plans:** `44-01` atomic primitives + scene shell refit + key hint molecule + verification; `44-02` document component workflow referencing PRD/GRD directives, embed improvement/self-check questions, and keep task/decision/state docs lean.

**Templates & References:** `doc-template-atoms.xml`, `doc-template-molecules.xml`, `doc-template-organisms.xml`, `PLAN-TEMPLATE.xml`, `SUMMARY-TEMPLATE.xml`, plus `PRD-formulas-and-spaces.md` & `GRD-escape-the-dungeon.md`.

---

### Phase 45 : Grid and first-person screen refit completion

**Goal:** Move all playable grid/first-person screens onto shared organisms and templates (left command panel, center content surface, right room/journal surface).

**Requirements:** [UI-COMPONENT-REGISTRY]

**Depends on:** Phase 44 complete.

**Plans:** `45-01` complete refit of combat/action/rune forge/inventory/dialogue + docs sync.

---

### Phase 46 : Human-playable input UX hardening

**Goal:** Improve discoverability and ergonomics of controls for keyboard + mouse players: stable key legends, clear command naming, and reduced mode confusion.

**Requirements:** [KAPLAY-INTERFACE-SPEC]

**Depends on:** Phase 45 complete.

**Plans:** `46-01` key legend standardization, command wording pass, interaction polish + smoke capture.

---

### Phase 47 : Agent-play and standalone parity hardening

**Goal:** Ensure standalone build and agent-play contracts remain in parity through deterministic smoke tests and explicit command surface checks.

**Requirements:** [REQ-75, REQ-76, REQ-80]

**Depends on:** Phase 46 complete.

**Plans:** `47-01` engine build/typecheck + engine-mcp typecheck/parity smoke + standalone build gating.

---

### Phase 48 : Release-ready text-graphics polish

**Goal:** Final pass on text-graphics readability, panel spacing, and scene consistency with no media assets while keeping deterministic behavior unchanged.

**Requirements:** [KAPLAY-INTERFACE-SPEC, UI-COMPONENT-REGISTRY]

**Depends on:** Phase 47 complete.

**Plans:** `48-01` polish checklist + capture artifacts + release notes draft.

---

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 01. Planning docs baseline | 2 / 2 | Complete | - |
| 02. Planning docs cleanup and simulations | 2 / 2 | Complete | - |
| 03. Narrative Engine first slice | 0 / 0 | Superseded (archived after Escape the Dungeon pivot) | - |
| 04. Dialog selection and integration | 0 / 0 | Superseded (archived after Escape the Dungeon pivot) | - |
| 05. Docs fold and notebook tooling | 2 / 2 | Complete | - |
| 06. Lab setup and notebook improvements | 2 / 2 | Complete | - |
| 07. Escape the Dungeon | 1 / 1 | Complete | - |
| 08. Vector-space simulation depth | 1 / 1 | Complete | - |
| 09. Cutscenes and gating layer | 1 / 1 | Complete | - |
| 10. Entity parity and faction pressure | 1 / 1 | Complete | - |
| 11. Content scale and balancing | 1 / 1 | Complete | - |
| 12. Terminal packaging and release | 1 / 1 | Complete | - |
| 13. Browser playable 3-column UX | 10 / 10 | Complete | - |
| 14. Browser parity closure and content scaling | 4 / 4 | Complete | - |
| 15. TypeScript cutover + package | 4 / 4 | Complete | - |
| 16. Publish hardening (browser package) | 1 / 1 | Complete | - |
| 17. Content production and long-run balancing loops | 4 / 4 | Complete | - |
| 18. Deterministic MCP playthrough and interface | 2 / 2 | Complete | - |
| 19. Assistant Frame + default remote MCP | 3 / 3 | Complete | - |
| 20. Report schema normalization + streamable ledgers | 2 / 2 | Complete | - |
| 21. Tooling PRD + analyzer + report viewer | 3 / 3 | Complete | - |
| 22. Action policies as static data | 3 / 3 | Complete | - |
| 23. KAPLAY single-file HTML build | 6 / 6 | Complete | - |
| 24. Docs-site deploy reliability + Vercel parity CI | 5 / 5 | Complete | - |
| 29. Engine/KAPLAY parity gaps (inventory slice) | 3 / 5 | In Progress | - |
| 30. Kaplay first-person UI simplification | 5 / 5 | Complete | - |
| 31. Canonical identifiers and content structure | 1 / 2 | In Progress | 31-01 |
| 32. Next parity or polish | 0 / 1 | Not started | - |
| 44. KAPLAY atomic component library baseline | 1 / 1 | In Progress | 44-01 |
| 45. Grid and first-person screen refit completion | 0 / 1 | Not started | - |
| 46. Human-playable input UX hardening | 0 / 1 | Not started | - |
| 47. Agent-play and standalone parity hardening | 0 / 1 | Not started | - |
| 48. Release-ready text-graphics polish | 0 / 1 | Not started | - |

]]></content>
  </doc>
  <doc>
    <path>TASK-REGISTRY.md</path>
    <content><![CDATA[
# Task Registry

High-level tasks are tracked here; per-phase task detail lives in `.planning/phases/<phase>/` (PLAN and SUMMARY files).

## Phase 01 – Planning docs baseline

| Id | Task | Status |
|----|------|--------|
| 01-1 | Align PROJECT, ROADMAP, REQUIREMENTS with DungeonBreak / Narrative Engine | Done (docs updated) |
| 01-2 | Run discuss-phase 1: capture desired outcome, constraints, non-goals | Done |
| 01-3 | Run plan-phase 1: create/refine Phase 01 plan(s) | Done |
| 01-4 | Execute Phase 01 plan(s) and write summary | Done |
| 01-5 | Run verify-work 1 | Done |

## Phase 02 – Planning docs cleanup and simulations

| Id | Task | Status |
|----|------|--------|
| 02-1 | PRD and developer guide in .planning; docs/ pointers only | Done |
| 02-2 | discuss-phase 2, plan-phase 2 | Done |
| 02-3 | Notebooks: state-space-saliency, verlet-constraints | Done |
| 02-4 | verify-work 2 | Done |

## Phase 03 – Narrative Engine first slice (Verlet + constraints)

| Id | Task | Status |
|----|------|--------|
| 03-1 | Implement Verlet integration in Narrative Engine | Superseded (archived after Escape the Dungeon pivot) |
| 03-2 | Add per-axis min/max constraints and old_p reprojection | Superseded (archived after Escape the Dungeon pivot) |
| 03-3 | Unit tests for constrained Verlet | Superseded (archived after Escape the Dungeon pivot) |

## Phase 04 – Dialog selection and integration

| Id | Task | Status |
|----|------|--------|
| 04-1 | Dialog selection by entity position + dialog Location (saliency) | Superseded (archived after Escape the Dungeon pivot) |
| 04-2 | Integration point for DialogComponent / Yarn Spinner | Superseded (archived after Escape the Dungeon pivot) |

## Phase 05 – Docs fold and notebook tooling

| Id | Task | Status |
|----|------|--------|
| 05-1 | Game PRD, design decisions, implementation roadmap folded into .planning | Done |
| 05-2 | Remove narrative-engine PRD/guide, PRD, design-decisions, implementation-roadmap from docs/ | Done |
| 05-3 | Notebook venv + JupyterLab scripts (notebooks:install, notebooks) | Done |
| 05-4 | verify-work 5 | Done |

## Phase 06 – Lab setup and notebook improvements

| Id | Task | Status |
|----|------|--------|
| 06-1 | Add Phase 06, REQ-08, TASK-REGISTRY | Done |
| 06-2 | uv-based lab: install uv if missing, lab/lab:install scripts | Done |
| 06-3 | .planning/REFERENCES.md with GDC/video links for agents | Done |
| 06-4 | Notebooks: Kaiza, stats, dialogue vectors, threshold, labels | Done |
| 06-5 | discuss-phase 6, plan-phase 6, execute, verify-work 6 | Done |

## Phase 07 â€“ Escape the Dungeon

| Id | Task | Status |
|----|------|--------|
| 07-1 | Update PRD for Escape the Dungeon (12 levels, chapters/acts/pages, Kael start) | Done |
| 07-2 | Vendor adventurelib and use it as base text-adventure engine | Done |
| 07-3 | Refactor monolithic text adventure into domain modules (world/entities/player/combat/narrative/engine) | Done |
| 07-4 | Implement Kael + NPC simulation loop (movement, training, talk, rest, search, speak) | Done |
| 07-5 | Implement chapter/act/page logging and quest progression | Done |
| 07-6 | Ship notebook demo for Escape the Dungeon and run smoke tests | Done |

## Phase 08 â€“ Vector-space simulation depth

| Id | Task | Status |
|----|------|--------|
| 08-1 | Start new planning phase for vector-space simulation clarity and extensibility | Done |
| 08-2 | Define explicit world/story objects (`Dungeon`, `Level`, `Room`, `Act`, `Chapter`, `Page`) and relation model | Done |
| 08-3 | Upgrade levels to 50 rooms each with one start and one exit | Done |
| 08-4 | Add room vectors + item vectors and apply room influence every turn | Done |
| 08-5 | Implement dialogue clusters/ranges with room-state conditions and option selection APIs | Done |
| 08-6 | Add CLI entry point for Escape the Dungeon command gameplay | Done |
| 08-7 | Update notebook/docs/planning artifacts and smoke-validate flow | Done |
| 08-8 | Add teen-readable architecture guide for classes, vectors, and turn simulation | Done |

## Phase 09 â€“ Cutscenes, action gating, skills, and fame economy

| Id | Task | Status |
|----|------|--------|
| 09-1 | Capture discuss context for cutscene presentation and filtered action UX | Done |
| 09-2 | Define prerequisite model for actions/dialogue/skills with explainable filters | Done |
| 09-3 | Design and implement cutscene trigger pipeline (item, quest, milestone events) | Done |
| 09-4 | Design skill-vector model and emergent unlock/use rules | Done |
| 09-5 | Implement livestream action with Fame and Effort cost 10 per turn | Done |
| 09-6 | Expose gated options + cutscenes + livestream state in notebook and CLI | Done |
| 09-7 | Add tests/docs and complete phase verification | Done |

## Phase 10 â€“ Entity parity, factions, and emergent pressure

| Id | Task | Status |
|----|------|--------|
| 10-1 | Capture discuss context for parity AI, faction lethality, and deterministic/emergent boundaries | Done |
| 10-2 | Define and implement shared action catalog/policy used by player and NPCs | Done |
| 10-3 | Update level composition: 20 treasure + 5 rune forge + exit boss + 4 dungeoneers per level | Done |
| 10-4 | Implement per-turn hostile spawn loop from exit with rune forge safe-haven navigation restriction | Done |
| 10-5 | Implement skill evolution flow at rune forge and enforce run-exclusive `appraisal` vs `xray` branch | Done |
| 10-6 | Implement companion system (max 1) with recruit/alignment checks | Done |
| 10-7 | Implement faction+reputation+trait lethal/murder gates and add `Laughing Face` faction rules | Done |
| 10-8 | Add rumor spread from deeds/livestream between entities and reflect it in dialogue/action filters | Done |
| 10-9 | Implement entity/player level curves and integrate into combat/action utility scoring | Done |
| 10-10 | Add deterministic global event chain + probabilistic emergent trigger system with explicit boundaries | Done |
| 10-11 | Add extensive pytest coverage for parity simulation and long-run interaction traces | Done |

## Phase 11 â€“ Content scale and archetype compass

| Id | Task | Status |
|----|------|--------|
| 11-1 | Define archetype compass vectors and emergent class labeling rules | Done |
| 11-2 | Expand skill tree/action/dialogue/item/event packs with data-driven authoring format | Done |
| 11-3 | Add balancing tools and simulation harness for curve tuning | Done |
| 11-4 | Add onboarding/API docs for new systems and verify maintainability | Done |

## Phase 12 â€“ Terminal packaging and release automation

| Id | Task | Status |
|----|------|--------|
| 12-1 | Define packaging scope and version strategy for terminal releases | Done |
| 12-2 | Add local binary build command for CLI entrypoint | Done |
| 12-3 | Add CI workflow for python tests + cross-platform binary artifact builds | Done |
| 12-4 | Add tag-based GitHub release publishing workflow for binaries | Done |
| 12-5 | Update README/AGENTS docs for publishing process | Done |
| 12-6 | Validate workflow syntax and traceability in planning docs | Done |

## Phase 13 - Browser playable 3-column game on docs `/play`

| Id | Task | Status |
|----|------|--------|
| 13-01 | Loop 13-01 planning/contracts update across ROADMAP/REQUIREMENTS/TASK-REGISTRY/STATE/DECISIONS/PRD | Done |
| 13-02 | Add browser parity matrix doc and lock command surface + DoD for browser release | Done |
| 13-03 | Implement initial `/play` Ink Web terminal shell + homepage CTA | Superseded by Loop 13-08 pivot |
| 13-04 | Implement TS engine parity slices (world, actions, dialogue, skills, cutscenes, deeds, fame, NPC sim, combat) | Done |
| 13-05 | Add browser persistence (IndexedDB autosave + slots) and expose save/load commands | Done |
| 13-06 | Add unit test suite and initial e2e smoke for browser runtime | Done |
| 13-07 | Recovery pivot: update planning docs/contracts for button-first 3-column UI + release closeout scope | Done |
| 13-08 | Replace `/play` UI with shadcn layout (left actions, middle Assistant UI feed, right status) and blocking cutscene queue | Done |
| 13-09 | Add presenter layer, expand unit/e2e tests for click-flow gameplay and blocked-reason visibility | Done |
| 13-10 | Fulfill Python release path (vendor tracking fix, publish docs, tag/release verification) and finalize summaries | Done |

## Phase 14 - Browser combat simulation parity (no combat grid)

| Id | Task | Status |
|----|------|--------|
| 14-01 | Planning loop: lock no-grid combat direction, interaction boundaries (`fight`/`flee`), and draft Definition of Done | Done |
| 14-02 | Define encounter simulation contracts (inputs, formulas, deterministic ordering, no-grid policy) and publish machine-readable action-outcome mapping | Done |
| 14-03 | Implement browser combat mode flow (`fight` resolution + deterministic `flee` movement with possible later chase) | Done |
| 14-04 | Implement deed misinformation model (source, confidence, about-self/about-other) and rumor propagation rules | Done |
| 14-05 | Publish shared JSON schemas/data packs for actions/items/skills/traits/cutscenes/room templates; TS canonical runtime policy | Done |
| 14-06 | Add deterministic 25-turn golden playthrough suite with canonical seed (`CANONICAL_SEED_V1`) that exercises all core systems and validates page logs/cutscenes | Done |
| 14-07 | Add deterministic golden trace replay harness for TS runtime consumers (package + docs app) | Done |
| 14-08 | Add vector/feature usage analytics report and CI artifact for low-usage/unused content | Done |
| 14-09 | Add long-run pressure/performance tests and enforce browser turn budget (`p95 <= 2s`) with pressure cap 120 (item-aware counting) and pruning policy | Done |
| 14-10 | Finalize Phase 14 DoD, parity matrix closure, and planning summaries | Done |
| 14-11 | Python sunset planning item | Superseded by Phase 15 immediate cutover |

## Phase 15 - TypeScript cutover and `DungeonBreak/engine` package

| Id | Task | Status |
|----|------|--------|
| 15-01 | Planning loop: lock immediate Python/notebook removal and package contract (`DungeonBreak/engine`) | Done |
| 15-02 | Remove Python gameplay runtime and notebook artifacts from active repo paths; keep archive via tags/releases | Done |
| 15-03 | Retain and retarget `npm run lab` and install helpers for TypeScript package/dev workflows | Done |
| 15-04 | Extract/package engine as installable JS package (`@dungeonbreak/engine` implementation id) | Done |
| 15-05 | Package bundled default content/data so game runs out of box in consumers | Done |
| 15-06 | Expose React component + engine APIs and ship complete working example integration in repo | Done |
| 15-07 | Update docs, onboarding, and publish guidance for package-first distribution | Done |
| 15-08 | Add CI install/build/smoke validation from consumer perspective and finalize cutover summary | Done |

## Phase 16 - Publish hardening (browser package, GitHub Releases only)

| Id | Task | Status |
|----|------|--------|
| 16-1 | Harden GitHub release workflow with strict semantic tag validation and automated release notes/changelog generation | Done |
| 16-2 | Add deterministic replay smoke-pack checks to release gating | Done |
| 16-3 | Validate docs-site consumes packaged tarball artifact (not workspace internals) in CI | Done |

## Phase 17 - Content production and long-run balancing loops

| Id | Task | Status |
|----|------|--------|
| 17-1 | Lock Phase 17 contracts and Definition of Done for content-scale + long-run balancing | Done |
| 17-2 | Expand schema-driven content packs (skills/dialogue/items/events/quests) | Done |
| 17-3 | Add long-run deterministic simulation suites (100/250/500 turns) | Done |
| 17-4 | Add balance artifact reporting for action usage/archetypes/survival and dead-content detection | Done |
| 17-5 | Tune pressure/performance with expanded content while preserving p95 and cap budgets | Done |
| 17-6 | Close phase with verification summary and planning traceability updates | Done |

## Phase 18 - Deterministic MCP playthrough and interface

| Id | Task | Status |
|----|------|--------|
| 18-1 | Define machine-playable gameplay API contract (session/state/actions/dispatch) | Done |
| 18-2 | Implement MCP server adapter exposing gameplay tools/resources to coding agents | Done |
| 18-3 | Build dense deterministic playthrough suite (>= 75 turns) with broad interaction coverage | Done |
| 18-4 | Add deterministic agent regression tests (same seed + script => same outputs) | Done |
| 18-5 | Document agent protocol/tool schemas and failure semantics in `.concept` + `.planning` | Done |
| 18-6 | Verify `/play` and MCP agent interface parity through shared engine contracts | Done |

## Phase 19 - Assistant Frame window-agent support + default remote MCP

| Id | Task | Status |
|----|------|--------|
| 19-1 | Planning loop update: roadmap/requirements/tasks/state/decisions/PRD for Assistant Frame + default remote MCP policy | Done |
| 19-2 | Integrate Assistant Frame bridge in `/play` (window-agent consumable) while preserving button-first gameplay UX | Done |
| 19-3 | Add presenter/runtime adapters for frame action dispatch + feed/status synchronization | Done |
| 19-4 | Add tests for frame dispatch flow and fallback behavior when frame host is absent | Done |
| 19-5 | Implement `/api/mcp` as default-on in deployed runtime with signed-in access control | Done |
| 19-6 | Add remote MCP hardening baseline (rate limit/session isolation/payload validation/audit metadata) | Done |
| 19-7 | Publish release-versioned play reports and build test-manifest artifacts for every shipped version | Done |
| 19-8 | Document local stdio MCP, window-agent/frame mode, and signed-in remote MCP usage and report artifact locations | Done |

## Phase 20 - Report schema normalization + streamable ledgers

| Id | Task | Status |
|----|------|--------|
| 20-1 | Lock compact report contract around reference-based storage (packed event ledger + entity/message tables) to reduce duplication | Done |
| 20-2 | Add report-viewer adapters that can replay timeline directly from packed references without loading full expanded objects | Done |
| 20-3 | Add optional split-artifact mode (summary + external event stream) for very long runs and low-memory playback | Done |
| 20-4 | Add report schema versioning/migration notes and viewer compatibility checks in CI | Done |
| 20-5 | De-scoped: MCP/LLM turn chooser mode is not planned for foreseeable delivery | Dropped |
| 20-6 | Add JSONL event-row streaming artifacts and viewer iterators so timeline consumers can read without hydrating full external ledger JSON | Done |

## Phase 21 - Tooling PRD, gameplay analysis, report viewer

| Id | Task | Status |
|----|------|--------|
| 21-1 | Tooling PRD + metric definitions (replayability, excitement, emergent) | Done |
| 21-2 | Playthrough analyzer (`playthrough-analyzer.ts`) | Done |
| 21-3 | Report viewer route `/reports` or `/play/reports` | Done |

## Phase 22 - Action policies as static data

| Id | Task | Status |
|----|------|--------|
| 22-1 | `action-policies.json` + schema + engine export | Done |
| 22-2 | Agent-play uses policy instead of inline PRIORITY_ORDER | Done |
| 22-3 | simulateNpcTurns accepts policy ID (future) | Done |

## Phase 23 - KAPLAY single-file HTML build

| Id | Task | Status |
|----|------|--------|
| 23-1 | UI Component Registry + KAPLAY Interface Spec (layouts, stubs) | Done |
| 23-2 | KAPLAY package with first-person text mode | Done |
| 23-3 | ASCII grid: Navigation screen | Done |
| 23-4 | ASCII grid: Combat screen (Pokemon-style) | Done |
| 23-5 | ASCII grid: Action menu, Rune Forge, Inventory, Dialogue screens | Done |
| 23-6 | Single-file HTML build + GitHub release workflow | Done |

## Phase 24 - Docs-site deploy reliability + Vercel parity logging

| Id | Task | Status |
|----|------|--------|
| 24-1 | Add GitHub Actions workflow that runs docs-site install/build on push/PR and uploads build logs as artifacts | Done |
| 24-2 | Add docs-site preinstall bootstrap so `file:../packages/engine` has built `dist` in clean deploy environments (including Vercel) | Done |
| 24-3 | Fix engine contracts export regression (`ACTION_POLICIES` schema + export) that broke `/api/mcp` build paths | Done |
| 24-4 | Harden engine build with clean output and refresh docs-site lockfile so frozen installs do not expect stale chunk files | Done |
| 24-5 | Fix `/llms.mdx/[...slug]` route to return `Response` in all branches, including 404 and error paths, to stop prerender build failures | Done |
| 24-6 | Stabilize parity workflow build step by forcing bash shell and explicit `PIPESTATUS` pass/fail check for tee'd logs | Done |

## Phase 25 - Docs-site cleanup: reports, downloadables, planning docs

| Id | Task | Status |
|----|------|--------|
| 25-1 | Add nav links: Play, Reports, Downloadables, Engine, Roadmap, GRD | Done |
| 25-2 | Markdown renderer + planning docs route (`/planning/roadmap`, `/planning/grd`) | Done |
| 25-3 | Public downloadables page + API to serve test reports with Content-Disposition | Done |
| 25-4 | Engine docs page (overview + package API pointer) | Done |
| 25-5 | Reduce template clutter and align nav with content | Done |

## Phase 27 - Formulas and spaces documentation

| Id | Task | Status |
|----|------|--------|
| 27-1 | Formulas reference doc (all calculations, sources) | Done |
| 27-2 | Spaces framework doc (definitions, expansion, gradient analogy) | Done |
| 27-3 | Analysis metrics doc (replayability, excitement, emergent) | Done |
| 27-4 | Add formulas to docs nav and link from reports | Done |
| 27-5 | Entity-input → space-position on Space Explorer (turn sync, reachability, KNN) | Done |

## Phase 28 - Space Explorer: 3D viz, Leva controls, KNN, deltas

| Id | Task | Status |
|----|------|--------|
| 28-1 | Add Phase 28 plan; deps: plotly, leva, ml-pca | Done |
| 28-2 | Build script: PCA 3D projection of content (skills, archetypes, dialogue) | Done |
| 28-3 | Space Explorer page: hero left, Plotly 3D center, Leva right (by space) | Done |
| 28-4 | Sliders drive player position; show in viz; KNN nearest content | Done |
| 28-5 | Color by archetype/skill branch; deltas/effects section | Done |
| 28-6 | Load report + turn selector; sync sliders from player state | Done |
| 28-7 | Docs formatting: code blocks, emphasis, section styling | Done |
| 28-8 | **Phase 28-02** Sectioned layout, collapsible blocks, JSON/3D toggle in viz panel | Done |
| 28-9 | **Phase 28-03** K-means clustering, colorBy cluster, reachability metrics, balancing PRD | Done |

## Phase 29 - Reporting & Formulas Polish

| Id | Task | Status |
|----|------|--------|
| 29-1 | **Phase 29-01** JSON viewer (react-json-view-lite), performance notes | Done |
| 29-2 | Browser report generation (run playthrough in browser, analyze, display) | Done |
| 29-3 | Space Explorer: slider for turn selector; Generate report when no report | Done |
| 29-4 | Excitement curve chart (Plotly); action coverage table | Done |

## Phase 30 - Game Value page and conceptual worth analysis

| Id | Task | Status |
|----|------|--------|
| 30-1 | Game Value page (`/play/reports/game-value`) with ReplayViewer, tabs (Summary, Replayability, Excitement, Emergent, Policies, Spaces) | Done |
| 30-2 | Reports index: nav cards, link to Game Value when report exists; Generate report redirects to Game Value | Done |
| 30-3 | Action Policies tab: list policies, edit agent-play-default priority order, Apply/Reset | Done |
| 30-4 | Space Explorer: Recompute PCA/K-means in browser (no t-SNE) | Done |
| 30-5 | Layout nav: Game Value link in Play section | Done |
| 30-6 | Game Value concept doc: conceptual vs implementation value; Escape the Dungeon vs DungeonBreak | Done |

## Phase 33 - Unified spaces model and behavior analysis

| Id | Task | Status |
|----|------|--------|
| 33-1 | Lock terminology: traits/features are shared dimensions reused by spaces, not standalone product spaces | Planned |
| 33-2 | Define canonical space contracts (entity/item/action/dialogue/event/room/level/effect/behavior) | In Progress |
| 33-3 | Add Event Space contract and report payload fields for trigger/context/outcome vectors | In Progress |
| 33-4 | Split effect modeling into delta effects and behavior effects with target scope and duration | In Progress |
| 33-5 | Define behavior timeline signature spec (window, sampling, aggregate features, similarity metrics) | In Progress |
| 33-6 | Extend Space Explorer with Event Space view and Behavior Lens controls | In Progress |
| 33-7 | Add generation loop: retrieve -> draft -> simulate -> score novelty/coverage/stability | Planned |
| 33-8 | Add feature-first `space-vectors.featureSchema` + runtime schema helpers (`getFeatureSchema`, `getSpaceFeatureIds`) | Done |
| 33-9 | KAPLAY runtime vector-pack loading and space hints in scene panels | Done |
| 33-10 | Engine docs + KAPLAY quickstart for content-space navigation workflow | Done |

## Phase 34 - Feature-first runtime space model

| Id | Task | Status |
|----|------|--------|
| 34-1 | Add feature-first `space-vectors.featureSchema` and runtime helpers | Done |
| 34-2 | Space Explorer runtime space switching (`content`/`action`/`event`/`effect`) | Done |
| 34-3 | Align docs/GRD terminology to feature-first model | Done |

## Phase 35 - Space Explorer multi-space UI

| Id | Task | Status |
|----|------|--------|
| 35-1 | Feature filters by `featureSchema.spaces` | In Progress |
| 35-2 | Baseline vs candidate comparison mode | Planned |
| 35-3 | Export feature configuration patch for content packs | Done |

## Phase 36 - Local policy-agent regression

| Id | Task | Status |
|----|------|--------|
| 36-1 | Policy selector in browser playthrough runner | Done |
| 36-2 | Deterministic fixture/report comparisons per policy | Planned |
| 36-3 | Local non-MCP regression docs | Planned |

## Phase 37 - KAPLAY construction and vector assignment docs

| Id | Task | Status |
|----|------|--------|
| 37-1 | Publish KAPLAY construction + vector assignment guide | Done |
| 37-2 | Link guide from engine docs/KAPLAY spec/GRD | Done |

## Phase 38 - Release hardening for pack-driven spaces

| Id | Task | Status |
|----|------|--------|
| 38-1 | CI parity checks for `spaceVectors` bundle hashes | Planned |
| 38-2 | Runtime override smoke tests (`spaceVectorsUrl`, contentFeaturesUrl) | Planned |
| 38-3 | Multi-space explorer smoke validation in docs pipeline | Planned |

## Phase 39 - Feature-first semantics simplification

| Id | Task | Status |
|----|------|--------|
| 39-1 | Default similarity/ranking to feature vectors only (semantic axes excluded) | Done |
| 39-2 | Document semantic axes as experimental with explicit TODO | Done |
| 39-3 | Prepare axis-definition framework (definition, source features, mapping formula) | Planned |

## Phase 40 - Content pack authoring flow (Space Explorer)

| Id | Task | Status |
|----|------|--------|
| 40-1 | Add docs-site API route to build full `content-pack.bundle.v1` from `spaceVectors` patch | Done |
| 40-2 | Extend Space Explorer model builder with presets, draft save/load, and restore-base controls | Done |
| 40-3 | Add patch validation and diff summary (current vs base bundle) in authoring panel | Done |
| 40-4 | Add full bundle download flow wired to `/api/content-packs/build-bundle` | Done |
| 40-5 | Add unit coverage for bundle builder route | Done |
| 40-6 | Next: add publish-to-release helper and docs report generation pipeline linkage | Planned |

## Phase 41 - Content pack release artifacts

| Id | Task | Status |
|----|------|--------|
| 41-1 | Add engine script to build release-ready content-pack artifacts (bundle + report + index) | Done |
| 41-2 | Wire release workflow to publish content-pack artifacts on tag builds | Done |
| 41-3 | Wire docs-browser workflow to emit content-pack artifacts on CI builds | Done |

## Phase 42 - Docs content-pack report ingestion

| Id | Task | Status |
|----|------|--------|
| 42-1 | Add content-pack report analyzer utility and persistence API (`/api/content-packs/reports`) | Done |
| 42-2 | Add docs report page (`/play/reports/content-packs`) for upload/list/inspect/open-in-explorer | Done |
| 42-3 | Load persisted report vectors into Space Explorer via `contentPackReportId` query param | Done |
| 42-4 | Add unit coverage for content-pack reports API route | Done |

## Phase 43 - Space explorer authoring e2e coverage

| Id | Task | Status |
|----|------|--------|
| 43-1 | Add e2e test for preset + draft authoring controls in Space Explorer | Done |
| 43-2 | Add e2e test for content-pack upload/report creation workflow | Done |

## Phase 44 - KAPLAY atomic component library baseline

| Id | Task | Status |
|----|------|--------|
| 44-1 | Expand atom primitives (surface/text/divider/button/keycap) with theme-token usage | Done |
| 44-2 | Expand molecule primitives (headers/stats/key legends) | Done |
| 44-3 | Refit grid scenes to shared organism shell composition | Done |
| 44-4 | Keep first-person on same shell and control language | Done |
| 44-5 | Verify standalone + agent parity smoke after refit | Done |

## Phase 45 - Grid and first-person refit completion

| Id | Task | Status |
|----|------|--------|
| 45-1 | Introduce template-level scene wrappers to reduce scene-local orchestration | Planned |
| 45-2 | Remove remaining duplicate panel/layout literals from scenes | Planned |
| 45-3 | Sync docs/spec snapshots with template contracts | Planned |

## Phase 46 - Human-playable input UX hardening

| Id | Task | Status |
|----|------|--------|
| 46-1 | Standardize key legends and control wording across scenes | Planned |
| 46-2 | Add controls/help overlay for keyboard + mouse discoverability | Planned |
| 46-3 | Tune spacing/readability for text-only graphics | Planned |

## Phase 47 - Agent and standalone parity hardening

| Id | Task | Status |
|----|------|--------|
| 47-1 | Add combined smoke gate for engine + kaplay + engine-mcp | Planned |
| 47-2 | Validate route/hotkey parity against scene contracts | Planned |
| 47-3 | Record and monitor deterministic parity-smoke hashes | Planned |

## Phase 48 - Release-ready text graphics polish

| Id | Task | Status |
|----|------|--------|
| 48-1 | Final iconography/spacing polish pass | Planned |
| 48-2 | Visual regression capture and checklist | Planned |
| 48-3 | Release-note summary for UI polish and known follow-ups | Planned |

## Phase 29 - Engine/KAPLAY parity gaps: inventory action slice

| Id | Task | Status |
|----|------|--------|
| 29-1 | Add engine inventory actions (`use_item`, `equip_item`, `drop_item`) with availability + execution rules | Done |
| 29-2 | Wire KAPLAY inventory UI buttons to engine inventory actions | Done |
| 29-3 | Add replay fixture coverage for new action types and refresh deterministic hashes | Done |
| 29-4 | Expand rune forge/combat/dialogue parity screens beyond stubs | Pending |
| 29-5 | Add authored content slice for new inventory/rune-forge loops | Pending |

## Docs-site / Admin stability

| Id | Task | Status |
|----|------|--------|
| DS-1 | Fix RangeError at String.repeat when opening admin (guard depth in llms routes) | Done |
| DS-2 | Track any remaining docs-site/admin runtime errors (e.g. email adapter warning) | Done (no release-blocking admin/runtime regressions in current verification sweep) |


]]></content>
  </doc>
  <doc>
    <path>STATE.md</path>
    <content><![CDATA[
# Project State

## Current Position

Phase: 44 (in progress)
Plan: 01 (atomic component baseline)
Status: Atomic KAPLAY component foundation is implemented and primary scenes are refit onto shared shell composition. Next execution focus is human-playable UX hardening (Phase 46).

Progress: [########--] 80% of current loop slice (44/45/47 baseline complete, 46/48 pending)

## Next Action

**Execute Phase 46-01** for human-playable UX hardening:

- standardize key legends and wording across every scene
- add explicit controls/help overlay
- tune spacing/readability for text-only graphics
- capture before/after scene screenshots for validation

## Planned Phases

| Phase | Focus |
|-------|-------|
| 44 | Atomic component library baseline (`atoms` -> `molecules` -> `organisms`) |
| 45 | Complete grid/first-person template refit and remove ad hoc scene drawing |
| 46 | Human-playable input UX hardening |
| 47 | Agent-play and standalone parity hardening |
| 48 | Release-ready text-graphics polish |

]]></content>
  </doc>
  <doc>
    <path>DECISIONS.md</path>
    <content><![CDATA[
# Decisions

## Lab and TypeScript package tooling

### Embeddings text-adventure demo architecture

- **Abstraction boundary:** The game engine depends on an `EmbeddingProvider` interface, not a specific model package. This keeps the core demo testable and reusable.
- **Fallback-first behavior:** A deterministic local embedding fallback is available by default; optional `sentence-transformers` improves semantics when installed (`uv sync --extra embeddings`).
- **Shared semantic projection path:** Player intent text, dialog/storylet placement, and placeable objects all use the same text->embedding->trait projection path so space semantics stay coherent.
- **Hybrid architecture:** Stateful orchestration uses object-oriented classes (session/game world), while scoring/projection/clamping math stays in pure functions for DRY behavior and easier verification.
- **Demo content policy:** If snapshot location/force data is missing, the demo uses explicit authored sample content instead of silently fabricating pretend live-game data.
- **Engine base:** Engine is TypeScript-first and framework-agnostic, consumed by docs-site and package consumers through stable APIs.
- **Domain split:** Escape the Dungeon implementation is partitioned into `world`, `entities`, `player`, `combat`, `narrative`, and `engine` modules to avoid monolithic logic files.
- **World scale:** Escape the Dungeon levels are fixed at 50 rooms each, with one start room and one exit room per level.
- **Simulation tick rule:** One action equals one turn. Player takes one action, then background NPCs each take one action.
- **Room vectors:** Rooms have base vectors and present items add room vector deltas; room vectors apply a small influence to acting entities each turn.
- **Dialogue range model:** Dialogue options live in vector clusters and are available only when entity+room context is within distance thresholds and room/item conditions are satisfied.
- **State-dependent options:** Options can appear/disappear based on room state, such as item present vs item absent conditions.
- **Phase 09 framing:** We will add a presentation layer where major triggers produce authored cutscene text blocks, while preserving deterministic simulation state updates.
- **Livestream baseline rule:** `live_stream` will spend **10 Effort per turn** and update `Fame` as a first-class feature.
- **Gating principle:** Actions/dialogue should be filtered by explicit prerequisite checks; unavailable options should be explainable (not hidden without reason in debug views).
- **Option A two-space model:** Keep semantic embeddings and explicit gameplay-feature vectors separate. Embeddings drive interpretable deltas through anchor projection instead of replacing core traits.
- **DeeD canonicalization:** DeeDs are embedded from stable canonical text (deterministic field order + normalized text), hashed, and cached with model metadata.
- **Delta safety policy:** Apply per-feature caps and a global per-turn budget when projecting semantic vectors into trait/feature deltas.
- **Livestream Fame context formula:** Fame gain is deterministic from effort, room/context factors, novelty/risk, optional skill bonus, and diminishing returns on current fame.
- **Exclusive perception branch:** `appraisal` vs `xray` is mutually exclusive per run; both remain valid branches but one run cannot unlock both.
- **Skill evolution venue:** Skill evolution choices are performed at `rune_forge` rooms only and are gated by explicit prerequisites.
- **Companion cap:** Maximum active companions is fixed at `1` for current scope.
- **Faction lethality gate:** Lethal/murder actions require both trait thresholds and faction/reputation checks; `Laughing Face` faction is explicitly represented as murder-aligned.
- **Room density targets:** Per level room composition target is `20 treasure`, `5 rune_forge`, and `1 exit boss room` within the fixed 50-room topology.
- **Population pressure targets:** Per level initialization target is `4 dungeoneers`; additionally, one hostile enemy spawns from the exit per turn and cannot enter rune forge rooms.
- **Event boundary policy:** We intentionally support both deterministic global events and probabilistic emergent triggers, and we will document which systems belong to each boundary.
- **Package release policy:** `@dungeonbreak/engine` is the primary distributable; release flow is semantic tags with package + docs integration gates.
- **Browser runtime policy:** Gameplay runtime for docs deployment is browser-only (`/play`) with no dedicated gameplay backend service.
- **UI pivot policy (Phase 13 recovery):** Ink Web terminal UI is deprecated for gameplay; `/play` moves to a structured 3-column interface.
- **Input policy (browser):** Gameplay is button-first with no required typed commands.
- **Assistant feed policy:** Assistant UI is used as the center feed presentation layer for narration, action outcomes, and dialogue/cutscene text.
- **Cutscene interaction policy:** Cutscenes are queued in a blocking modal flow and must be dismissed before new turn actions.
- **Release closeout target:** Package release follows semantic tags (`v*`) with docs + package CI gates required.
- **TS embedding baseline:** Browser embedding v1 uses deterministic hash projection (no model downloads in repo/executable), with model-based embeddings deferred.
- **Runtime governance update:** TypeScript runtime is the canonical gameplay source of truth across docs app and package consumers.
- **Terminology policy:** Use "action payload wiring" for gameplay action data flow; do **not** refer to this as Payload CMS plumbing.
- **Combat model policy (Phase 14):** Browser combat will not use a combat grid. Combat remains encounter simulation driven by equipped gear, stats, traits/features, passives/effects, room context, and internal skill-priority policy.
- **Player control policy (combat):** Player combat controls stay high-level (`fight` or `flee`), while the engine chooses detailed skill usage and priority resolution.
- **Dialogue/combat boundary policy:** Narrative dialogue choice UX is for non-combat interaction; combat turns expose combat-resolution outcomes, not full dialogue trees.
- **Depth/Level separation policy:** `depth` means dungeon floor position; `level` means character progression from XP. They are always represented and tested as distinct fields.
- **Reference-run policy:** A deterministic 25-turn integration playthrough is required and acts as a gating scenario for gameplay regressions.
- **Canonical replay seed policy:** v1 uses a single canonical replay seed (`CANONICAL_SEED_V1 = 20260227`) for golden trace and 25-turn reference tests.
- **Deed memory policy:** Deeds can be about self or other entities and may be incorrect; memory records include source and confidence to model misinformation.
- **Flee resolution policy:** Flee does not roll for hard failure. It resolves as deterministic movement to a legal adjacent room; hostiles may chase in subsequent turns.
- **Data portability policy:** Gameplay content contracts are JSON-schema based (actions, rooms, items, skills, cutscenes, vectors), with golden trace fixtures for cross-runtime replay.
- **Performance policy:** Browser turn loop target is `p95 <= 2s` with pressure cap `120` entities when items are modeled as entities; deterministic pruning/backpressure applies when limits are exceeded.
- **Telemetry policy:** Each build must emit a vector/feature usage report to identify underused mechanics and content for balancing and cleanup.
- **Python cutover update:** Python gameplay runtime is removed immediately from active repo development paths; recovery is via archived tags/releases only.
- **Notebook cutover update:** Gameplay notebooks are removed from active development scope; concept simulation remains in `scratch/` + `.concept/` markdown artifacts.
- **Lab continuity policy:** `npm run lab` and install helpers remain first-class and are retargeted to TypeScript/package workflows instead of notebook runtime.
- **Package distribution policy:** Engine is distributed as `DungeonBreak/engine` (implementation id `@dungeonbreak/engine`) with bundled default game data and out-of-box React component.
- **Lab install flow:** `npm run lab` remains the single entry for local setup and docs/package development helpers; it is no longer notebook-dependent.
- **Simulation semantics location:** Turn-by-turn simulation semantics are documented in `.planning/GRD-escape-the-dungeon.md` and `.concept/*` artifacts.
- **Cutover completion policy:** Python gameplay runtime and notebook assets are now removed from active mainline. TypeScript package/runtime is the single supported gameplay implementation.
- **Release artifact policy:** Tagged releases publish package artifacts (`@dungeonbreak/engine` tarball) to GitHub Releases after package checks and docs consumer checks pass.
- **Registry policy:** We do not publish gameplay package builds to npm registry by default; official distribution is GitHub Releases tarball and repo-based install paths.
- **Archetype compass policy (Phase 11):** Archetypes are first-class, schema-validated content packs and are recomputed after each turn for all entities so status/UI and behavior scoring stay aligned.
- **Content authoring policy (Phase 11):** Skills/dialogue/item growth is contract-driven (JSON pack -> parser -> runtime directors), avoiding hardcoded content tables in engine constructors.
- **Balancing harness policy (Phase 11):** Runtime balancing is validated through deterministic batch simulations and report scripts (`simulateBalanceBatch`, `report:balance-sim`, package balance harness smoke).
- **Phase 17 content policy:** Ongoing content expansion must remain contract-first (JSON/schema) and be validated by deterministic long-run simulation outputs before release.
- **MCP gameplay contract policy (Phase 18):** Gameplay engine behavior must be exposed through a machine-playable contract (session/state/actions/dispatch) that does not depend on UI rendering.
- **MCP integration policy (Phase 18):** MCP server adapter is maintained over engine APIs so `/play` and tool-driven flows stay aligned through shared engine/presenter contracts.
- **Local MCP onboarding policy:** Repo ships an installer helper (`scripts/install-mcp-config.mjs`) that merges DungeonBreak MCP server entries into Cursor (`~/.cursor/mcp.json`) and Codex (`~/.codex/config.toml`) configs.
- **Dense replay policy (Phase 18):** Agent regression uses a canonical dense fixture (`canonical-dense-trace-v1.json`, >=75 turns) with setup preconditions and hash lock for deterministic drift detection.
- **MCP parity gating policy:** CI must run MCP parity smoke (`test:parity-smoke`) to assert replay-hash equivalence between direct engine replay and MCP session dispatch.
- **Long-run balancing policy (Phase 17):** Deterministic suites run at 100/250/500 turn windows with canonical seed set and emit dead-action detection plus survival/archetype/performance metrics.
- **Deterministic runner policy:** Repo includes deterministic policy runner (`npm run agent:play`) that emits `.planning/test-reports/agent-play-report.json` for regression and report generation; it is not an LLM turn chooser.
- **Assistant Frame integration policy (Phase 19):** `/play` must expose Assistant Frame-compatible window bridge wiring for agent control, while preserving button-first gameplay as the default UX.
- **Remote MCP default policy (Phase 19):** `/api/mcp` is enabled by default in deployed runtime; local stdio MCP and browser/window-agent play remain first-class paths.
- **Remote access control policy:** Remote MCP usage requires authenticated signed-in users.
- **Remote hardening policy:** Remote MCP must always enforce baseline hardening (rate limiting, payload validation, session isolation, and audit metadata).
- **Release report versioning policy:** Play reports and test-result manifests are version-coupled to the shipped game build and published as release artifacts.
- **API doc generation scope policy:** OpenAPI generation is deferred; current docs focus on manual MCP tool/API documentation and engine SDK docs.
- **LLM gameplay scope policy (Phase 20):** LLM/autonomous turn choosing is de-scoped for foreseeable delivery. Docs must explicitly state MCP support exists, but autonomous model-driven gameplay is not currently implemented.
- **Future autonomous-play prerequisites policy:** Before enabling LLM turn choice, we require deterministic tool-call sandboxing, reproducible policy evaluation traces, bounded action budgets, and separate reliability/perf gates from baseline deterministic replay checks.
- **Legacy phase archive policy:** Early Narrative Engine implementation phases (03/04) remain documented for history but are formally superseded by the Escape the Dungeon runtime track and excluded from active delivery gates.

### Narrative Engine (plugin) — state and dialog

- **State model:** N-dimensional basis-vector space (e.g. Courage, Loyalty, Hunger). Entity position = `FVectorND`; velocity inferred from position history (Verlet). **Verlet:** `new_p = p + (p - old_p) + a*dt²`; then clamp `p` to bounds and **reproject `old_p`** so implied velocity respects the boundary (slide). Per-axis min/max (and optionally global norm) keep state in a valid region.
- **Dialog in state space:** Dialog options have a **Location** (point in same N-D space). Selection = spatially oriented: **saliency** by distance (closer = more relevant; e.g. Gaussian falloff). **Threshold:** when entity is within a distance threshold of a dialog location, that option is **available** (e.g. show in menu); when several above threshold: show all, pick random among them, or pick closest with variety. See GRD and runtime reference docs.
- **Qualities — what lives where:** Position in basis-vector space, needs (as basis vectors), relationships/alignment → Narrative Engine. Physical location (world XYZ) → Quaterra. Combat stats, inventory → Combat/Economy. Time → Narrative Engine (Scene).

---

## Payload (docs-site) schema and migrations

- **Schema push disabled:** The SQLite adapter uses `push: false`. All schema and data changes go through **migrations** in `docs-site/migrations/`.
- **Workflow after changing Payload schema:** From docs-site: run `pnpm migrate:create`, then `pnpm migrate`. If the change **renames or removes columns** (or changes field shape), edit the new migration’s `up` to **backfill new columns from old**, then alter/drop old columns; then run `pnpm migrate`. Commit new migration files.
- **First-time setup:** From docs-site, run `pnpm migrate` before first `pnpm dev` so tables exist.

---

## Phase-level decisions

- **Verification config (Phase 01):** Disabled `docs`/`build`/`tests` and added a single no-op `genericCommands` check (`node -e "process.exit(0)"`) so `forge-loop verify-work 1 --strict` can pass on this repo. The default forge-loop profile expects `pnpm forge-loop:test`, which is not set up here. When we add a real test suite, we can re-enable or point verification at it.
- **Canonical identifiers (Phase 31):** Engine is the single source of truth for `actionType` and `roomFeature`. Consumers (KAPLAY, engine-mcp, docs-site) must use `PLAYER_ACTION_TYPES`/`ROOM_FEATURES` from `@dungeonbreak/engine`, not magic strings. `engine.status()` exposes `roomFeature` so UI derives context from structured data, not prose parsing. **Action** = engine primitive; **Option** = contextual choice shown to player; options map to actions.

---

## Design decisions (GDC / research → application)

Key design decisions and rationale, informed by GDC talks, procedural generation research, and DungeonBreak's design pillars. Agents and tooling should use these when reasoning about systems.

### 1. Perceptual Uniqueness over Mathematical Uniqueness

**Decision**: Prioritize perceptual differentiation in characters and narrative over mathematically unique content.

**Rationale** (Kate Compton, *Practical Procedural Generation for Everyone*): The "10,000 bowls of oatmeal" problem—you can generate 10,000 bowls where each oat is in a different position and orientation; mathematically they are unique, but the user sees a lot of oatmeal. What matters is *perceptual uniqueness*: players must perceive differentiation. Design for meaning, not combinatorics.

**Application**: Character traits, dialog variants, and world events should feel meaningfully different to the player, not merely statistically varied.

---

### 2. Orthogonal Traits with Higher-Order Meaning

**Decision**: Use orthogonal (non-overlapping) character traits; layer archetypes/suits for coherence.

**Rationale** (Tanya X. Short, *Writing Modular Characters for System-Driven Games*): Traits like flirtiness and extraversion overlap in the behaviors they create (both govern whether a character wants to talk to you). Players won't see the difference. Use orthogonal axes (e.g., romantic interest vs. extroversion) so values combine meaningfully. At the same time, add higher-order meaning—suits, archetypes, themes—to shape the "bucket" of content, not just the water inside. Emily Short's *Nines of the Perixx* tagged corpus by suits (salt, beeswax, venom, mushroom, egg) to create perceptual regions.

**Application**: Define orthogonal axes for character generation; apply thematic archetypes (e.g., elements, tarot suits) for coherent flavor across systems.

---

### 3. Authoring Tools over Complex AI

**Decision**: Design prompts and authoring tools; let players construct narratives from those prompts.

**Rationale** (Matt Brown, *Emergent Storytelling Techniques in The Sims*): The Sims uses an AI-minimalist approach. Authoring tools proved more valuable than complex AI for creating extensive narrative content. The game provides interesting prompts; players bear responsibility for constructing stories. Prioritize coherence and retellability over complexity.

**Application**: Invest in rich authored content, story trees, and authoring workflows. Use systems to *match* player actions to content rather than to *generate* narratives from scratch.

---

### 4. Linear Structure with Branching Agency

**Decision**: Fix the A-to-B destination (linear world); allow variable characters and events along the path.

**Rationale** (DungeonBreak vision): One Piece's plot illustrates a constant destination with any path of human will between points. Diverse, linear map with a single path ahead; towns and dungeons offer localized exploration. Bifurcations in choice create new universes to explore within the same space.

**Application**: Main world path is linear; POIs populate dynamically from narrative canon. Player choices in dialog alter which characters and events appear along that path.

---

### 5. Story Volume over Story Lines

**Decision**: Define a *story volume*—the family of emergent stories—rather than a single story or fixed branch set.

**Rationale** (Tanya X. Short, Jason Grimm): A story volume "encloses a family of emergent stories created by a set of carefully curated system parameters." Parameters (themes, setting, societal collapse, love ballads, moon festivals) define what is *allowed*, not what *should* happen. Orthogonality ensures the volume is rich and explorable.

**Application**: Curate parameters that bound DungeonBreak's story space—dungeon breaks, guild politics, time travel, training, alignment—without scripting every outcome.

---

### 6. Local Intelligence over Long-Term Planning

**Decision**: NPC AI should ensure consistency in immediate behavior sequences; avoid complex long-term planning.

**Rationale** (The Sims): Local intelligence allows players to construct their own long-term narratives. Consistency promotes player personality attribution; some randomness prevents exploitable repetition. Story trees are matched against player actions and scored by interest and NPC personality.

**Application**: NPCs react to player momentum. Focus on coherent short sequences and clear cause-effect; let emergence arise from player interpretation.

---

### 7. Surfacing Meaning Quickly

**Decision**: Surface character meaning to the player as quickly as possible; don't hide perceptual uniqueness.

**Rationale** (Tanya X. Short): Priority order—(1) what has changed (if re-encounter), (2) unique trait reminder, (3) current status, (4) finer details on opt-in. Avoid stacking too much into one channel (e.g., a thousand greeting variants); use orthogonal broadcasting. Don't mimic naturalist pacing (physical first, name second)—players forget names; give meaningful info upfront.

**Application**: First-second re-encounter: show what changed. Then unique trait. Status and detail on demand.

---

### 8. Procedural Generation Techniques

**Decision**: Use tiles, grammars, and distribution; apply additive and subtractive iteration.

**Rationale** (Kate Compton): Tiles for chunk-based assembly (maps, cards). Grammars for recursive structure (Tracery, L-systems). Distribution with hierarchy and clustering (bolding, footing, greebling). Additive: build good stuff. Subtract: whitelist good seeds, generate-and-reject, or use search/genetic algorithms. Be flexible; follow the generator when it leads.

**Application**: Dungeon and POI layout (tiles/grammars); character and event distribution (hierarchical, clustered); iterate with subtractive curation where needed.

---

### 9. Centers and Coherence (Nature of Order)

**Decision**: Design narrative and world elements as "centers" with varying degrees of "life" and coherence.

**Rationale** (Jesse Schell, *The Nature of Order in Game Narrative*): Christopher Alexander's patterns characterize persistent entities. Stories supported by these universal patterns resonate more and have lasting appeal. Design for replayability and shareability across generations.

**Application**: Ensure narrative beats, locations, and characters form coherent wholes that feel alive and complete rather than arbitrary fragments.

---

### 10. Iterate with Systems as Co-Authors

**Decision**: Treat the corpus of data and the systems that combine them as co-authors; iterate on both.

**Rationale** (Tanya X. Short): Writers and system designers must iterate together. Different co-authors (e.g., Theophrastus vs. medieval pope descriptions) add different flavor. Measure what "common" and "too surprising" mean; allow exceptions and handwritten flourishes that break rules.

**Application**: Cycle between content iteration and system tuning. Define rarity/commonness; use analytics to tune surprise levels.

---

### Summary: Key Tensions Resolved

| Tension | Resolution |
|--------|------------|
| Perceptual vs mathematical uniqueness | Favor perceptual differentiation |
| Orthogonality vs higher-order meaning | Orthogonal traits + archetype layers |
| Authoring vs emergence | Author prompts; players construct stories |
| Linear structure vs player agency | Linear path; variable characters/events |
| Consistency vs randomness | Consistent short sequences; controlled randomness |

]]></content>
  </doc>
  <doc>
    <path>ERRORS.md</path>
    <content><![CDATA[
# Errors and Attempts

## 2026-03-01 - Disable non-essential template OG/LLMS routes to stabilize Vercel publishes

- Context: Vercel builds kept failing during prerender on template routes (`/docs-og/...` and `/llms.mdx/...`) with `No response is returned from route handler`, while core docs and `/play` were otherwise buildable.
- Cause: Base-template route handlers remained in the docs-site app and were still included in static generation paths even though they are not required for current DungeonBreak shipping scope.
- Mitigation applied:
  - Disabled `docs-site/app/(fumadocs)/docs-og/[...slug]/route.tsx` route behavior:
    - `GET` now returns 404
    - `generateStaticParams` now returns `[]`
  - Disabled `docs-site/app/(fumadocs)/llms.mdx/[...slug]/route.ts` route behavior:
    - `GET` now returns 404
    - `generateStaticParams` now returns `[]`
  - Verified local loop checks:
    - `pnpm --dir docs-site run build`
    - `pnpm --dir docs-site run build:vercel-parity`

## 2026-02-28 - Vercel prerender failed in `docs-og` route with missing response

- Context: Vercel build for commit `c8e770b` failed while prerendering `/docs-og/.../image.png` with `No response is returned from route handler`.
- Cause: `docs-site/app/(fumadocs)/docs-og/[...slug]/route.tsx` used `notFound()` inside a route handler branch, which does not reliably satisfy route-handler response expectations in Next 16 prerender paths.
- Mitigation applied:
  - Changed route handler to always return explicit `Response` objects for invalid/missing slug and missing page branches.
  - Added route-level `try/catch` fallback returning an OG `ImageResponse` so unexpected runtime exceptions cannot fail prerender with a missing-response error.
  - Added local reproduction command in `docs-site/package.json`:
    - `pnpm --dir docs-site run build:vercel-parity` (`CI=1`, `NEXT_PRIVATE_BUILD_WORKER=1`).

## 2026-02-28 - Vercel docs-site build failed to resolve `@dungeonbreak/engine`

- Context: Vercel production build failed at `next build --webpack` with repeated `Module not found: Can't resolve '@dungeonbreak/engine'` from `/play` and `/api/mcp` imports.
- Cause: local file dependency bootstrap only ran in `preinstall`; on clean deploy environments this can leave the installed package without hydrated `dist` when the app build begins.
- Mitigation applied:
  - Added a second bootstrap pass after install and before build in:
    - `docs-site/package.json`
      - `postinstall`: `node scripts/ensure-engine-dist.mjs && fumadocs-mdx`
      - `prebuild`: `node scripts/ensure-engine-dist.mjs`

## 2026-02-27 - Tag workflow failed on `v0.1.0`

- Context: `Terminal Game CI and Release` run for tag `v0.1.0` failed before build jobs started.
- Failing job: `Docs Browser Checks`.
- Failing step: `Setup Node` in both browser workflow and terminal workflow docs gate.
- Mitigation applied:
  - Removed pnpm cache wiring from `actions/setup-node` in:
    - `.github/workflows/docs-browser-game.yml`
    - `.github/workflows/terminal-game-release.yml`
  - Re-run publish flow after pushing workflow fix.

## 2026-02-27 - Tag workflow failed on docs build env assumptions

- Context: re-run of `v0.1.0` failed in `Docs Browser Checks` at `Build docs-site`.
- Cause: CI job did not provide required Payload/S3 environment variables used by docs-site config.
- Mitigation applied:
  - Added deterministic CI placeholder env vars to docs build jobs in:
    - `.github/workflows/docs-browser-game.yml`
    - `.github/workflows/terminal-game-release.yml`

## 2026-02-27 - Tag workflow failed only at release publish step

- Context: `Terminal Game CI and Release` run `#13` for `v0.1.0` passed tests and all 3 binary builds, then failed at `Publish GitHub Release`.
- Cause: release action step failure was not visible without authenticated logs; likely rerun/create-upload edge behavior.
- Mitigation applied:
  - Replaced `softprops/action-gh-release` with explicit `gh`-based create/upload logic in:
    - `.github/workflows/terminal-game-release.yml`
  - New flow is idempotent (`gh release view` then `gh release create` if missing; `gh release upload --clobber`).

## 2026-02-27 - Release publish failed due duplicate asset names across OS

- Context: run `#17` published `v0.1.0` but still failed the release job.
- Observed state: release existed with only one uploaded asset (`escape-the-dungeon.exe`), while linux/macos binaries shared the same filename (`escape-the-dungeon`).
- Cause: release upload step collided on duplicate asset names from different OS builds.
- Mitigation applied:
  - Normalized artifact filenames per matrix OS before upload:
    - `escape-the-dungeon-ubuntu-latest`
    - `escape-the-dungeon-macos-latest`
    - `escape-the-dungeon-windows-latest.exe`
  - Updated release artifact glob to `artifacts/**/escape-the-dungeon-*` in:
    - `.github/workflows/terminal-game-release.yml`

]]></content>
  </doc>
  <doc>
    <path>REQUIREMENTS.md</path>
    <content><![CDATA[
# Requirements: DungeonBreak

## v1 Requirements

### Planning baseline

- [x] **REQ-01**: PROJECT.md reflects DungeonBreak and Narrative Engine scope and core value
- [x] **REQ-02**: ROADMAP.md defines Phase 01 (planning docs baseline) and Phase 02+ with goals and requirements
- [x] **REQ-03**: Phase 01 has captured context (desired outcome, constraints, non-goals) and at least one plan with tasks and must-haves

### Planning docs and simulations

- [x] **REQ-06**: PRD and decisions in .planning; docs/ not extended. Notebooks in `notebooks/` visualize 3D state space, saliency, triggering, and Verlet+constraints (Phase 02)

### Narrative Engine (implementation)

- [x] **REQ-04**: Narrative Engine Verlet-first implementation track is archived/superseded after Escape the Dungeon runtime pivot (Phase 03 legacy closure)
- [x] **REQ-05**: Narrative Engine dialog-saliency first-slice implementation track is archived/superseded after Escape the Dungeon runtime pivot (Phase 04 legacy closure)

### Docs and tooling

- [x] **REQ-07**: docs/ only image-catalog (+ images). Game PRD, design decisions, implementation roadmap in .planning. Notebook venv + JupyterLab from root (Phase 05)

- [x] **REQ-08**: `npm run lab` remains the single entry for local setup/install helpers after TypeScript cutover; no notebook dependency is required (Phase 15 target update)

### Escape the Dungeon demo

- [x] **REQ-09**: PRD in `.planning` defines canonical concepts, architecture, and definition of done for **Escape the Dungeon** (12-level dungeon crawler demo) (Phase 07)
- [x] **REQ-10**: Reusable Python game modules are split by domain (world/entities/player/combat/narrative/engine) and use vendored adventurelib as base engine (Phase 07)
- [x] **REQ-11**: Notebook demo supports Kael gameplay loop with level/chapter/act progression, NPC background simulation, quests, and chapter page logs (Phase 07)
- [x] **REQ-12**: Free-text intent projection (embeddings + deterministic fallback) updates trait vector slice in gameplay flow (Phase 07)

### Escape the Dungeon simulation depth (Phase 08)

- [x] **REQ-13**: Concrete world objects (`Dungeon`, `Level`, `Room`) model 12 levels with 50 rooms per level; each level has exactly one start room and one exit room
- [x] **REQ-14**: Room vectors + room item vectors are represented explicitly and influence entity trait vectors each turn
- [x] **REQ-15**: Dialogue options are attached to vector anchors/clusters with distance ranges and room/item conditions; options can be listed and chosen through engine APIs
- [x] **REQ-16**: CLI entry point exists for Escape the Dungeon and uses vendored adventurelib command flow
- [x] **REQ-17**: Planning documentation includes a plain-language (teen-readable) guide for classes, vectors, and simulation flow

### Escape the Dungeon presentation and gating layer (Phase 09)

- [x] **REQ-18**: Cutscene/event system triggers authored scene popups for item/quest/stat milestones and logs them to chapter pages
- [x] **REQ-19**: Action and dialogue availability are filtered by explicit prerequisites and can explain why options are unavailable
- [x] **REQ-20**: Skills are represented in narrative space with vector profiles and prerequisite-based unlock/use rules
- [x] **REQ-21**: Livestream action exists with Fame effects and Effort cost fixed at 10 per turn
- [x] **REQ-22**: Notebook and CLI both expose filtered options, cutscene presentation, and livestream gameplay state

### Escape the Dungeon emergent parity and faction pressure (Phase 10)

- [x] **REQ-23**: Player and non-player entities use the same action catalog and prerequisite checks for turn execution
- [x] **REQ-24**: Level composition supports 20 treasure rooms and 5 rune forge rooms per level while preserving 50-room total
- [x] **REQ-25**: Exit room boss exists on every level and 4 dungeoneers are present per floor at initialization
- [x] **REQ-26**: A hostile enemy spawns from level exit every turn, seeks entities, and cannot enter rune forge rooms
- [x] **REQ-27**: Skills support evolution at rune forge rooms and enforce run-exclusive branch `appraisal` vs `xray`
- [x] **REQ-28**: Companion system exists with max active companions = 1, recruitable from dungeoneers by alignment checks
- [x] **REQ-29**: Faction/reputation + trait checks gate lethal and murder actions; `Laughing Face` faction is represented
- [x] **REQ-30**: Livestream/deed rumor propagation between entities exists and influences dialogue/action availability
- [x] **REQ-31**: Entity/player level curves are modeled and influence combat and decision systems
- [x] **REQ-32**: Deterministic global events and probabilistic emergent triggers are both implemented with explicit boundary docs and tests

### Terminal release packaging and version pipeline (Phase 12)

- [x] **REQ-33**: CI workflow runs Python test suite for pull requests and pushes before release artifact generation
- [x] **REQ-34**: Cross-platform terminal binaries (Windows/macOS/Linux) are built from the CLI entrypoint and uploaded as artifacts
- [x] **REQ-35**: Tag-based GitHub release pipeline (`v*`) publishes terminal game binaries with versioned assets

### Browser runtime and parity gating (Phase 13)

- [x] **REQ-36**: Browser game route exists at `docs-site/app/(fumadocs)/play/page.tsx` and is playable with a 3-column button-first UX (no required command typing)
- [x] **REQ-37**: Browser runtime uses TypeScript engine with documented parity matrix against Python baseline
- [x] **REQ-38**: Browser persistence supports autosave + named slots (IndexedDB adapter + memory fallback)
- [x] **REQ-39**: Homepage links prominently to `/play` without replacing docs-first landing purpose
- [x] **REQ-40**: Browser test coverage includes world topology, action gating, branching/exclusivity, hostile pressure, pages/logs, and snapshot restore
- [x] **REQ-41**: CI includes a docs/browser workflow (typecheck/build/unit/e2e smoke)
- [x] **REQ-42**: Release gating requires engine package pipeline success and browser docs workflow success before publish
- [x] **REQ-43**: `/play` presents three columns: action lists (left), Assistant UI feed (center), and character/status panels (right)
- [x] **REQ-44**: Cutscenes and critical dialogue moments are surfaced in a blocking modal queue before further actions
- [x] **REQ-45**: Playwright validates clickable gameplay loop output and state updates without command-bridge typing
- [x] **REQ-46**: Tagged release flow publishes downloadable `@dungeonbreak/engine` package tarball artifacts to GitHub Releases (no npm registry publish required)

### Browser combat simulation parity (Phase 14)

- [x] **REQ-47**: Combat uses encounter simulation factors (equipment, stats, traits, room context, effects, skill policy) and does **not** introduce a combat grid
- [x] **REQ-48**: Player combat input surface is high-level only (`fight`, `flee`); users do not micromanage per-skill priority queues
- [x] **REQ-49**: Dialogue choice simulation remains a non-combat interaction flow; combat interactions are limited to combat-resolution decisions and outcomes
- [x] **REQ-50**: Browser test coverage adds deterministic combat/flee scenarios and long-run turn-performance assertions for entity pressure
- [x] **REQ-51**: A deterministic 25-turn reference playthrough exists in tests and exercises all core systems (movement, room vectors, deeds, dialogue, combat/flee, skills, cutscenes, pages, rumors)
- [x] **REQ-52**: Deed memory supports uncertain/incorrect beliefs (`misinformed`) about self and other entities, with source tracking and confidence
- [x] **REQ-53**: Flee has no fail roll; it resolves as deterministic movement to an adjacent legal room, and chase can still occur on later turns
- [x] **REQ-54**: Action-to-outcome mapping is explicit and machine-readable (data contract), including formula constants and delta caps
- [x] **REQ-55**: Shared content contracts are JSON schema driven (actions, items, skills, traits, cutscenes, room templates) for TS now and C++/other runtimes later
- [x] **REQ-56**: Golden trace replay fixtures exist for deterministic cross-runtime parity checks and use a single canonical seed for v1 (`CANONICAL_SEED_V1`)
- [x] **REQ-57**: Automated vector/feature usage reporting exists and flags low-usage or unused dimensions/content
- [x] **REQ-58**: Browser turn processing meets performance target (`p95 <= 2s`) under pressure cap `120` entities (when items are modeled as entities) with deterministic pruning/backpressure policy
- [x] **REQ-59**: TypeScript runtime is the canonical gameplay source of truth; Python runtime is removed from active gameplay development

### TypeScript cutover and package distribution (Phase 15)

- [x] **REQ-60**: Python gameplay runtime and dead gameplay code are removed from active mainline paths, with archive/tag retained for historical recovery
- [x] **REQ-61**: Python gameplay runtime is removed from active repo paths immediately, with archival access preserved via tags/releases only
- [x] **REQ-62**: Notebook artifacts are removed from active development scope; concept simulation docs remain text-first under `scratch/` and `.concept/`
- [x] **REQ-63**: Package `DungeonBreak/engine` is installable without npm publish via GitHub release tarball and repository-based install flows
- [x] **REQ-64**: Package includes a production-ready React component that renders a playable game out of the box
- [x] **REQ-65**: Package bundles default game content/data (rooms, items, skills, cutscenes, vectors) so consumers can run without external data setup
- [x] **REQ-66**: Repository includes a complete working example app consuming the package APIs/components (docs-site `/play` + package-consumer tests)
- [x] **REQ-67**: CI and docs validate package build, install, and runtime smoke integration from consumer perspective

### Content scale and balancing closure (Phase 11)

- [x] **REQ-68**: Archetype compass data is schema-validated and available to runtime consumers with deterministic heading classification per turn
- [x] **REQ-69**: Skills/dialogue/items are expanded through data-driven contract packs and consumed by engine builders without hardcoded content tables
- [x] **REQ-70**: Balancing simulation harness exists with deterministic batch metrics and report generation scripts for engine/package/docs workflows

### Content production and long-run balancing loops (Phase 17)

- [x] **REQ-71**: Content packs (skills/dialogue/items/events/quests) expand through schema-validated JSON contracts with no hardcoded authoring growth
- [x] **REQ-72**: Long-run deterministic simulations (100/250/500 turns) are executed in CI-facing suites with stable summary metrics
- [x] **REQ-73**: Balance report artifacts include action usage, archetype distribution, survival/escape rates, and dead-content detection
- [x] **REQ-74**: Performance and pressure budgets remain enforced (`p95 <= 2s`, cap 120 with deterministic pruning) under expanded content load

### MCP gameplay interface and deterministic replay harness (Phase 18)

- [x] **REQ-75**: Engine exposes a machine-playable turn API contract for controlled tool orchestration (create session, inspect state, list legal actions, dispatch action)
- [x] **REQ-76**: MCP server adapter is available to expose gameplay tools/resources without browser UI dependency
- [x] **REQ-77**: A dense deterministic playthrough suite (>= 75 turns) validates broad interactions (combat/flee, dialogue, rumors, skills/evolution, faction gates, companions, cutscenes, chapter/act/page logs)
- [x] **REQ-78**: Regression tests assert deterministic outputs from identical seed + action scripts across repeated runs
- [x] **REQ-79**: MCP docs define tool schemas, action payloads, response formats, and failure semantics for deterministic runner flows
- [x] **REQ-80**: `/play` UX and MCP interface remain behaviorally aligned through shared presenter/engine contracts

### Assistant Frame window-agent integration and default remote MCP (Phase 19)

- [x] **REQ-81**: `/play` exposes Assistant Frame-compatible bridge wiring so host/window agents can operate gameplay without typed command input
- [x] **REQ-82**: Browser gameplay remains fully usable without `/api/mcp` and degrades cleanly to button/DOM-driven operation for agents that do not consume frame bridges
- [x] **REQ-83**: Remote MCP delivery at `/api/mcp` is enabled by default in deployed docs runtime
- [x] **REQ-84**: Remote MCP access requires authenticated signed-in users; unauthenticated requests are rejected
- [x] **REQ-85**: Remote MCP hardening baseline is always enforced (rate limiting, session isolation, payload validation, audit log metadata)
- [x] **REQ-86**: Docs include clear integration guides for local stdio MCP, Assistant Frame/window-agent mode, and signed-in remote MCP mode
- [x] **REQ-87**: Play reports are versioned with the exact game build/release version and published as release artifacts
- [x] **REQ-88**: Each published build includes a machine-readable pass/fail test manifest (unit/integration/e2e/parity/perf) as a release artifact

### Report schema streaming and LLM scope policy (Phase 20)

- [x] **REQ-89**: Split report artifacts support JSONL event-row streams so viewers can iterate timeline data without hydrating full external ledger JSON into memory
- [x] **REQ-90**: LLM turn-chooser/autonomous gameplay remains out of scope for foreseeable delivery; docs must clearly state MCP support exists but autonomous model-driven play is not currently implemented, plus list required future changes

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| REQ-01 | Phase 01 | Done |
| REQ-02 | Phase 01 | Done |
| REQ-03 | Phase 01 | Done |
| REQ-06 | Phase 02 | Done |
| REQ-04 | Phase 03 | Superseded (archived) |
| REQ-05 | Phase 04 | Superseded (archived) |
| REQ-07 | Phase 05 | Done |
| REQ-08 | Phase 06 | Done |
| REQ-09 | Phase 07 | Done |
| REQ-10 | Phase 07 | Done |
| REQ-11 | Phase 07 | Done |
| REQ-12 | Phase 07 | Done |
| REQ-13 | Phase 08 | Done |
| REQ-14 | Phase 08 | Done |
| REQ-15 | Phase 08 | Done |
| REQ-16 | Phase 08 | Done |
| REQ-17 | Phase 08 | Done |
| REQ-18 | Phase 09 | Done |
| REQ-19 | Phase 09 | Done |
| REQ-20 | Phase 09 | Done |
| REQ-21 | Phase 09 | Done |
| REQ-22 | Phase 09 | Done |
| REQ-23 | Phase 10 | Done |
| REQ-24 | Phase 10 | Done |
| REQ-25 | Phase 10 | Done |
| REQ-26 | Phase 10 | Done |
| REQ-27 | Phase 10 | Done |
| REQ-28 | Phase 10 | Done |
| REQ-29 | Phase 10 | Done |
| REQ-30 | Phase 10 | Done |
| REQ-31 | Phase 10 | Done |
| REQ-32 | Phase 10 | Done |
| REQ-33 | Phase 12 | Done |
| REQ-34 | Phase 12 | Done |
| REQ-35 | Phase 12 | Done |
| REQ-36 | Phase 13 | Done |
| REQ-37 | Phase 13 | Done |
| REQ-38 | Phase 13 | Done |
| REQ-39 | Phase 13 | Done |
| REQ-40 | Phase 13 | Done |
| REQ-41 | Phase 13 | Done |
| REQ-42 | Phase 13 | Done |
| REQ-43 | Phase 13 | Done |
| REQ-44 | Phase 13 | Done |
| REQ-45 | Phase 13 | Done |
| REQ-46 | Phase 13 | Done |
| REQ-47 | Phase 14 | Done |
| REQ-48 | Phase 14 | Done |
| REQ-49 | Phase 14 | Done |
| REQ-50 | Phase 14 | Done |
| REQ-51 | Phase 14 | Done |
| REQ-52 | Phase 14 | Done |
| REQ-53 | Phase 14 | Done |
| REQ-54 | Phase 14 | Done |
| REQ-55 | Phase 14 | Done |
| REQ-56 | Phase 14 | Done |
| REQ-57 | Phase 14 | Done |
| REQ-58 | Phase 14 | Done |
| REQ-59 | Phase 14 | Done |
| REQ-60 | Phase 15 | Done |
| REQ-61 | Phase 15 | Done |
| REQ-62 | Phase 15 | Done |
| REQ-63 | Phase 15 | Done |
| REQ-64 | Phase 15 | Done |
| REQ-65 | Phase 15 | Done |
| REQ-66 | Phase 15 | Done |
| REQ-67 | Phase 15 | Done |
| REQ-68 | Phase 11 | Done |
| REQ-69 | Phase 11 | Done |
| REQ-70 | Phase 11 | Done |
| REQ-71 | Phase 17 | Done |
| REQ-72 | Phase 17 | Done |
| REQ-73 | Phase 17 | Done |
| REQ-74 | Phase 17 | Done |
| REQ-75 | Phase 18 | Done |
| REQ-76 | Phase 18 | Done |
| REQ-77 | Phase 18 | Done |
| REQ-78 | Phase 18 | Done |
| REQ-79 | Phase 18 | Done |
| REQ-80 | Phase 18 | Done |
| REQ-81 | Phase 19 | Done |
| REQ-82 | Phase 19 | Done |
| REQ-83 | Phase 19 | Done |
| REQ-84 | Phase 19 | Done |
| REQ-85 | Phase 19 | Done |
| REQ-86 | Phase 19 | Done |
| REQ-87 | Phase 19 | Done |
| REQ-88 | Phase 19 | Done |
| REQ-89 | Phase 20 | Done |
| REQ-90 | Phase 20 | Done |

]]></content>
  </doc>
  <doc>
    <path>REFERENCES.md</path>
    <content><![CDATA[
# References for agents

When implementing narrative systems, state-space math, character traits, or procedural content, **agents should review these references** so design matches the intended research and GDC guidance.

## GDC / talks (video and transcripts)

- **Tanya X. Short – Writing Modular Characters for System-Driven Games**  
  Orthogonal traits, higher-order meaning (suits, archetypes). Referenced in [DECISIONS.md](DECISIONS.md).  
  - Video: https://www.youtube.com/watch?v=qX5-2D8SP5A  
  - Use when: defining basis vectors, character axes, narrative state dimensions.

- **Kate Compton – Practical Procedural Generation for Everyone**  
  Perceptual uniqueness vs mathematical uniqueness; "10,000 bowls of oatmeal."  
  - Search: GDC Kate Compton procedural generation  
  - Use when: tuning variety, avoiding perceptual redundancy.

- **Matt Brown – Emergent Storytelling Techniques in The Sims**  
  Authoring tools over complex AI; matching player actions to content.  
  - Search: GDC Matt Brown Sims storytelling  
  - Use when: dialog/storylet selection, authoring workflows.

- **Jesse Schell – The Nature of Order in Game Narrative**  
  Centers and coherence; Christopher Alexander patterns.  
  - Search: GDC Jesse Schell Nature of Order narrative  
  - Use when: narrative beats, world coherence.

## Where to look in this repo

- **Design decisions and narrative engine (state, Verlet, saliency, threshold):** [.planning/DECISIONS.md](DECISIONS.md)  
- **Game and narrative scope (vision, pillars, loops):** [.planning/PROJECT.md](PROJECT.md)  
- **Implementation phases:** [.planning/implementation-roadmap.md](implementation-roadmap.md)  
- **Phases and goals:** [.planning/ROADMAP.md](ROADMAP.md)

Agents: review the relevant talk (video/transcript) when changing narrative or state-space behavior so the implementation stays aligned with the cited design.

]]></content>
  </doc>
  <doc>
    <path>PROJECT.md</path>
    <content><![CDATA[
# Project

## What This Is

**DungeonBreak** – linear world, branching narrative RPG. This planning workspace (forge-loop) tracks delivery in traceable phases and plans. We use it to flesh out planning docs, break down work, and run discuss → plan → execute → verify cycles.

## Game and narrative scope (folded from prior PRDs)

- **Vision:** Emergent, system-driven RPG: narrative branches from player choices within a linear world; tactical RTS-lite combat; rich dialog and alignment. **Pillars:** Linear world, branching narrative (A-to-B fixed, path of human will varies); stop the Dungeon Breaks; TTRPG feel (walk and talk, companions); character-driven emergence (time-spend simulation).
- **Core loops:** Interaction (dialog choices, effort ⚡, alignment; Friendly/Hostile toggle); combat (RTS-lite, auto-attack); time-spend economy (characters progress with game time).
- **Narrative Engine (plugin):** Owns **narrative state** (N-D position in basis-vector space, e.g. Courage, Loyalty, Hunger). **Game state** = actors, world (Quaterra). Entity position = thematic state; dialog options live at **locations** in that space; selection by **saliency** (distance) and optional **threshold** (available if within radius). Verlet integration; constraints (clamp, reproject old_p). Alignment (faction + individual + history); canon and subjective knowledge (designed; implementation TBD). Integration: DialogComponent, HostilityComponent, Yarn Spinner.
- **Qualities:** Position in basis-vector space, needs (as basis vectors), relationships/alignment → Narrative Engine. Physical location (world XYZ) → Quaterra. Combat stats, inventory → Combat/Economy.

## Core Value

Ship tested, traceable slices. Each phase has a goal, requirements, and plans; execution produces summaries and verification so we know what was done and what’s next.

## Requirements

### Validated

None yet.

### Active

- [ ] Align PROJECT, ROADMAP, REQUIREMENTS with DungeonBreak and Narrative Engine scope
- [ ] Complete Phase 01: Planning docs baseline (context, plans, task breakdown)
- [ ] Define Phase 02+ (e.g. Narrative Engine first slice: Verlet + constraints)
- [x] Deliver Phase 07: Escape the Dungeon (adventurelib base + modular dungeon crawler demo)
- [x] Deliver Phase 08: vector-space simulation depth (50-room levels, room vectors, dialogue ranges, CLI)
- [ ] Deliver Phase 09: cutscene presentation + prerequisite gating + skill vectors + livestream fame economy

### Out of Scope (for now)

- Full GSD parity in v1
- Implementing Narrative Engine code before planning baseline is stable

## Key References

Former standalone `game-PRD.md`, `narrative-engine-PRD.md`, and `narrative-engine-developer-guide.md` have been removed; their content is folded into PROJECT (Game and narrative scope), DECISIONS (Narrative Engine subsection), and implementation-roadmap.

- **Design decisions and rationale:** `.planning/DECISIONS.md`
- **Phases and goals:** `.planning/ROADMAP.md`
- **Requirements traceability:** `.planning/REQUIREMENTS.md`
- **Implementation phases (Ralph Wiggum loop):** `.planning/implementation-roadmap.md`
- **Embeddings text-adventure PRD:** `.planning/PRD-text-adventure-embeddings-demo.md`
- **Simple engine guide (teen-readable):** `.planning/escape-the-dungeon-teen-guide.md`
- **GDC/references for agents:** `.planning/REFERENCES.md`
- **Simulation notebooks:** `notebooks/` — single entry: `npm run lab`. **dungeonbreak-narrative.ipynb** teaches axis semantics (Courage, Loyalty, Hunger), dialogue by meaning, how Kaiza reaches options (training, dialog, forces), and time simulation. **dungeonbreak-text-adventure.ipynb** is a playable text-adventure using the same narrative model (state cube, events, effort, quest regions). **verlet-constraints.ipynb** is math-only reference. The narrative engine is in **`src/dungeonbreak_narrative/`** (shared by notebooks and game).
- **Images:** `docs/image-catalog.md` (images in docs/)

]]></content>
  </doc>
  <doc>
    <path>PRD-content-balancing.md</path>
    <content><![CDATA[
# PRD: Content Space Balancing

**Status:** Draft - future work

## Problem

If skills (or dialogue options) are too far in an evaluation space from vectors a player can realistically reach through gameplay, that content becomes unreachable. We need:

1. **Visibility** - Aggregated distance metrics (skills in range, minimum distance, mean distance to nearest `N`)
2. **Adjustment** - Move content vectors, gating radii, or player capability vectors so content becomes reachable
3. **Balancing** - Understand whole-space effects when one content point changes

## Aggregated Metrics (Implemented)

- **Skills in range** - Count of skills where `d(playerVector, skillVector) <= unlockRadius`
- **Minimum distance to skill** - Distance to nearest skill vector
- **Mean distance to nearest 5** - Average distance to the five closest skills
- **Reachable IDs** - Skill IDs currently in range

## Balancing Strategies

1. **Move content closer** - Reduce distance from content clusters to typical player trajectories
2. **Tune unlock radius** - Adjust skill reachability without collapsing progression
3. **Add jump mechanics** - Deeds/items/events that project vectors toward otherwise distant content
4. **Adjust action deltas** - Room influence and action deltas so typical paths traverse more useful regions

## Future Tooling

- **Balance algorithm** - Suggest vector shifts or radius changes to reach target coverage
- **Trajectory simulation** - Run `N` playthroughs, record player vectors over time, and visualize coverage
- **Content placement optimizer** - Given target "X% reachable by turn Y", suggest `vectorProfile` / `unlockRadius` updates

## References

- [Space Explorer](/play/reports/spaces) - reachability panel and distance metrics
- [Spaces doc](/docs/formulas/spaces) - canonical vector/space definitions
- [Formulas](/docs/formulas) - unlock rule `d(playerVector, skillVector) <= unlockRadius`

]]></content>
  </doc>
  <doc>
    <path>PRD-formulas-and-spaces.md</path>
    <content><![CDATA[
# PRD: Formulas, Spaces, and Documentation

## Document Status

- Owner: DungeonBreak docs/lab team
- Date: 2026-02-28
- Status: Draft
- Location: `.planning/PRD-formulas-and-spaces.md`

## Purpose

Surface all game calculations, analysis metrics, and vector/space models in docs. Explain reasoning, expansion strategies, and integrate with reporting.

---

## Goals

1. **Formulas visible** - Every major calculation documented: action effects, combat damage, grouped-vector deltas, room influence, deed projection, archetype ranking, skill unlock distance, dialogue option distance, analysis metrics.
2. **Reasoning** - Why formulas were chosen, what question each formula answers, and how space expansion changes reachable content.
3. **Spaces framework** - Mathematical definitions for space size, content density, and clusters across skill/dialogue/archetype/deed evaluations.
4. **Terminology consistency** - Feature = labeled scalar, vector = grouped feature array, fundamental spaces compose into compounding spaces.
5. **Reporting integration** - Input entity stats (player at turn `N`) -> show position in all relevant spaces and run content-pack evaluations.
6. **Docs location** - MDX/MD in docs site, with TypeDoc for package API as complement.

---

## Spaces and Terminology

Phase 33 terminology lock:

- There is no standalone product concept called "trait space".
- Traits and features are canonical **dimensions** reused by many spaces.
- A space is a typed object family plus its projection/similarity rules.

| Space | Core object | Dimensions used | Position / Similarity rule |
|-------|-------------|-----------------|-----------------------------|
| **Entity Space** | `EntityState` | traits + features + attributes + status axes | weighted cosine / euclidean over selected dimensions |
| **Item Space** | item definitions + instances | trait deltas + tags + rarity + behavior hooks | nearest-neighbor by effect profile |
| **Action Space** | `PLAYER_ACTION_TYPES` actions | intent + gating + expected deltas + risk/cost | utility/policy score + embedding distance |
| **Dialogue Space** | clusters/options | context traits + room feature + prerequisites + effects | `distanceBetween(context, anchor) <= radius` + gate predicates |
| **Event Space** | deterministic/emergent events | trigger vector + context vector + outcome deltas | trigger match + vector proximity + occurrence stats |
| **Room Space** | `RoomNode` | `RoomFeature` + base vector + item tags + exits | room-context similarity + reachability |
| **Level Space** | level metadata | aggregate room features + pressure curves + pacing metrics | structural similarity and pacing distance |
| **Effect Space** | effect definitions | target scope + delta payload + duration/stacking | effect-profile distance |
| **Behavior Space** | sampled effect trajectories | time-series signature features | cosine + DTW/shape distance on sampled windows |

**Space size** (formal): `|S| =` count of typed points in that space.  
**Content density:** points per unit volume (or per cluster) in projected coordinates.

### Effect taxonomy

1. `deltaEffect`: direct scalar changes (instant or over duration).
2. `behaviorEffect`: transition rules over time (for example regeneration, periodic pulses, conditional triggers).

Both are first-class and may co-exist in one effect definition.

### Behavior signatures (timeline modeling)

For behavior similarity, each effect can emit a short sampled run:

- fixed window `T` (for example 5 seconds)
- sample interval `dt`
- signature features: totals, min/max, slope, variance, oscillation count, time-to-peak

This supports "same net result, different cadence" comparisons.

---

## Formulas Inventory (Source of Truth)

- `action-formulas.json` - trait/feature deltas per action
- `packages/engine/.../combat/system.ts` - combat damage formula
- `core/types.ts` - Euclidean distance, clamp, vector magnitude
- `narrative/archetypes.ts` - cosine scoring weights
- `narrative/skills.ts` - distance-based skill unlock
- `narrative/dialogue.ts` - distance-based option availability
- `narrative/deeds.ts` - AnchorProjector and deed projection budget
- `engine/game.ts` - room influence scale and effective room vector
- `playthrough-analyzer.ts` - entropy, excitement, novel combinations

---

## Reporting Integration (Future)

- Entity input: snapshot at turn `N` from playthrough report
- Output: position in each evaluation space (nearest skill distance, archetype ranking, dialogue clusters in range)
- Clusters: group content by vector and show nearest cluster per space
- Batch mode: run calculations over content packs and emit per-space metrics

---

## Tasks (Phase 27)

- 27-1: Formulas reference doc (all calculations, symbols, sources)
- 27-2: Spaces framework doc (definitions, expansion, navigation)
- 27-3: Analysis metrics doc (replayability, excitement, emergent)
- 27-4: Embed in `/docs` and link from `/play/reports`
- 27-5: (Future) Entity-input -> space-position calculator on reports page

]]></content>
  </doc>
  <doc>
    <path>PRD-text-adventure-embeddings-demo.md</path>
    <content><![CDATA[
# PRD: Escape the Dungeon (Adventurelib-Based Dungeon Crawler Demo)

## Document Status

- Owner: DungeonBreak docs/lab team
- Date: 2026-02-28
- Status: Working v15 (Phase 19 complete: Assistant Frame + default remote MCP + versioned release reports shipped)
- Location rule: PRD lives in `.planning` (not `docs/`)
- **Related:** [GRD-escape-the-dungeon.md](GRD-escape-the-dungeon.md) defines concrete gameplay behavior and the gameplay discovery loop. Simulation in `scratch/game-state.md`; agent guide in `.concept/SIMULATION-AGENT-GUIDE.md`.

## Product Name

**Escape the Dungeon**

## Product Vision

A modular, extendable dungeon crawler runtime with:

- **TypeScript browser target** mounted at docs route `/play` (primary and canonical demo runtime).
- **TypeScript package target** distributed as `DungeonBreak/engine` via GitHub Releases tarball (and repo install paths) with bundled data and installable React component.

The gameplay model is:

- We start at the lowest dungeon level as **Kael**.
- The player is unaware there are **12 total levels** to escape.
- Each level is navigated as a room graph.
- NPC entities simulate in the background (Sims-like), performing the same action classes as the player.
- Narrative state uses traits/features (vector slice), chapter logs, and act progression.

## Core Premise

Kael awakens in the deep dungeon and must climb to the surface through 12 levels. Every action by Kael and NPCs contributes to chapter pages; four completed chapters form one act.

## Scope

### In Scope (Current Demo Slice)

- Browser runtime in `docs-site` using TypeScript, shadcn layout primitives, and Assistant UI feed components.
- Playable docs route at `/play` with button-first action selection, blocking cutscene queue, autosave, and slot persistence.
- Browser parity matrix tracking TypeScript engine/package behavior across app and consumer examples.
- Domain-based modular architecture:
  - `world` (levels, rooms, navigation)
  - `entities` (player/NPC state, traits, attributes, inventory)
  - `player` (action intent types)
  - `combat` (encounter simulation system, no combat grid)
  - `narrative` (dialogue, chapter pages, act logic, embeddings projection)
  - `engine` (orchestration, package API surface, app integration)
- 12-level dungeon world generation with **50 rooms per level**.
- Room features (training, dialogue, rest, treasure, combat, stairs, escape gate).
- Quest tracking.
- Chapter/act/page logging for player and each NPC.
- Embeddings-backed trait projection with deterministic fallback provider.
- Dialogue-space ranges and cluster-based option availability.
- Cutscene/event presentation planning for major milestones.
- Action filtering/gating planning based on prerequisites.
- Fame + livestream planning with effort cost economy.
- Next-slice planning for full entity parity, faction conflict, skill evolution, and high-density room/content population.
- Package + docs/browser publish gates: package build/tests and docs/browser workflow must both pass before release tagging.
- 25-turn deterministic reference playthrough coverage as a release gate.
- Shared JSON content contracts for cross-language portability (TS now, C++ plugin path next).
- Automated vector/feature usage report to flag low-value or unused dimensions/content.
- Installable package distribution with bundled game data and working React component consumer example.
- Data-driven archetype, dialogue-cluster, skill, and item packs with deterministic balancing harness/report tooling.
- Machine-playable MCP interface over canonical engine APIs (`packages/engine-mcp`) for deterministic tool-driven control and report generation.
- Assistant Frame-compatible window-agent bridge on `/play` so in-window agents can drive gameplay without typed command flows.
- Remote MCP mode (`/api/mcp`) enabled by default for signed-in users in deployed runtime.
- Version-coupled release artifacts for play reports and build test pass/fail manifests.

### Out of Scope (This Slice)

- Full Unreal gameplay parity.
- Networked multiplayer.
- Full content-authoring pipeline and advanced UI/FX.
- Python gameplay runtime and notebook gameplay surfaces.

## Definitive Concepts in Code

1. **World Topology**
- Dungeon depth from level 12 (start) to level 1 (escape gate level).
- Each level contains exactly 50 rooms.
- Each level has one start room and one exit room.

2. **Action Economy**
- Core actions: move, train, talk, rest, search, speak-intent, fight, flee.
- NPCs run background actions after player turns.

3. **Combat Simulation (No Grid)**
- Combat is resolved as encounter simulation, not a positional combat grid.
- Resolution factors include equipment, entity stats, traits/features, passives/effects, room context, and skill policy.
- Player combat controls remain high-level (`fight` / `flee`), while detailed skill choice is engine-driven.

4. **Traits/Attributes/Items**
- Traits are vector-like feature state (from canonical trait names).
- Attributes are RPG scalar stats (might, agility, insight, willpower).
- Items are discoverable per room and move into inventory.
- Rooms and items both carry vectors that can influence entity vectors.

5. **Narrative Structure**
- Each dungeon level maps to one chapter.
- 4 chapters = 1 act.
- Chapter logs contain:
  - global chapter page
  - per-entity page (Kael + each NPC)
- Both player and NPC actions write to pages.

6. **Embeddings Projection**
- Free-text intent is projected to trait deltas using an embedding provider.
- Fallback provider must work offline/no-model-install.
- DeeDs use canonical text -> embedding -> anchor projection with per-feature caps and global turn budget.

7. **Dialogue Space**
- Dialogue options are anchored to vectors.
- Options belong to clusters with radius ranges.
- Room state conditions (for example item present/absent) can enable/disable options.

8. **Cutscene and Event Presentation**
- High-signal events can trigger authored scene text blocks ("cutscenes").
- Cutscenes can be triggered by specific items, quest transitions, stat milestones, or major world events.
- Cutscene events are written to chapter pages so story history is auditable.

9. **Action Availability Filtering**
- Every action and dialogue option can declare prerequisites.
- Prerequisites may depend on room state, inventory, attributes, traits, skills, entity state, or other entities.
- UI/shell must only present currently available actions/options.

10. **Skills as Vector-Bound Build Layer**
- Skills exist in narrative space and carry vector profiles.
- Skill unlock/use depends on vector distance + prerequisites.
- Item + room + entity combinations can create emergent build paths.

11. **Livestream Economy**
- `Fame` is a feature/trait in the vector set.
- Action: `live_stream`.
- Action cost: spend `Effort` 10 per turn.
- Livestream outcomes can influence Fame, dialogue options, and quest opportunities.
- Fame gain is deterministic from context formula components (room interest, novelty, risk, momentum/skill bonus, diminishing returns).

12. **Shared Action Parity**
- Non-player entities use the same action catalog as the player.
- An entity may execute any action only when prerequisites pass.
- NPC behavior policy scores those same legal actions each turn.

13. **Skill Evolution and Exclusive Branching**
- Skills can evolve into higher-tier skills at player/NPC choice if prerequisites pass.
- Skill evolution requires visiting a `rune_forge` room.
- `appraisal` vs `xray` is a mutually exclusive run-level branch.

14. **World Population and Room Distribution**
- Each level keeps 50 rooms and now reserves:
  - 20 treasure rooms
  - 5 rune forge rooms (safe haven type)
  - 1 exit room with a level boss guardian
  - 4 non-player dungeoneers present at level start

15. **Hostile Pressure Loop**
- Each turn, a hostile enemy spawns from the current level's exit room.
- Spawned hostiles path toward other entities and prefer combat actions.
- Hostiles cannot enter rune forge rooms.

16. **Faction, Reputation, and Lethality Escalation**
- Faction model includes `Laughing Face` (murder-guild alignment).
- Murder-capable actions require both:
  - trait thresholds
  - faction/reputation thresholds
- Escalation path supports "attack -> lethal attack -> murder" style progression.

17. **Companion System**
- Max companions at once: `1`.
- Dungeoneers can be recruited by alignment in traits/info/faction/attributes/archetype space.

18. **Deterministic vs Emergent Boundaries**
- Deterministic rails:
  - dungeon topology, win condition, key authored event chain, explicit prerequisite evaluation
- Probabilistic/emergent layer:
  - NPC action selection among legal actions, rumor spread, encounter timing, local social outcomes

19. **Package Distribution**
- The engine is distributed as installable package `DungeonBreak/engine` (implementation id `@dungeonbreak/engine`) via GitHub Releases tarball and repository install paths.
- Package ships with bundled default game data and React component(s) that render playable experience out of the box.
- Build/release automation is handled by CI workflows and semantic version tags.

20. **Deed Memory and Misinformation**
- Deeds are attached to entities and can describe self or other entities.
- Deed memories include belief quality (`verified`, `rumor`, `misinformed`), source, and confidence.
- Dialogue and action scoring may use incorrect deed beliefs, intentionally modeling social misinformation.

21. **Concrete Reference Playthrough**
- A deterministic 25-turn reference run must exist and be testable.
- v1 uses one canonical replay seed: `CANONICAL_SEED_V1 = 20260227`.
- That run exercises all core systems together (movement, vectors, deeds, skills, cutscenes, combat/flee, rumors, pages).
- This scenario is treated as a parity and regression gate.

22. **Flee Contract**
- `flee` has no hard failure roll.
- It resolves as deterministic movement to a legal adjacent room.
- Pursuers can still chase on later turns under normal movement rules.

23. **Reusable Cross-Language Data Contracts**
- Actions, vectors, skills, items, room templates, cutscenes, and formula constants are authored in schema-validated JSON.
- Engine code in each language consumes these contracts instead of duplicating content logic.
- Golden trace fixtures verify deterministic replay across runtimes.

24. **Performance and Pressure Boundaries**
- Browser gameplay target is responsive turn resolution (`p95 <= 2s`) with pressure cap `120` when items are modeled as entities.
- When pressure exceeds budget, deterministic pruning/backpressure rules are applied and logged.

25. **Archetype Compass and Balancing Harness**
- Archetype headings/classes are computed from schema-backed archetype vectors every turn and surfaced in status/UI.
- Balance simulation batch APIs and report scripts are required to evaluate content pacing and emergent class distribution.

## Architecture Requirements

### Required Domain Split

- No monolithic text-adventure file.
- Game logic must be separated by domain packages.
- Browser app and package consumers should call engine APIs, not contain business logic.

### Runtime Target Policy

- Browser TS runtime is the primary playable demo surface (`/play`).
- TypeScript runtime is the canonical behavior source.
- Python gameplay runtime is removed from active development scope.
- No gameplay backend service is introduced for browser play.
- Remote MCP endpoint support is default-on in deployed runtime.
- Remote MCP access requires signed-in authenticated users with hardening always enabled.

### Browser UX Policy

- `/play` uses a 3-column layout:
  - left: grouped clickable action lists
  - center: Assistant UI feed for narration, action outcomes, dialogue, and cutscenes
  - right: player status, vectors, quests, and nearby entity context
- Command typing is optional/future only; core gameplay must be fully playable via buttons.
- Cutscenes are presented as a blocking queue modal before next-turn actions.
- Combat UI is no-grid and exposes high-level controls (`fight` and `flee`) while encounter details stream to the feed.
- `/play` should support Assistant Frame/window-host integration as a non-breaking enhancement over normal button play.

### Agent Integration Policy

- Local MCP control remains supported through stdio MCP (`packages/engine-mcp`).
- Browser window agents can use DOM/button interaction and, when available, Assistant Frame bridge wiring.
- Remote MCP (`/api/mcp`) is baseline in deployed runtime for signed-in users.
- Autonomous LLM turn choosing is out of scope for foreseeable delivery.
- OpenAPI generation for gameplay APIs is deferred; docs use manual MCP/SDK references for now.

### Engine Base Requirement

- Engine is framework-agnostic TypeScript core consumed by docs app and package consumers.
- Package exports stable interfaces and React component(s) for out-of-box integration.

### Engineering Practices

- OOP for stateful orchestration (`EscapeDungeonGame`, session/shell objects).
- Functional/pure utility style where practical (projection/math transforms).
- DRY + SOLID for interfaces and extension points.

## Gameplay System Requirements

1. GR-01: Start game at depth 12 as `Kael`.
2. GR-02: Dungeon has 12 levels; player-facing UI does not reveal full count by default.
3. GR-03: Every level has 50 rooms, exactly one start room, and exactly one exit room.
4. GR-04: Specific room capabilities exist (train, talk, rest, search/combat, travel).
5. GR-05: NPCs simulate in background and can move/train/talk/rest/speak.
6. GR-06: Items, traits, attributes, quests all exist in runtime model.
7. GR-07: Chapter page logs persist per chapter for all entities.
8. GR-08: 4 completed chapters mark act completion.
9. GR-09: Embeddings intent path updates trait vector slice.
10. GR-10: Room vectors + present item vectors influence entities each turn.
11. GR-11: Dialogue options are range-gated by vector distance and room/item conditions.
12. GR-12: High-signal events can trigger cutscene pop-ups with authored text.
13. GR-13: Action and dialogue visibility are filtered by prerequisite checks.
14. GR-14: Skills are represented in narrative space and can unlock emergent action builds.
15. GR-15: Livestream action exists and consumes 10 effort per turn while modifying Fame and related systems.
16. GR-16: Every level has exactly 20 treasure rooms, 5 rune forge rooms, and one exit-room boss.
17. GR-17: Exactly 4 dungeoneers spawn per floor at initialization.
18. GR-18: One hostile enemy spawns from the exit room every turn and seeks other entities.
19. GR-19: Hostile enemies cannot enter rune forge rooms.
20. GR-20: NPCs and player share the same action catalog and prerequisite rules.
21. GR-21: Skills support evolution trees at rune forge rooms.
22. GR-22: `appraisal` and `xray` are run-exclusive branches.
23. GR-23: Companion system exists with max active companions = 1.
24. GR-24: Reputation/faction checks and trait checks gate lethal/murder actions.
25. GR-25: `Laughing Face` faction exists and can influence murder-oriented behavior.
26. GR-26: Rumors of deeds (including livestreamed deeds) can spread entity-to-entity.
27. GR-27: Per-entity pages capture interaction logs (room, action/dialogue, counterparts).
28. GR-28: Entity/player level curves exist and affect combat/action outcomes.
29. GR-29: Deterministic global events and probabilistic emergent triggers both exist with explicit boundaries.
30. GR-30: Combat uses encounter simulation (equipment/stats/traits/effects/room context/skill policy) and does not require a combat grid.
31. GR-31: During combat, player controls are high-level (`fight` or `flee`) and do not require per-skill micromanagement.
32. GR-32: Dialogue choice UX is non-combat; combat turns surface combat outcomes and flee results.
33. GR-33: Combat simulation is deterministic for fixed seed/snapshot and testable in docs runtime and package consumer runtime.
34. GR-34: `depth` (dungeon floor) and `level` (character progression) are represented as distinct fields in all APIs, logs, and tests.
35. GR-35: A deterministic 25-turn integration playthrough exists and must exercise all core systems in one scenario.
36. GR-36: Deeds support self-target and other-target references plus misinformation states (`verified`, `rumor`, `misinformed`) with source and confidence.
37. GR-37: `flee` resolves as deterministic room movement with no hard fail roll; chase behavior is handled by normal subsequent turns.
38. GR-38: Action outcome formulas and constants are stored in schema-validated data contracts, not hidden in ad hoc UI logic.
39. GR-39: Shared JSON schemas and data packs exist for cross-language reuse (TS runtime now, C++ plugin ingestion later).
40. GR-40: Golden trace replay fixtures verify deterministic behavior for TS docs runtime and package consumer runtime.
41. GR-41: Build/test pipeline emits a vector/feature usage report highlighting low-usage and unused features/content.
42. GR-42: Browser turn processing meets performance target (`p95 <= 2s`) under configured entity pressure, with deterministic pruning policy.
43. GR-43: Golden replay and 25-turn integration tests use one canonical v1 seed (`CANONICAL_SEED_V1 = 20260227`).
44. GR-44: Pressure profile cap is 120 when items are represented as entities; otherwise cap applies to active simulated entities only.
45. GR-45: Python gameplay runtime and gameplay notebooks are removed from active repo paths immediately; archived releases/tags are the recovery mechanism.
46. GR-46: `DungeonBreak/engine` is published as downloadable GitHub Releases tarball package (implementation id `@dungeonbreak/engine`) with bundled default game data.
47. GR-47: Package exports out-of-box React component(s) and typed engine APIs that run without external services.
48. GR-48: `npm run lab` and install helpers remain operational after cutover and target TypeScript/package workflows.

## Package Consumer UX Requirements

1. UX-01: One-command local setup (`npm run lab`) remains available after cutover.
2. UX-02: Package consumer can render playable game component with minimal wiring.
3. UX-03: Consumer UI receives status/feed/actions without custom backend.
4. UX-04: Bundled default data enables immediate play without data migrations.
5. UX-05: Cutscene/dialogue/action output is visible in consumer example.
6. UX-06: Consumer example documents override/extension points for custom data.

## Package API Requirements

1. API-01: Export stable engine creation and action dispatch APIs.
2. API-02: Export React component(s) that wrap engine lifecycle and persistence defaults.
3. API-03: Export default content pack and schemas for validation/custom extension.

## Definition of Done

The current slice is done when:

1. **Engine and Architecture**
- Modular package split exists by required domains.
- Monolithic gameplay logic is replaced by domain modules.

2. **Gameplay**
- Kael can traverse levels and rooms.
- NPC background simulation executes each turn.
- Items/traits/attributes/quests are active and mutable.
- Chapter/act/page logic is operational for player + NPCs.
- Every level has 50 rooms with one start and one exit.
- `depth` and `level` remain distinct in engine state, logs, and UI.
- Room and item vectors influence entities.
- Dialogue options can appear/disappear when room state changes.
- Combat encounter simulation works without a combat grid and respects equipment/stats/effects/room context.
- Combat control surface is high-level (`fight` / `flee`) with deterministic resolution for a fixed seed/snapshot.
- `flee` always resolves as legal movement; chase can still happen in later turns.
- Deed memory supports self/other targets and misinformation states with source/confidence.

3. **Embeddings**
- Free text action updates trait state through embedding projection.
- Deterministic hash projection baseline is always available in browser runtime.

4. **Browser `/play`**
- Docs site route `/play` is playable with a 3-column button-first UX (actions/feed/status).
- Assistant UI feed clearly displays narration, action outcomes, dialogue, and cutscenes.
- Blocking cutscene modal queue is enforced before additional actions.
- Assistant Frame-compatible window-agent wiring exists and does not break normal button-first play.
- Homepage links to `/play`.
- Browser autosave + named slots work.
- Browser unit + e2e smoke tests pass in CI.
- Browser turn processing meets `p95 <= 2s` in configured pressure scenario (cap 120 when items are entities).
- Remote MCP is enabled by default for signed-in users and does not break local/browser-only play.
- Each published game build ships version-matched play reports and test pass/fail manifest artifacts.

5. **Package Distribution**
- Package `DungeonBreak/engine` (implementation id `@dungeonbreak/engine`) builds and installs cleanly from GitHub Releases tarball.
- Package includes bundled default game data and playable React component exports.
- Repository includes a complete working consumer example using published package APIs.
- CI validates package build/install/smoke integration from consumer perspective.

6. **Planning Traceability**
- ROADMAP, REQUIREMENTS, TASK-REGISTRY, STATE align to this PRD and phase.

7. **Presentation and Filtering**
- Cutscenes can be triggered and logged.
- Action visibility/prerequisite filtering is in place.
- Livestream fame/effort loop is playable.
- Unit tests and e2e tests validate core mechanics and integration points.

8. **Contracts and Reuse**
- JSON schemas/data packs define shared gameplay content contracts.
- Golden trace replay fixtures exist for deterministic parity checks.
- Automated vector/feature usage report is generated and reviewable in CI artifacts.
- Python gameplay runtime and notebooks are removed from active repo development paths.

9. **Reference Playthrough**
- A deterministic 25-turn integration playthrough test exists and passes.
- The reference run uses canonical seed `CANONICAL_SEED_V1 = 20260227`.
- The playthrough covers movement, vectors, deeds, dialogue, combat/flee, skill unlock/evolution, cutscenes, rumors, and page logging.

## Open Design Decisions

1. Cutscene authoring format (template strings vs small DSL vs data JSON).
2. Precondition language for action gating (code predicates vs declarative rule schema).
3. Fame economy tuning (reward curves, diminishing returns, penalties).
4. Default NPC population and spawn distribution per level.
5. Level curve tuning (player, dungeoneers, hostiles, bosses) and scaling constants.
6. Rumor confidence decay model and propagation radius.
7. Companion loyalty decay/growth model and betrayal conditions.
8. Combat encounter weighting model (how equipment/stats/traits/effects/room context combine into action resolution).
9. Max-entity pressure budget before deterministic pruning triggers.
10. Package naming/publishing policy detail (`DungeonBreak/engine` branding vs implementation id constraints and GitHub Releases distribution).

]]></content>
  </doc>
  <doc>
    <path>PRD-tooling-gameplay-analysis.md</path>
    <content><![CDATA[
# PRD: Tooling for Gameplay Analysis & Reporting

## Document Status

- Owner: DungeonBreak docs/lab team
- Date: 2026-02-27
- Status: Draft
- Location: `.planning/PRD-tooling-gameplay-analysis.md`

## Purpose

Tooling to analyze Escape the Dungeon playthroughs: compute replayability, excitement, and emergent scores; display reports on the docs site; surface content pack changes. Scientific, formulaic approach to understand how the game plays.

---

## Metric Definitions

### Replayability

**Definition:** How many distinct playthrough paths exist given current content.

**Formula / Approach:**
- Over N runs: `unique_action_sequences / total_possible`
- Action diversity index per run: Shannon entropy of action-type usage; higher = more varied
- Count distinct (action, roomFeature) pairs visited
- Flag: if agent can only `move` for entire run → replayability = 0; emit warning

### Excitement

**Definition:** Per-turn "interesting event" density.

**Formula / Approach:**
- Per-turn score: count of high-signal events (cutscene, skill_unlock, combat, dialogue, treasure, fame_milestone)
- Rolling window average (e.g. 5 turns)
- Excitement curve: plot score vs turn index
- Sufficient threshold: if actionCoverage.missing includes all non-move actions for whole run → insufficient excitement

### Emergent

**Definition:** Unpredictable but coherent outcomes from system rules. Novel (first-seen) event combinations; entity-state trajectories that diverge from scripted paths.

**Formula / Approach:**
- Novel event combinations: first-seen (actionType, depth, roomFeature, entityKind) tuples
- Entity trajectory divergence: trait/feature delta from baseline run
- "Surprise" score: entropy of event-type distribution vs expected

---

## Tooling Scope

### Playthrough Analyzer

- **Input:** `agent-play-report.json` (schema `agent-play-report/v2`)
- **Output:** Analysis report JSON with `replayability`, `excitement`, `emergent` sections
- **Implementation:** `packages/engine-mcp/src/playthrough-analyzer.ts`
- **CLI:** `pnpm --dir packages/engine-mcp run analyze` or similar

### Report Viewer

- **Route:** `/reports` or `/play/reports`
- **Sections:** Playthrough summary, Excitement curve, Action coverage, Emergent highlights, Content pack changelog
- **Data:** Load latest `agent-play-report.json` + analysis; fallback to placeholder if missing

### Content Pack Diff

- **Input:** `contracts/data/*.json` at two version tags
- **Output:** Per-file diff (added/removed/changed keys)
- **Display:** `/content` route; version selector

### CI Integration

- Run agent-play on merge (or scheduled)
- Publish report artifacts to `.planning/test-reports/`
- Optional: block if excitement/replayability below configurable thresholds

---

## UX for Reports

- **Audience:** Stakeholders, colleagues, users
- **Layout:** Summary cards → Excitement curve (chart) → Action coverage table → Emergent highlights list → Content changelog
- **Links:** Download standalone HTML, GitHub releases
- **No auth:** Public; reports are static from CI or local runs

---

## Sufficient Excitement Threshold

**Rule:** If agent can only `move` and no other action ever becomes available, we need new systems.

**Implementation:** In playthrough analyzer, flag runs where `actionCoverage.missing` includes all non-move actions for the whole run. Emit warning; optionally fail CI.

]]></content>
  </doc>
  <doc>
    <path>PRD-ui-engineering-aesthetic.md</path>
    <content><![CDATA[
# PRD: Engineering/Scientific UI Aesthetic

**Status:** In progress  
**Scope:** Docs-site, Play, Planning, Kaplay integration

---

## Goals

1. **Text-heavy, subtle** — Engineering/scientific team preference; reduce visual noise.
2. **Less rounded** — Reduce border-radius across the site.
3. **Remove gradient** — Flat backgrounds; no decorative gradients.
4. **Compact cards** — Smaller, denser cards on Play and reports.
5. **Sidebar + ToC everywhere** — All pages show nav sidebar; content pages show ToC where applicable.
6. **Better planning markdown** — Planning docs render cleanly.

## Kaplay ASCII Grid

- **Fix:** "Styled text error: unclosed tags" — Escape `[` and `]` in engine output before passing to `k.text()` so they are not parsed as Kaplay style tags.
- **Dev:** Reload iframe when kaplay-demo dist changes (e.g. cache-bust query param, or dev proxy).
- **Production:** Build kaplay in CI; artifact at `docs-site/public/game/index.html`; deploy as static asset.
- **Debug:** Forward iframe console errors to parent; surface in dev tools.

## Out of scope (for now)

- First-person mode removal (still needed for React/play shell).
- Full redesign of Play layout (incremental).

]]></content>
  </doc>
  <doc>
    <path>GRD-escape-the-dungeon.md</path>
    <content><![CDATA[
# GRD: Escape the Dungeon - Game Requirements Document

## Document Status

- Owner: DungeonBreak docs/lab team
- Date: 2026-02-28
- Status: Living document (gameplay discovery loop; roadmap Phase 19 complete)
- Location: `.planning/GRD-escape-the-dungeon.md`
- Relationship: Complements PRD (architecture, scope) with concrete gameplay behavior.
- Simulation north star: `scratch/game-state.md`, `.concept/PROCESS.md`, `.concept/SIMULATION-AGENT-GUIDE.md`
- UI components: `.planning/UI-COMPONENT-REGISTRY.md`, `.planning/KAPLAY-INTERFACE-SPEC.md`
- Code reference: `docs-site/lib/escape-the-dungeon/`

## Purpose

The GRD defines how the game plays: concrete turn flow, formulas, prerequisites, and state changes.

- PRD defines what we are building and why.
- GRD defines how the runtime behaves.

---

## Gameplay Discovery Loop

```
Scope -> Simulate (scratch/game-state.md) -> Check -> Refine (discovery artifacts) -> Emit -> Sync back -> repeat
```

1. Scope: define a simulation goal (example: trigger skill evolution and one cutscene chain).
2. Simulate: play in chat or browser and log updates in `scratch/game-state.md`.
3. Check: confirm expected behavior, identify gaps.
4. Refine: capture discoveries in `.concept/DISCOVERY-LOG.md` and backlog in `.concept/CONTENT-BACKLOG.md`.
5. Emit: update this GRD when behavior contracts change.
6. Sync back: once code ships, ensure GRD reflects implementation.

---

## Core Identifiers

| Term | Meaning |
|------|---------|
| Depth | Dungeon floor position, from 12 (start) to 1 (surface) |
| Level | Character progression from XP, `level = base_level + floor(xp / 30)` |
| Chapter | One depth corresponds to one chapter |
| Act | Four completed chapters |
| Turn | One player action plus simulation pipeline |

Depth and level are separate everywhere (state, UI, logs, tests).

---

## Turn Loop (Concrete Order)

For each player action:

1. Parse input to action.
2. Prerequisite check (room, stats, skills, target, state).
3. Execute action effects.
4. Apply room vector influence (`room_base * 0.05`, clamp traits to `[-1, 1]`).
5. Write deed and project deltas (per-feature cap `0.22`, global cap `0.35`).
6. Evaluate skill unlock/evolution availability.
7. Resolve cutscene triggers.
8. Resolve deterministic global events.
9. Spawn one hostile from exit room (cannot enter `rune_forge`).
10. NPC tick: each NPC chooses one legal action.
11. Persist autosave snapshot.

---

## Action Effects

| Action | Energy | Effort | Attributes | Trait Delta | Feature Delta | Other |
|--------|--------|--------|------------|-------------|---------------|------|
| move | - | - | - | - | - | Room change; chapter progress if depth changes |
| train | -0.15 | - | might +1, willpower +1, xp +5 | Constraint +0.07, Direction +0.05 | Momentum +0.10 | Requires training room |
| rest | +0.20 corridor / +0.30 rest room | - | - | Equilibrium +0.04, Levity +0.02 | - | - |
| talk (target) | - | - | - | Empathy +0.05, Comprehension +0.03 | Awareness +0.05 | Needs nearby entity |
| talk (no target) | - | - | - | Empathy -0.01 | - | No effect action result |
| search (found) | - | - | - | Item vector delta | - | Loot to inventory; tag-based cutscene checks |
| search (empty) | - | - | - | Comprehension +0.01 | - | Empty search result |
| speak | - | - | - | intent projection | intent projection | `say <text>` creates deed |
| fight | - | - | xp +4 | Survival +0.03, Direction +0.03 | Momentum +0.10 | Requires enemy encounter |
| flee | - | - | - | Survival +0.01 | - | Deterministic move to selected legal adjacent room; no hard fail roll |
| choose_dialogue | - | - | - | Option-specific | - | Applies selected dialogue outcome |
| live_stream | - | -10 | - | Projection +0.03 | Fame +gain, Momentum +0.20 | Deterministic fame formula |
| steal | - | - | - | Constraint +0.01, Survival +0.02 | Guile +0.15 | Needs Shadow Hand and valid target |
| recruit | - | - | - | Empathy +0.04 | Awareness +0.10 | Max one companion |
| murder | - | - | xp +10, reputation -2 | Survival +0.06, Constraint -0.04 | Momentum +0.20 | Trait plus faction/reputation gate |
| evolve_skill | - | - | - | Skill-defined | Skill-defined | Requires rune forge + parent + prereqs |

---

## Action Prerequisites

| Action | Prerequisite |
|--------|--------------|
| move | Exit exists for chosen direction |
| train | Room feature `training` |
| talk | Nearby entity |
| fight | Nearby enemy |
| flee | Active encounter and legal destination exit |
| choose_dialogue | Option id valid and currently available |
| live_stream | Effort >= 10 |
| steal | Shadow Hand unlocked and target has loot |
| recruit | Target recruitable and companion slot empty |
| murder | Survival >= 0.2 and (`laughing_face` or reputation <= -6) |
| evolve_skill | Room feature `rune_forge`, parent unlocked, prereqs pass |

---

## Deed Memory Model (Including Misinformation)

Deeds are stored as information vectors and can reference self or other entities.

`DeedMemory` contract:

- `deed_id`: stable id
- `actor_entity_id`: who did the action
- `subject_entity_id`: who the deed is about (self or other)
- `canonical_text`: normalized deed text
- `embedding`: deterministic vector projection
- `belief_state`: `verified | rumor | misinformed`
- `source_entity_id`: who supplied the information
- `confidence`: float in `[0, 1]`
- `timestamp_turn`

Rules:

1. Entities can hold incorrect deeds about others (`belief_state = misinformed`).
2. Dialogue can spread correct or incorrect deeds.
3. Action scoring may consume deed beliefs regardless of correctness.
4. Verified observations can overwrite misinformed beliefs when confidence is higher.

---

## Combat Encounter Model (No Grid)

Combat is encounter simulation, not a positional grid.

**Turn model:** Opening or closing the combat UI does **not** spend a turn. Each **fight** or **flee** choice is a dungeon turn. Combat resolution advances time; browsing the combat screen does not.

Inputs to resolution:

- Equipped gear and weapon tier
- Entity attributes and derived level
- Traits/features (including momentum and survival)
- Active effects/passives
- Room context modifiers
- Opponent profile and faction relationship
- Internal skill-priority policy

Player controls in combat are high-level only:

- `fight`
- `flee`

`flee` contract:

1. No hard-fail roll.
2. Player chooses direction.
3. If direction is legal, player moves immediately.
4. Enemy does not get a same-turn free counteraction.
5. Enemy can pursue in later turns under normal movement logic.

Dialogue choices are non-combat flow and do not replace encounter resolution.

---

## Room Vectors (Base, before 0.05 scale)

| Feature | Base Vector |
|---------|-------------|
| training | Constraint 0.45, Direction 0.30 |
| dialogue | Empathy 0.40, Comprehension 0.20 |
| rest | Equilibrium 0.50, Levity 0.20 |
| rune_forge | Construction 0.45, Comprehension 0.25 |
| treasure | Projection 0.45, Survival 0.25 |
| combat | Survival 0.40, Direction 0.25 |
| stairs_up | Direction 0.35, Projection 0.15 |
| escape_gate | Freedom 0.55, Projection 0.35 |
| start | Comprehension 0.20 |
| corridor | (empty) |

---

## Fame Formula

- Base: `1.0 * (effort_spent / 10)`
- Room interest: `0.6*Projection + 0.4*Levity + 0.25*Direction + 0.2*Survival`
- Novelty multiplier: `0.75` if repeat stream, else `1.0`
- Risk multiplier: combat `1.0`, treasure `0.6`, other `0.35`
- Momentum bonus: `Momentum * 0.04`
- Skill bonus: `+0.15` if Battle Broadcast unlocked
- Diminishing factor: `1 / (1 + Fame / 120)`

---

## Act and Chapter Structure

- **Acts:** 1, 2, 3 (four chapters per act; 12 levels total).
- **Chapter:** One depth corresponds to one chapter. Ascending depth completes a chapter.
- **Chapter complete cutscene:** Fires when player ascends (e.g. "Chapter Closed"). Per-chapter boss or narrative cutscenes are content-driven.
- **Opening narrative:** When starting a new game, show a narrative from the player's first-person perspective: they find themselves at the bottom of the dungeon, not knowing why or how they got there, and are trying to escape. Same engine; gridless first-person UI is descriptive only.

---

## Cutscene Triggers

| Cutscene | Trigger | Once |
|----------|---------|------|
| A Locked Cache | First search with `treasure` tag | Yes |
| Steel Memory | First train after Might >= 8 | Yes |
| Signal in the Dark | First `live_stream` with Fame >= 1 | Yes |
| Hands Like Smoke | Shadow Hand unlock | Yes |
| Chapter Closed | On chapter complete (ascend depth) | No |
| Surface Air | On escape gate at depth 1 | Yes |

---

## Global Events

| Event | Trigger | Effect |
|-------|---------|--------|
| depth_pressure_turn_40 | `turn_index >= 40` | `global_enemy_level_bonus +2` |
| fame_watchers_20 | `Fame >= 20` | Projection +0.08, Momentum +0.25 |

---

## Skills (Unlock and Evolution)

| Skill | Branch | Unlock Radius | Prereqs | Evolution |
|-------|--------|---------------|---------|-----------|
| Appraisal | perception_branch | 2.2 | Insight >= 5 | Deep Appraisal at rune forge |
| X-Ray Instinct | perception_branch | 2.4 | Comprehension <= 0 | Trap Vision at rune forge |
| Keen Eye | - | 1.8 | Insight >= 6, Comprehension >= 0.05 | - |
| Shadow Hand | - | 1.9 | Keen Eye, Agility >= 6, Awareness >= 0.2 | - |
| Battle Broadcast | - | 2.0 | Might >= 7, Fame >= 5 | - |

Perception branch is exclusive: Appraisal and X-Ray cannot both be unlocked in one run.

---

## Feature Groups

The engine treats stats as labeled features. Current packs organize them into groups for clarity.

Thematic feature group (range `[-1, 1]`):

- Comprehension
- Constraint
- Construction
- Direction
- Empathy
- Equilibrium
- Freedom
- Levity
- Projection
- Survival

Runtime feature group:

- Fame (default 0)
- Effort (default 100)
- Awareness (default 0)
- Guile (default 0)
- Momentum (default 0)

Spaces (dialogue/skill/archetype/event/room/effect) consume these feature groups via content-pack vector schemas.

---

## Archetype Compass Contract

- Archetypes are loaded from shared JSON contracts and scored against entity trait vectors each turn.
- Status surfaces expose:
  - `archetype_heading` (top score label)
  - ranked archetype score list for explainability.
- Archetype heading changes are logged in event metadata for balancing analysis.

---

## World Topology

- 12 levels (depth 12 to 1)
- 50 rooms per level (5×10)
- **Room-bound:** No physics, colliders, or mesh grid; rooms are discrete cells. Future **open-world mode** will introduce continuous space and is a separate mode.

### Fog of War (Grid Mode)

Discovery radius is **formulaic**: derived from attribute(s) and/or trait(s), not a fixed constant. Revealed cells = visited rooms + rooms within `fogRadius` steps of any visited room. Contract: `fogRadius(entity) -> number`. Candidate inputs: `insight`, `Awareness`. Formula TBD; implementation must support easy swap.
- Per level: 1 start, 1 exit, 20 treasure, 5 rune forge
- 4 dungeoneers per level
- One hostile spawn from exit each turn
- Hostiles cannot enter rune forge rooms

---

## Treasure Room Items

Treasure rooms include:

- Treasure Cache (`tags: treasure, loot`, vector delta: Projection +0.4, Survival +0.2)
- Weapon item (rarity weighted by depth)

---

## Reference Playthrough Contract (25 Turns)

A deterministic 25-turn integration run is required in automated tests.

Canonical replay seed for v1:

- `CANONICAL_SEED_V1 = 20260227`

Coverage map by turn window:

1. Turns 1-4: movement boundaries, look, room vector influence.
2. Turns 5-8: search, item state changes, cutscene trigger.
3. Turns 9-11: talk and rumor spread between entities.
4. Turns 12-14: deed projection and misinformation propagation.
5. Turns 15-17: combat encounter with `fight` then `flee`.
6. Turns 18-20: skill unlock/evolution gate checks (including rune forge).
7. Turns 21-23: livestream fame/effort effects and deed broadcasting.
8. Turns 24-25: page/chapter logging assertions and snapshot determinism checks.

Required assertions:

- Action availability and blocked reasons are stable.
- Depth and level remain separate and correct.
- Deed beliefs include at least one `misinformed` record.
- Cutscene queue behavior is visible in feed output.
- Same seed and same action script produce same final snapshot hash.

---

## Phased Roadmap: What We Do and What We Phase Out

| Phase | Focus | Status |
|-------|-------|--------|
| Current | Room-bound play; ASCII/text-first; engine parity; agent play surface; report generation | Active |
| KAPLAY | First-person + grid modes; primitives only; no media; walkthrough/AI behavior support | In progress |
| Future | Open-world mode: continuous space, mesh grid, colliders beyond room boundaries | Deferred |

**Phased-out for now:**
- Physics, colliders, tile meshes, canvas AI pathfinding
- Media (sprites, audio)—we use components and primitives only
- Non-room topology

**Intentionally light:**
- Text and ASCII as primary content; some UI (buttons, overlays) for interaction
- RTS-like discrete cells; room boundaries define movement

---

## MCP Tool Interface Contract (Phase 18)

Machine interface is defined over engine APIs so external tools (MCP or other agents) can drive turns without browser UI. **Agent play** includes walkthrough-style scripts, AI behavior, and report generation; agents consume the same action surface and state contracts as human players.

Required operations:

1. `create_session(seed?) -> session_id`
2. `get_snapshot(session_id) -> snapshot`
3. `list_actions(session_id, entity_id?) -> legal_actions[]`
4. `dispatch_action(session_id, action) -> turn_result`
5. `get_log_page(session_id, chapter, entity_id?) -> page_entries[]`
6. `restore_snapshot(session_id, snapshot) -> ok`

Implementation: `packages/engine-mcp` exposes these operations as MCP tools over stdio.

Contract rules:

- Action payloads use the same schemas as browser runtime actions.
- Repeated runs with same seed + same action script must return deterministic snapshots and event ordering.
- Errors must be explicit (`invalid_action`, `blocked_action`, `invalid_payload`, `session_not_found`) with readable reasons.
- Current automated runner is deterministic-policy based; autonomous LLM turn choosing is not implemented.

Autonomous LLM gameplay (future, out-of-scope) would require all of:

- Tool-call budget controls and bounded retry policy per turn.
- Reproducible decision trace capture (prompt/model/tool calls) for deterministic drift triage.
- Separate reliability and safety gates beyond replay parity tests.
- Stable evaluation harness that compares model-play outcomes to deterministic baselines.

---

## Assistant Frame Window-Agent Contract (Phase 19)

Browser `/play` will expose Assistant Frame-compatible bridge wiring so in-window agents can consume a structured game surface when supported by host tooling.

Contract rules:

- Standard button-first DOM gameplay remains valid and unchanged.
- Frame bridge path must map to the same engine actions as UI buttons.
- If no frame host is present, gameplay behavior must fall back cleanly to existing local/browser flow.
- Remote MCP endpoint (`/api/mcp`) is enabled by default in deployed runtime and requires signed-in authentication.
- Remote MCP hardening baseline is always enforced (rate limiting, payload validation, session isolation, and audit metadata).

---

## Extended Playthrough Contract (Phase 18)

Beyond the 25-turn baseline, Phase 18 includes a deterministic >= 75-turn scripted run that includes:

- branching dialogue choices
- multiple combat and flee chains
- skill unlock and at least one evolution
- rumor propagation with misinformation correction
- faction/reputation gate transitions
- companion recruitment attempt
- chapter/act/page progression checks

The 25-turn fixture remains baseline; the 75-turn suite is agent-regression coverage via `canonical-dense-trace-v1.json`.

Phase 17 long-run suite contract:

- deterministic windows: 100, 250, 500 turns
- emitted metrics: action usage, archetype distribution, survival/escape rates, dead action types, turn-time stats
- report paths:
  - `packages/engine/test-reports/long-run-balance-report.json`
  - `docs-site/test-reports/balance-sim-report.json`

Phase 19 release-report contract:

- each shipped game version must publish version-coupled play report artifacts
- each shipped game version must publish a machine-readable pass/fail test manifest
- report filenames include release version and commit/build identifier
- docs must expose links to the matching report set for that version

Phase 20 report schema normalization contract:

- compact reports use reference-based event storage (`packed-v1`) to minimize duplication while preserving full replay fidelity
- packed ledger contains lookup tables (`actors`, `actions`, `rooms`, `messages`) and numeric row references
- entity summaries refer to event indices instead of duplicating dialogue payload records
- optional split-artifact mode writes summary report with `eventLedgerFormat = external-v1` plus an external ledger file
- report top-level schema version is `agent-play-report/v2`; external ledger payload schema is `agent-play-event-ledger/v1`
- viewer implementations must support both `inline-v1` and `packed-v1` report formats by schema version
- viewers that consume split artifacts must support either JSON hydration (`json-v1`) or row streaming iteration (`jsonl-v1`) before timeline traversal
- split-artifact ledgers add JSONL row-stream mode so viewers can iterate timeline entries without loading the full ledger JSON document
- MCP/LLM turn chooser is de-scoped for foreseeable delivery and is not an active roadmap item

---

## Performance and Pressure Contract

Targets:

- Browser turn loop `p95 <= 2s` under pressure cap `120` entities when items are modeled as entities.
- No visible UI stall longer than 2s for a single action.

Pressure controls:

- Deterministic pruning/backpressure policy when entity cap is exceeded.
- If items are plain room/inventory data (not entity objects), the cap is applied to active simulated entities only.
- Pruning decisions are logged to chapter/system pages for auditability.
- Balance harness batches (`simulateBalanceBatch`) are used to track aggregate action usage, archetype distribution, fame, and escape rates across canonical seeds.

---

## Shared Contracts and Replay Artifacts

Cross-language reusable assets live as JSON contracts:

- Action definitions and prerequisites
- Formula constants and delta caps
- Vector anchors and room templates
- Skills, items, cutscenes, global events

Deterministic replay assets:

- Golden trace fixtures (seed + scripted actions + expected snapshots)
- Reference playthrough snapshots
- Vector/feature usage report generated in CI

---

## Simulation Coverage Checklist

Tick when simulated in a run:

- [ ] Actions: move, train, rest, search, talk, speak, fight, flee, live_stream, steal, recruit, murder, evolve_skill, choose_dialogue
- [ ] Rooms: corridor, start, exit, training, rest, treasure, rune_forge, combat
- [ ] Cutscenes: A Locked Cache, Steel Memory, Signal in the Dark, Hands Like Smoke, Chapter Closed, Surface Air
- [ ] Skills: Appraisal/X-Ray, Keen Eye, Shadow Hand, Battle Broadcast, evolutions
- [ ] Events: depth_pressure_turn_40, fame_watchers_20
- [ ] Deed misinformation appears and later reconciliation is possible
- [ ] 25-turn reference run (`CANONICAL_SEED_V1`) passes deterministic snapshot checks

---

## Simulation Artifacts

| Artifact | Purpose |
|----------|---------|
| `scratch/game-state.md` | Current simulation state and log |
| `.concept/PROCESS.md` | Discovery loop process and drift control |
| `.concept/RUNTIME-REFERENCE.md` | Runtime quick reference |
| `.concept/SIMULATION-AGENT-GUIDE.md` | Agent execution protocol |
| `.concept/DISCOVERY-LOG.md` | Curated discoveries |
| `.concept/CONTENT-BACKLOG.md` | Planned content additions |
| `.planning/UI-COMPONENT-REGISTRY.md` | UI component layouts and engine hooks |
| `.planning/KAPLAY-INTERFACE-SPEC.md` | KAPLAY standalone interface spec |
| `packages/kaplay-demo/` | KAPLAY standalone build; first-person + grid; agent-play surface |

---

## GRD Update Process

1. Add findings to `scratch/game-state.md`.
2. Curate validated findings to `.concept/DISCOVERY-LOG.md`.
3. Update GRD when behavior contracts change.
4. Update PRD when architecture or scope changes.
5. Keep GRD and implementation synchronized each loop.
6. Add or refine UI components in `.planning/UI-COMPONENT-REGISTRY.md` when screens are designed; update `.planning/KAPLAY-INTERFACE-SPEC.md` when layouts change.

]]></content>
  </doc>
  <doc>
    <path>KAPLAY-INTERFACE-SPEC.md</path>
    <content><![CDATA[
# KAPLAY Standalone Interface Specification

**Status:** Design spec (implementation in `packages/kaplay-demo`)  
**Location:** `.planning/KAPLAY-INTERFACE-SPEC.md`  
**Source:** Demo tooling plan; reuse `.planning/PRD-tooling-gameplay-analysis.md`, `13-08-PLAN.md`, `global.css` play-grid  
**Related:** [UI Component Registry](.planning/UI-COMPONENT-REGISTRY.md) | [GRD](.planning/GRD-escape-the-dungeon.md)

---

## Agent Play and Walkthroughs

The interface must support **agent play** (not necessarily MCP-specific): automated walkthroughs, AI-like behavior, and report generation.

| Requirement | Rationale |
|-------------|-----------|
| **Structured action surface** | Agents consume `buildActionGroups`, `list_actions`-style catalogs; same schema as MCP tools |
| **Report generation** | Playthrough logs, event streams, and analysis artifacts feed balancing and content pipelines |
| **Deterministic replay** | Same seed + action script → same snapshot; required for regression and baseline comparison |
| **Text/ASCII dominant** | Low media footprint; agents parse state from text/structured output; UI stays light |
| **Room-bound topology** | No physics, colliders, or mesh grid; rooms are discrete cells; future open-world is a distinct mode |

Walkthrough-style play (scripted or AI-driven) uses the same engine APIs as human play. Reports and entertaining systems are built from emitted events, not from rendering.

---

## Composable UI: shadcn vs KAPLAY

| Build | Stack | Composable UI |
|-------|-------|---------------|
| **Docs-site `/play`** | Next.js + React | shadcn (Card, Button, etc.) — already in use |
| **KAPLAY standalone** | Single HTML, Canvas/WebGL | No shadcn (not DOM). Build primitives from KAPLAY `rect()`, `text()`, `area()`. Compose as reusable scene layers. |

**Implication:** We design UI components as *layout + behavior specs*. React build implements with shadcn; KAPLAY build implements with rect/text/area. Shared contracts (data shape, actions) come from the engine.

---

## Screen-Based Model (Pokemon-Style)

Grid mode has **distinct screens** that swap based on context:

| Screen | Trigger | Purpose |
|--------|---------|---------|
| **Navigation** | Default; moving on map | Overworld; ASCII map, move with WASD/arrows |
| **Combat** | Enter room with hostile; choose Fight | Turn-based combat UI (Pokemon-style); Fight / Flee; encounter resolution |
| **Action menu** | Interact key in room | Choose: Train, Rest, Search, Talk, Inventory, Rune Forge, etc. |
| **Rune Forge** | In rune_forge room; choose rune forge action | Rest, Inventory, Evolve skill, (future: Purchase, Re-equip) |
| **Inventory** | From action menu or Rune Forge | List items; select to view/use/equip |
| **Dialogue** | Talk / choose_dialogue | Present options; select outcome |
| **Cutscene** | Trigger fires | Blocking prose; [Continue] |

*Always accessible (when not blocked):* Status strip, Save/Load via Esc.

## Implementation Snapshot (March 2, 2026)

- KAPLAY now has an atomic UI stack in `packages/kaplay-demo/src/ui`: `atoms.ts`, `molecules.ts`, `organisms.ts`.
- Scene-level rendering is moving to organism-first composition; new work should not introduce scene-local color literals when tokenized primitives exist.
- KAPLAY first-person and grid both use a 3-column layout: left nav, center viewport, right journal. Shared constants in `layout-constants.ts`.
- Grid center = ASCII map; first-person center = narrative (look). Right panel = room stats + journal in both modes.
- Grid combat/action/rune forge/inventory/dialogue now use the same shared shell model (left command panel, center scene content, right room brief + log).
- Grid discovery is persisted to local storage and reveals adjacent rooms for a light fog-of-war effect.
- Feed rendering uses tone-aware styling to distinguish chapter/scene beats, dialogue, combat, and system lines.
- Shared chip/panel/tab primitives are now implemented in `packages/kaplay-demo/src/shared.ts` and reused across scenes.
- Semantic tone tokens are standardized (`neutral`, `good`, `warn`, `danger`, `accent`) and applied consistently to buttons/chips/feed.
- Keyboard discoverability is improved via keycap legend molecules in footer hints.
- Action labels are now glyph-prefixed (for example `[ATK]`, `[RUN]`, `[EVO]`, `[INV]`) using shared context mapping for reuse.
- Dialogue progression state is now tracked in UI session state (`sequence`, recent steps, latest option) and rendered in the Dialogue screen.
- Fog-of-war reveal is now formula-driven (`radius = 1 + level/comprehension/awareness factors`, clamped) and shown in the context panel.
- Scene-local header/tab setup is refactored through shared scene layout scaffolding to reduce duplicated scene bootstrapping.
- Scene rendering now uses a declarative `SceneLayout` frame (`renderSceneLayout`) for consistent header/tab composition.
- Action rendering now goes through shared `ActionRenderer` helpers (`action-renderer.ts`) for labels, tones, and action lookups.
- Reusable panel components now include `renderInfoPanel`, `renderActionListPanel`, and `renderEventLogPanel`.
- Formula logic is centralized in `formula-registry.ts` and consumed by UI systems (fog metrics).
- UI session state is now managed by `ui-state-store.ts` with local persistence for dialogue progression and derived fog state.
- Intent routing is centralized in `intent-router.ts` so action-to-scene transitions are convention-driven instead of scattered conditionals.
- Widget composition is centralized with `widget-registry.ts`, backed by `panel-schema.ts` for a constrained, portable panel schema.
- Selector layer is added (`ui-selectors.ts`) so scenes consume stable view models instead of raw state internals.
- Action intent metadata is now engine-authored via `action-intents.json` and carried through presenter action items (`uiIntent`, `uiScreen`, `uiPriority`).
- Dialogue progression is now canonical engine state (`state.dialogueProgress`) and exposed via `engine.status()` for parity across clients.
- Unreal handoff artifact is published as `.planning/UNREAL-UI-CONTRACT-PACK.v1.json`.
- Scene-local reusable blocks are now authored in `scene-blocks.ts` and emitted as constrained `PanelSchema` (no generic UI DSL).
- Panel sizing/tone choices now use `panel-formulas.ts` to keep context rendering compact and deterministic.
- Engine content pack bundle is generated as `content-pack.bundle.v1.json` (hash-addressable pack set for parity testing).
- KAPLAY can optionally validate runtime pack parity at boot via `?contentPackUrl=/game/content-pack.bundle.v1.json&contentPackStrict=1`.
- KAPLAY can load runtime vector overrides via:
  - `?spaceVectorsUrl=/path/to/space-vectors.json`
  - `?contentFeaturesUrl=/generated/thematic-basis-vectors/thematic-basis-vectors.json`
  - Legacy alias also supported: `?thematicBasisUrl=...`
- Navigation/first-person right panels should expose compact space hints (nearest actions/events/effects) from engine runtime model.
- **Escape convention:** Any string with `[` or `]` passed to `k.text()` must use `escapeKaplayStyledText()` to avoid "unclosed tags" errors. Kaplay interprets brackets as styled text tags.
- Docs reporting now includes `/play/reports/content-packs` for uploading new `content-pack.bundle.v1` files, generating persisted reports, and routing those vectors into Space Explorer.

### Layout Reuse: First-Person and Grid

Both modes share the same shell: **header** (health/progress/location) → **left panel** (navigation) → **right panel** (info/inspector) → **center viewport**. Only the center swaps: grid shows the ASCII map; first-person shows narrative/description. Same header, footer, room info, and action primitives. Player glyph `@` and entity glyphs (hostile `E`, dungeoneer `D`, etc.) are defined in `grid.ts` GLYPHS.

### Constrained Panel Schema (Unreal Parity)

- `panel-schema.ts` is intentionally limited to three primitives: `info`, `event_log`, and `action_list`.
- This is a **schema bridge**, not a full UI DSL/runtime. Rule: do not add generic expression engines or nested dynamic layout logic.
- Goal: keep panel definitions portable to Unreal UMG/Slate equivalents while preserving readability and low complexity.

---

## Design Constraints

| Constraint | Rule |
|-----------|------|
| **No media** | No images, sprites, audio. Text and ASCII only. Components and primitives only. |
| **Text-first** | Text and ASCII glyphs are the primary content; some UI (buttons, overlays) can use rect/area for interaction. |
| **RTS-like** | Top-down, discrete cells; think room-based RTS rather than physics-based action game. |
| **Single file** | Self-contained HTML with inline JS/CSS. No external deps at runtime. |
| **Engine parity** | Same `@dungeonbreak/engine` state, actions, and turn flow as `/play` React build. |
| **One action per turn** | Player commits one action; dungeon responds; autosave. |
| **Persistence** | IndexedDB or localStorage; same contract as browser `/play`. |
| **Room boundaries** | No canvas mesh, AI pathfinding grid, or colliders. Rooms are bounded cells; future open-world mode is separate. |

---

## Mode Comparison

| Aspect | First-Person Mode | ASCII Grid Mode |
|--------|-------------------|-----------------|
| **POV** | Immersive narrative; "you are in the room" | Top-down; you see the map (Pokemon-style) |
| **Primary input** | Click/tap action buttons | WASD / arrows for move; Interact key for actions |
| **Room representation** | Prose description (engine `look`), piece by piece as you walk | ASCII glyphs; one cell per room |
| **Cognitive load** | Story-focused; slow, narrative pacing | Map-focused; fast navigation; UI-based |
| **Cutscene handling** | Full-screen or overlay prose; blocking | Overlay or side pane; same blocking |
| **Status visibility** | Collapsible or sidebar; room info panel available | Compact HUD; inventory, status, room info panels |
| **Overlap** | Same engine; room info panel mirrors grid's room details | Same engine; first-person narrative available via room info |

---

## First-Person Mode

### Intent

*"It really feels like its just us and the story."* Narrative mode. One room at a time. You are described the room piece by piece as you walk—slower than grid navigation. Prose dominates. Room details (feature, exits, entities) available via an **info panel** that mirrors grid mode's room data so both modes stay aligned.

### Layout Greybox

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Escape the Dungeon                                    [First-Person] [▾] │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─ Room narrative (engine.look) ────────────────────────────────────┐  │
│  │                                                                    │  │
│  │  The stone corridors smell of rust and old dust. Your boots echo   │  │
│  │  on the flagstones. Exits lead north and east. A training dummy    │  │
│  │  stands in the corner. Mira watches from the doorway.              │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ Room info panel (collapsible, same data as grid mode) ────────────┐  │
│  │  [▼ Room info]  start_12_0_0 · training  │  Exits: N E  │  Mira   │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ Actions (grouped, scrollable) ───────────────────────────────────┐  │
│  │  [Go North]  [Go East]  [Train]  [Rest]  [Talk to Mira]  [Look]    │  │
│  │  [Save]  [Load]                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─ Feed / Turn log (scrollable, newest bottom) ─────────────────────┐  │
│  │  t12 Kael move@12_start_0_0: Moved north.                           │  │
│  │  t12 Room influence: +Survival 0.02                                 │  │
│  │  ...                                                                │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  Depth 12 · start_12_0_0  │  HP 100  Energy 0.85  Level 12  │  [▼ Status]│
└─────────────────────────────────────────────────────────────────────────┘
```

### Region Specifications

| Region | Min height | Behavior | Placeholder |
|--------|------------|----------|-------------|
| **Room narrative** | 120px | `engine.look()`; wraps; mono or serif. Piece-by-piece feel as you move. No scroll if short. | `[Room description from engine.look]` |
| **Room info panel** | 32px collapsed | roomId, feature, exits, nearby entities. Collapsible. Same source as grid mode room label. | `[Room info - roomId · feature │ Exits │ Entities]` |
| **Actions** | 80px | Grouped (Movement, Room, Dialogue, Conflict, Special). One row per group; wrap. Disabled actions show dimmed + tooltip/reason. | `[Action buttons - same groups as React /play]` |
| **Feed** | 150px | Scroll to bottom. `toFeedMessages` per turn. Compact lines. | `[tN] Actor action@room: message` |
| **Footer status** | 36px | Inline: depth, roomId, HP, Energy, Level. Expandable "[▼ Status]" → full traits/features/nearby. | `Depth N · roomId  │  HP  Energy  Level  │  [▼]` |

### Expected User Behavior (First-Person)

1. **Read** room text; infer context (exits, features, entities).
2. **Optionally** expand room info panel for structured data (aligned with grid).
3. **Choose** one action per turn via button click.
4. **Scan** feed for outcome; optionally expand status.
5. **Cutscene:** Modal or overlay blocks actions until dismissed; prose only.
6. **Save/Load:** Utility actions always available; no confirmation unless load overwrites.

### Styling Constraints

- **Font:** Monospace for feed/status; serif or system for narrative (optional).
- **Colors:** Dark background (e.g. `#0f172a`), light text; accent for actions.
- **Spacing:** 8px–16px padding; 4px gap between buttons.
- **No animations** beyond optional fade for cutscenes.

---

## ASCII Grid Mode

### Intent

Pokemon-style top-down grid. Fast navigation. Movement is primary; actions appear only after pressing the **Interact key** (E or Space). More UI-based than first-person: varying panels for inventory, status, room info, action menu. Engine overlap with first-person where possible (same actions, same state).

### Map Granularity

**One cell = one room.** Engine model: 50 rooms per level, 5×10 grid. Each room is a single cell. No tile subdivision within rooms.

### Layout Greybox

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  Escape the Dungeon                                                    [ASCII Grid] [▾]  │
├──────────────────────────────┬────────────────────────────────────────────────────────────┤
│                              │  ┌─ Room info ──────────────────────────────────────────┐ │
│  ┌─ ASCII Map ─────────────┐ │  │  L12_R042 · training  │  Exits: N S E W  │  Mira    │ │
│  │  #####  #####  #####     │ │  └──────────────────────────────────────────────────────┘ │
│  │  #.##   #.@.   #...     │ │                                                           │
│  │  #.T#   #.D#   #.E#     │ │  ┌─ Action menu (Interact key) ─────────────────────────┐ │
│  │  #####  #####  #####     │ │  │  [Train] [Rest] [Search] [Talk] [Inventory] ...      │ │
│  │                          │ │  │  E/Space: actions  │  WASD/arrows: move             │ │
│  │  # = undiscovered        │ │  └─────────────────────────────────────────────────────┘ │
│  │  . = floor  @ = you      │ │                                                           │
│  └──────────────────────────┘ │  ┌─ Feed (3–5 lines) ──────────────────────────────────┐ │
│                              │  │  t12 move north. +Survival 0.02                      │ │
│  ┌─ Inventory panel ───────┐ │  └──────────────────────────────────────────────────────┘ │
│  │  Worn Blade, Treasure    │ │                                                           │
│  │  [▼ Expand]             │ │  ┌─ Status (collapsible) ───────────────────────────────┐ │
│  └──────────────────────────┘ │  │  HP 100  E 0.85  Lv12  Traits...  [▼]                │ │
├──────────────────────────────┴──┴────────────────────────────────────────────────────────┘ │
│  Depth 12  HP 100  E 0.85  Lv12  │  [▼ Full status]                                        │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

### ASCII Glyph Convention

| Glyph | Meaning |
|-------|---------|
| `#` | **Undiscovered** (room not yet visited) |
| `.` | Floor (discovered, empty/passable) |
| `@` | Player (Kael) |
| `E` | Hostile entity |
| `D` | Dungeoneer |
| `^` | Level exit |
| `R` | Rune forge room |
| `T` | Treasure room |
| `*` | Training room |
| `~` | Rest room |
| ` ` | Empty / out of view |

*Undiscovered rooms render as `#` until the player enters them; then they show the appropriate feature glyph or `.`.*

### Discovery and Fog of War

**Visited set:** Track room IDs the player has entered. **Revealed set:** Visited plus rooms within `fogRadius` steps (Manhattan or Chebyshev distance on the grid).

**FOG_RADIUS is formulaic:** Derived from player attribute(s) and/or trait(s), not a fixed constant. Implementation should make the formula easy to swap. Candidate inputs:

- `insight` (attribute) — perception, scouting
- `Awareness` (feature) — situational awareness

Example stub: `fogRadius = 1 + floor(insight / 6)` (min 1, max TBD). Exact formula TBD; contract: `fogRadius(state) -> number`.

### Input

| Input | Action |
|-------|--------|
| **WASD** or **Arrow keys** | Move (one keypress = one turn) |
| **[x]** | Open action menu (train, rest, search, talk, fight, etc.); actions consume turns |
| **[e]** | Equipment panel (what is equipped; slot restrictions) — no turn |
| **[b]** | Bag (inventory; everything carried) — no turn |
| **[i]** | Info (current room + character status) — no turn |
| **[s]** | Skills (list + evolution tabs) — no turn |
| **Esc** | Close overlay / system menu (save, load, quit) |

### Region Specifications

| Region | Min size | Behavior | Placeholder |
|--------|----------|----------|-------------|
| **ASCII map** | 400×300px | One cell per room; 5×10 per level. Centered on player. Glyphs per convention. | `[ASCII map - 5×10, # . @ E D R T ...]` |
| **Room info** | 1–2 lines | roomId, feature, exits, nearby entities. Same data as first-person room info panel. | `roomId · feature │ Exits │ Entities` |
| **Action menu** | 80px | **Only visible after Interact key.** Grouped actions; inventory access. | `[Actions]  │  E/Space: actions, WASD: move` |
| **Inventory panel** | 48px collapsed | List item names; expand for details. Engine: `entity.inventory`. No explicit "equip" in engine—weapon auto-used in combat. | `[Inventory - items]  [▼]` |
| **Feed** | 60px | Last 3–5 lines; scroll optional. | `[Compact feed]` |
| **Status panel** | 48px collapsed | HP, Energy, Level, traits, features. Collapsible. | `HP E Lv  │  [▼]` |
| **Footer** | 24px | Depth, HP, Energy, Level. Expand for full status. | `Depth HP E Lv  │  [▼]` |

### Expected User Behavior (ASCII Grid)

1. **Move** with WASD or arrows; one keypress = one turn.
2. **Press Interact (E/Space)** to open action menu; then choose train, rest, talk, fight, search, inventory, etc.
3. **View inventory** via action menu or dedicated panel; engine supports viewing, no explicit equip (weapon auto-used in combat).
4. **Combat/dialogue:** Modal or overlay; no movement until resolved.
5. **Cutscene:** Same blocking overlay as first-person; map dimmed or hidden.
6. **Save/Load:** Via Esc menu or footer.

### Engine Support vs. Desired (Grid Mode)

| Feature | Engine support | Notes |
|---------|----------------|-------|
| Inventory display | ✅ `entity.inventory` | List items by name, tags (weapon, armor, loot) |
| Equip | ❌ No explicit action | Weapon auto-selected in combat from inventory |
| Room info | ✅ Room feature, exits, entities | Same as first-person room info |
| Actions | ✅ Full catalog | All actions available via Interact menu |
| Movement | ✅ move action | WASD/arrows map to move directions |

*Future: "equip" or "use item" could be added to engine if needed for grid UX.*

### Varying Components (Grid Mode)

Grid mode needs multiple display components that first-person can omit or collapse:

- **Map view** (primary)
- **Room info** (always visible or one click)
- **Action menu** (on Interact)
- **Inventory panel** (list + expand)
- **Status panel** (compact + expand)
- **Feed** (compact)

### Styling Constraints

- **Font:** Monospace only (e.g. `Consolas`, `JetBrains Mono`). Fixed width per glyph.
- **Cell size:** 12–14px; grid aligns to character boundaries.
- **Colors:** Dark bg; `#` undiscovered (dim); `.` floor (mid); `@` player (bright); entities distinct.
- **No sprites:** Pure text/ASCII.

---

## Mode Switching

| Element | Behavior |
|---------|----------|
| **Entry** | Mode selector at launch: "First-Person" or "ASCII Grid". Persist choice to localStorage. |
| **Runtime** | Header dropdown `[First-Person ▼]` / `[ASCII Grid ▼]`; switch reloads scene with same game state. |
| **State** | Switching preserves engine snapshot; only layout/input change. |
| **First-person mode** | Reuses components; no grid. Descriptive only: what was done, dialogue, narrative. Same game, gridless UI. |
| **Grid mode** | ASCII map, panels. Both modes follow Act 1 / Act 2 / Act 3 and chapter structure. |

## Opening Narrative and Chapter Flow

- **Game start:** Narrative from player's perspective: they find themselves at the bottom of the dungeon, not knowing why or how, trying to escape. Shown as opening cutscene or initial dialogue.
- **Chapter complete:** Cutscene when ascending depth (e.g. "Chapter Closed"). Per-chapter boss cutscenes are content-driven.

---

## Cutscene Handling (Both Modes)

| Constraint | Rule |
|------------|------|
| **Blocking** | No actions until cutscene dismissed. |
| **Content** | Title (bold) + prose (italic or plain). No images. |
| **Dismiss** | Single "[Continue]" or click/tap. |
| **Placeholder** | `*** Title ***` `[Cutscene prose...]` `[Continue]` |

---

## Placeholder Summary (For Greyboxing)

```
FIRST-PERSON:
- [Room narrative - engine.look, piece by piece]
- [Room info panel - roomId, feature, exits, entities; collapsible]
- [Action buttons - grouped]
- [Feed - scrollable log]
- [Footer - depth, hp, energy, level, status expand]

ASCII GRID:
- [ASCII map - # undiscovered, . floor, @ E D R T * ~ ^]
- [Room info - same as first-person]
- [Action menu - Interact key only]
- [Inventory panel - list items, expand]
- [Status panel - compact, expand]
- [Feed - 3-5 lines]
- [Footer - compact]
```

---

## Engine Overlap (Both Modes)

| Shared | Source |
|--------|--------|
| Room description | `engine.look()` |
| Room metadata | roomId, feature, exits, nearby entities from snapshot |
| Actions | `buildActionGroups(engine)`; same catalog |
| Turn flow | One action per turn; `engine.dispatch`; `toFeedMessages` |
| State | Traits, features, inventory, skills, cutscenes |
| Persistence | IndexedDB/localStorage; same contract |

First-person room info panel and grid room info use identical data. Narrative (engine.look) is first-person emphasis; grid can surface it in room info expand if desired.

---

## Differences from React /play

| React /play | KAPLAY Standalone |
|-------------|-------------------|
| 3-column (actions \| feed \| status) | Vertical stack or split (first-person); map + panels (grid) |
| Assistant UI feed | Plain text feed; same `toFeedMessages` |
| shadcn Card components | Rect/text only; no component library |
| Cutscene modal (Radix) | Custom overlay or scene |
| No mode switch | First-person vs ASCII grid |
| Actions always visible | First-person: buttons; Grid: Interact key only |
| Docs-site embedded | Single HTML; no Next.js |

---

## Future: Open-World Mode

Current design is **room-confinement**: discrete rooms, 5×10 per level, no free movement or physics. A future **open-world mode** will relax these constraints:

- Continuous space, mesh grid, or pathfinding
- Colliders and movement boundaries beyond room edges
- New game mode; not a drop-in replacement for room topology

Until then, all KAPLAY interfaces assume room-bound play. Open-world will be documented as a separate mode when scoped.

---

## Reference Layout (React /play grid)

Current CSS from `global.css`:
- `play-grid`: `minmax(250px,300px) | minmax(520px,1fr) | minmax(280px,360px)` at 72vh
- First-person: narrative replaces center; actions left; feed below or right.
- ASCII: map occupies center; side panel(s) for room info, inventory, status; action menu on Interact.

---

## Screen Specifications (Stubs)

*Full layouts in [UI Component Registry](.planning/UI-COMPONENT-REGISTRY.md).*

### Combat Screen (Pokemon-Style)

Encounter UI when player chooses Fight. Turn-based; player picks Fight or Flee each turn until resolution.

```
┌─────────────────────────────────────────────────────────────────┐
│  COMBAT                                    Depth 12 · L12_R042   │
├─────────────────────────────┬───────────────────────────────────┤
│  Enemy: Hostile_001         │  Kael  HP ████████░░ 100/100     │
│  HP ████░░░░░░ 40/80        │  Energy 0.85  Level 12            │
│  Level 10                   │                                   │
│  [Enemy sprite placeholder]  │  [Player placeholder]             │
├─────────────────────────────┴───────────────────────────────────┤
│  [Fight]  [Flee]  [Item]  [Skill]  (Item/Skill stubbed for future)│
├───────────────────────────────────────────────────────────────────┤
│  t45 Kael fights. Hostile_001 takes 12 damage.                    │
└───────────────────────────────────────────────────────────────────┘
```

**Engine hook:** `fight`, `flee` actions. Combat resolution is internal; UI displays outcome from `toFeedMessages`.

### Rune Forge Screen

When in `rune_forge` room and player selects "Rune Forge" from action menu.

```
┌─────────────────────────────────────────────────────────────────┐
│  RUNE FORGE                             L12_R031 · rune_forge   │
├─────────────────────────────┬───────────────────────────────────┤
│  [Rest]  [Inventory]        │  Evolve skill                     │
│  [Evolve skill]             │  ┌─────────────────────────────┐   │
│  (Purchase — future)        │  │ Appraisal → Deep Appraisal  │   │
│  (Re-equip — future)        │  │   Requires: Insight 7, ...   │   │
│                             │  │ [Evolve]                    │   │
│                             │  └─────────────────────────────┘   │
├─────────────────────────────┴───────────────────────────────────┤
│  HP 100  E 0.85  [▼ Status]                                       │
└───────────────────────────────────────────────────────────────────┘
```

**Engine hook:** `rest`, `evolve_skill`; `buildActionGroups` filters by room feature. Purchase/requip stubbed.

### Inventory Screen

```
┌─────────────────────────────────────────────────────────────────┐
│  INVENTORY                                                       │
├───────────────────────────────────────────────────────────────────┤
│  Worn Blade (weapon)     Common    [Select] [Drop — future]       │
│  Treasure Cache (loot)    Rare      [Select]                       │
│  — empty slots —                                                  │
├───────────────────────────────────────────────────────────────────┤
│  [Back]                                                          │
└───────────────────────────────────────────────────────────────────┘
```

**Engine hook:** `entity.inventory`; item tags (weapon, armor, loot). Equip/Drop stubbed.

### Dialogue Screen

```
┌─────────────────────────────────────────────────────────────────┐
│  Mira: "The forge is safe. Rest while you can."                  │
├───────────────────────────────────────────────────────────────────┤
│  [Loot the treasure cache]                                       │
│  [Say: I wish something else was here]                           │
│  [Appraise the cache seals]                                      │
├───────────────────────────────────────────────────────────────────┤
│  [Back]                                                          │
└───────────────────────────────────────────────────────────────────┘
```

**Engine hook:** `choose_dialogue`; options from dialogue clusters by vector distance.

]]></content>
  </doc>
  <doc>
    <path>UI-COMPONENT-REGISTRY.md</path>
    <content><![CDATA[
# UI Component Registry

**Status:** Living doc — stubs for layout; hook-in as we build  
**Location:** `.planning/UI-COMPONENT-REGISTRY.md`  
**Related:** [KAPLAY Interface Spec](.planning/KAPLAY-INTERFACE-SPEC.md) | [GRD](.planning/GRD-escape-the-dungeon.md)

---

## Purpose

Registry of all UI components/screens needed for Escape the Dungeon. Each entry has:
- **Layout** (greybox)
- **Engine hook** (what state/actions drive it)
- **Build target** (React/shadcn vs KAPLAY)
- **Status** (stub | partial | complete)

Composable: design once, implement in React (shadcn) and/or KAPLAY (rect/text).

---

## Component List

| Id | Component | Build | Engine Hook | Status |
|----|-----------|-------|-------------|--------|
| UI-01 | Navigation (grid) | KAPLAY | move, roomId, exits, vector hints | partial (tabs + persisted discovery/fog + space hint strip) |
| UI-02 | Combat | KAPLAY, React | fight, flee | partial (KAPLAY three-column shell + battle log + action buttons) |
| UI-03 | Action menu | KAPLAY | buildActionGroups | partial (shared shell + grouped action routing) |
| UI-04 | Rune Forge | KAPLAY | rest, evolve_skill, room=rune_forge | partial (shared shell + rest/evolve/purchase/re-equip) |
| UI-05 | Inventory | KAPLAY, React | entity.inventory | partial (shared shell + use/equip/drop rows in KAPLAY) |
| UI-06 | Dialogue | KAPLAY, React | choose_dialogue | partial (shared shell + dialogue progression + options) |
| UI-07 | Cutscene | KAPLAY, React | extractCutsceneQueue | partial (React) |
| UI-08 | Room info panel | KAPLAY, React | roomId, feature, exits, entities | partial (Room Brief organism in KAPLAY) |
| UI-09 | Status strip | KAPLAY, React | engine.status() | partial (React) |
| UI-15 | Gameplay Debug (collapsible) | React | archetype, traits, features, nearby, quests, recent events | partial (React) |
| UI-16 | Content Pack Reports | React | `/api/content-packs/reports` upload/list/report-open flow | complete |
| UI-17 | DungeonBreak Content Dashboard Shell | React (non-MDX route) | `/dungeonbreak-content-app` unified app shell with grouped sections | partial |
| UI-18 | DungeonExplorer | React (non-MDX route) | `/dungeonbreak-content-app/dungeon-explorer` room/level topology + 3D space plots | stub |
| UI-10 | Feed / log | KAPLAY, React | toFeedMessages | partial (right-panel journal + tone feed in KAPLAY) |
| UI-11 | First-person narrative | KAPLAY | engine.look(), vector hints | partial (three-column shell + command panel + room brief + journal) |
| UI-12 | System menu (Esc) | KAPLAY | Save, Load | stub |
| UI-13 | Purchase (Rune Forge) | — | future | not in engine |
| UI-14 | Re-equip / Equip | — | future | not in engine |
| UI-19 | Atomic primitives library | KAPLAY | theme tokens + layout contracts | partial (atoms/molecules/organisms integrated in primary scenes) |

---

## Layout Stubs

### UI-01: Navigation (Grid)

```
┌─ ASCII Map ─────────────────────┐
│  #####  #.T#  #.R#              │
│  #.@.   #.D#  #.^#              │
│  #...   #...  #...              │
└─────────────────────────────────┘
```
- **Content:** Room glyphs; player position; discovered vs `#`
- **Input:** WASD, arrows

---

### UI-02: Combat

```
┌──────────────────────────────────────────────────┐
│  Enemy: [name]  HP [bar]  Lv [n]                 │
│  Kael   HP [bar]  Energy  Lv                     │
│  [Fight] [Flee] [Item] [Skill]                   │
│  [Feed: last 1–2 combat messages]                │
└──────────────────────────────────────────────────┘
```
- **Content:** Opponent stats, player stats, action buttons
- **Engine:** fight, flee dispatch; outcome in feed
- **Future:** Item, Skill if engine gains them

---

### UI-03: Action Menu

```
┌──────────────────────────────────────────────────┐
│  Movement: [N] [S] [E] [W]                       │
│  Room: [Train] [Rest] [Search]                   │
│  Dialogue: [Talk] [Choose…]                      │
│  Combat: [Fight] [Flee] [Steal] [Recruit]        │
│  Special: [Stream] [Evolve] [Rune Forge]         │
│  Utility: [Inventory] [Save] [Load]              │
└──────────────────────────────────────────────────┘
```
- **Content:** buildActionGroups(engine), filtered by room/state
- **Trigger:** Interact key (E/Space)

---

### UI-04: Rune Forge

```
┌──────────────────────────────────────────────────┐
│  RUNE FORGE                                      │
│  [Rest] [Inventory] [Evolve skill]               │
│  [Purchase] [Re-equip]  (future)                 │
│  ┌─ Evolvable skills ──────────────────────┐   │
│  │  Appraisal → Deep Appraisal  [Evolve]     │   │
│  │  X-Ray → …  (locked: …)                   │   │
│  └──────────────────────────────────────────┘   │
└──────────────────────────────────────────────────┘
```
- **Content:** Skills with requiresRuneForge; prereq check
- **Engine:** evolve_skill, rest; room feature = rune_forge

---

### UI-05: Inventory

```
┌──────────────────────────────────────────────────┐
│  INVENTORY                                       │
│  ┌────────────────────────────────────────────┐  │
│  │  1. Worn Blade (weapon, common)             │  │
│  │  2. Treasure Cache (loot, rare)             │  │
│  │  3. — empty —                               │  │
│  └────────────────────────────────────────────┘  │
│  [Select] [Equip] [Drop] [Back]                  │
└──────────────────────────────────────────────────┘
```
- **Content:** entity.inventory; itemId, name, tags, rarity
- **Engine:** display only; Equip/Drop future

---

### UI-06: Dialogue

```
┌──────────────────────────────────────────────────┐
│  [NPC name]: "[Line]"                            │
│  ┌────────────────────────────────────────────┐  │
│  │  [Option A]                                 │  │
│  │  [Option B]                                 │  │
│  │  [Option C]                                 │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```
- **Content:** Dialogue cluster options by vector distance
- **Engine:** choose_dialogue with optionId

---

### UI-07: Cutscene

```
┌──────────────────────────────────────────────────┐
│  *** [Title] ***                                 │
│  [Prose text...]                                 │
│  [Continue]                                      │
└──────────────────────────────────────────────────┘
```
- **Content:** extractCutsceneQueue
- **Engine:** Blocking until dismissed

---

### UI-08: Room Info Panel

```
┌──────────────────────────────────────────────────┐
│  L12_R042 · training                             │
│  Exits: N S E W  │  Nearby: Mira, Hostile_001   │
└──────────────────────────────────────────────────┘
```
- **Content:** roomId, feature, exits, nearby entities
- **Shared:** First-person and grid use same data

---

### UI-09: Status Strip

```
Depth 12  HP 100  E 0.85  Lv12  [▼]
```
- **Expand:** Traits, features, archetype, quests
- **Engine:** engine.status()

### UI-15: Gameplay Debug (Collapsible)

Developer/debug section; collapsed by default. Does not fit main player-facing UI.

- **Content:** Archetype Compass, Traits, Features, Nearby entities, Quests, Recent Events
- **Build:** React only; collapsible under "Gameplay Debug"
- **Engine:** snapshot.eventLog, status.archetypeScores, status.traits, status.features, status.quests

---

### UI-10: Feed

```
t45 Kael move@L12_R042: Moved north.
t45 Room influence: +Survival 0.02
```
- **Content:** toFeedMessages (last N lines)
- **Engine:** Turn result events

---

## Composability Notes

- **React build:** Use shadcn Card, Button, etc. Map each stub to a component.
- **KAPLAY build:** Each stub = scene or scene layer. Primitives: rect, text, area (click).
- **Shared:** Data contracts from engine (status, actions, inventory, dialogue options).
- **Semantic theming:** Use shared UI tones (`neutral`, `good`, `warn`, `danger`, `accent`) for context coloring parity across screens.
- **Action iconography:** Use shared glyph-prefixed action labels (for example `[ATK]`, `[RUN]`, `[EVO]`, `[INV]`) from common helpers.
- **Scene scaffolding:** Use shared frame builder (`renderSceneLayout`) for header + tab wiring to keep scene setup DRY.
- **Scene layout model:** Use declarative `SceneLayout` (`renderSceneLayout`) for per-screen frame consistency.
- **Action renderer:** Use shared `action-renderer.ts` helpers for action lookup/label/tone mapping.
- **Panel components:** Prefer `renderInfoPanel`, `renderActionListPanel`, and `renderEventLogPanel` before writing scene-local panel code.
- **Scene blocks:** Prefer `scene-blocks.ts` builders for repeated contextual panels (dialogue progression, fog/context status).
- **Panel formulas:** Use `panel-formulas.ts` for panel height and log line budgeting instead of scene-local magic numbers.
- **State and formulas:** Use `ui-state-store.ts` + `formula-registry.ts` for derived UI state instead of ad-hoc scene calculations.
- **Atomic stack (default):** Build new KAPLAY UI via `ui/atoms.ts` -> `ui/molecules.ts` -> `ui/organisms.ts`; avoid direct scene-local drawing unless introducing a new reusable primitive.
- **Intent routing:** Use `intent-router.ts` for action/hotkey scene routing to avoid scene-local branching drift.
- **Selector layer:** Use `ui-selectors.ts` for view models (`dialogue summary`, `fog metrics`, `recent timeline`) and keep raw-state reads minimal.
- **Schema portability:** Use `panel-schema.ts` only for constrained panel primitives that can map directly to Unreal panels.
- **Engine intent parity:** Prefer engine-provided action metadata (`uiIntent`, `uiScreen`, `uiPriority`) when routing or ordering action UI.
- **Dialogue canon:** Dialogue progression should read from engine status/snapshot, not inferred from feed text.

---

## KAPLAY API Mapping

KAPLAY exposes a single default export. Call `kaplay(options)` to get context `k`; all APIs live on `k`.

| Component | KAPLAY primitives | Notes |
|-----------|-------------------|-------|
| UI-01 Navigation | `k.text` (monospace), `k.pos`, `k.color` | Map = lines of glyphs; no canvas/tilemap |
| UI-02 Combat | `k.rect`, `k.text`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Buttons via rect+area+onClick |
| UI-03 Action menu | `k.rect`, `k.text`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Same pattern; Interact key opens |
| UI-04 Rune Forge | `k.rect`, `k.text`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Layout stub |
| UI-05 Inventory | `k.text`, `k.rect`, `k.area`, `k.pos`, `k.anchor`, `k.color` | List items; expand stub |
| UI-06 Dialogue | `k.text`, `k.rect`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Options as rect buttons |
| UI-07 Cutscene | `k.rect`, `k.text`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Full-screen overlay; tag `"cutscene"` |
| UI-08 Room info | `k.text`, `k.pos`, `k.color`, `k.anchor` | 1–2 lines; shared by both modes |
| UI-09 Status strip | `k.text`, `k.pos`, `k.color`, `k.anchor` | Compact line; expand = more text |
| UI-10 Feed | `k.text`, `k.pos`, `k.color`, `k.anchor` | Scrolling region via slice+layout |
| UI-11 First-person | `k.text`, `k.rect`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Narrative block + buttons |
| UI-12 System menu | `k.rect`, `k.text`, `k.area`, `k.pos`, `k.anchor`, `k.color` | Esc; Save/Load buttons |

**Common pattern:** `k.add([k.text(...), k.pos(...), k.anchor(...), k.color(...), k.area()])` for clickable elements. No sprites, no audio—primitives only.

---

## Update Log

| Date | Change |
|------|--------|
| 2026-02 | Initial stub registry; screen-based model; Combat, Rune Forge, Inventory, Dialogue layouts |
| 2026-03 | Added reusable scene blocks + panel formulas; first-person/status and grid context/dialogue now render via constrained block builders |
| 2026-03 | Added Content Pack Reports docs screen (upload bundle, persist report, open in Space Explorer). |
| 2026-03 | Added non-MDX dashboard shell route for DungeonBreakContentApp (`/dashboard-shell-01`) with sidebar/cards/table shadcn block structure. |

]]></content>
  </doc>
  <doc>
    <path>PANEL-ARCHITECTURE.md</path>
    <content><![CDATA[
# Panel Architecture: ASCII-First, No Random Popups

**Status:** Design doc  
**Location:** `.planning/PANEL-ARCHITECTURE.md`  
**Related:** KAPLAY-INTERFACE-SPEC, UI-COMPONENT-REGISTRY

---

## Principle

Content panels (equipment, bag, actions, info, skills) are **fixed layout**. Nothing pops up randomly. Panels swap content in-place; layout is predictable.

---

## Layout Model

### First-Person (React) and Grid (KAPLAY)

Both use the same conceptual layout:

- **Left panel:** Switches content by hotkey. Legend at bottom. Content area above.
  - `[e]` Equipment → equipment slots
  - `[b]` Bag → inventory list
  - `[x]` Actions → action groups (turn-consuming)
  - `[i]` Info → current room + character status
  - `[s]` Skills → list + evolution tabs

- **Center:** Narrative (first-person) or ASCII map (grid).

- **Right panel:** Current room info (always). Or journal when invoked.

- **Feed:** Monospace, line-by-line. Scroll to bottom. No chat bubbles or floating UI.

---

## Construction Rules

| Rule | Meaning |
|------|---------|
| **Fixed slots** | Panels have reserved regions. Content fills slots; no overflow popovers. |
| **Monospace dominant** | Feed, status, room info use mono font. Narrative can use serif. |
| **ASCII aesthetic** | Bordered boxes, `---` separators, `[Label]` for buttons. Think terminal, not Material. |
| **No modals for core flow** | Cutscenes block; equipment/bag/actions are in-panel, not dialogs. |
| **State-driven content** | Panel content = `f(state)`. Same state ⇒ same layout. No animation-driven surprises. |

---

## Panel Content Swap (Not Popup)

When user presses `[b]`:

1. Left panel **content area** clears.
2. Content area renders bag list (from `entity.inventory`).
3. Legend stays: `[e] equip [b] bag [x] actions [i] info [s] skills`.
4. No modal, no overlay, no slide-in. Just a content swap.

---

## React Implementation

- Use a single left column with `useState<"equipment"|"bag"|"actions"|"info"|"skills">`.
- Render `{content}` based on state. Each view is a pure component: `BagPanel({ inventory })`, `EquipmentPanel({ equipped })`, etc.
- Legend at bottom: always visible.
- Actions panel = current `ActionPanel` but styled with borders/mono, not Card/chrome.

---

## KAPLAY Implementation

- Left panel = scene layer. `k.add([...])` for legend; content area = conditional `k.add` based on `panelMode`.
- Same pattern: one content block, swap on keypress.
- Primitives: `k.rect`, `k.text`, `k.area` for clickable. No floating elements.

---

## Equipment, Bag, Skills (Future)

When engine gains:

- **Equipment:** Slots from content pack. Panel shows grid of slots + item names. No drag-drop for now; select slot → pick from bag.
- **Bag:** List. `entity.inventory`. Simple scroll.
- **Skills:** Tabs (list | evolution). Same panel, tab state switches content.

All in-panel. No random popups.

]]></content>
  </doc>
  <doc>
    <path>CONTENT-PACK-VERSIONING.md</path>
    <content><![CDATA[
# Content Pack Versioning and Migration

**Status:** Design doc  
**Location:** `.planning/CONTENT-PACK-VERSIONING.md`  
**Related:** GRD, engine contracts, `packages/engine/src/escape-the-dungeon/contracts/`

---

## Purpose

Support DLC and content pack evolution without breaking existing saves or runs. New content can add traits, skills, item slots, etc. Old data must migrate cleanly to new schemas.

---

## Principles (AAA MMO-style)

1. **Schema versioning:** Every content pack has a `schemaVersion` (semver or ordinal).
2. **Migration over replacement:** Never drop old data; migrate to new format with defaults for new fields.
3. **Engine remains testable:** Core engine is stable; content packs are pluggable. Tests run against versioned fixtures.
4. **MCP/engine contract:** Engine powers everything; MCP server (`engine-mcp`) exposes tools. Game UIs (React, KAPLAY) are thin clients.

---

## Content Pack Structure

| Pack | Current | Schema |
|------|---------|--------|
| action-formulas | bundled | `action-formulas.schema.json` |
| action-catalog | bundled | `action-catalog.schema.json` |
| archetypes | bundled | — |
| cutscenes | bundled | — |
| dialogue-clusters | bundled | — |
| events | bundled | `events.schema.json` |
| items | bundled | — |
| quests | bundled | `quests.schema.json` |
| room-templates | bundled | — |
| skills | bundled | — |

**DLC extension:** A DLC content pack adds new files (e.g. `dlc-traits-v1.json`) and declares dependencies on base pack versions. Loader merges or extends base data.

---

## Migration Contract

When schema evolves (e.g. new trait, new skill branch):

1. **Bump schema version** in pack metadata.
2. **Provide migration function:** `migrate(v: OldShape, fromVersion: string) -> NewShape`.
3. **Defaults for new fields:** New traits/skills/slots get `0` or `[]` in migrated entities.
4. **Backward compat:** Old saves loadable; migration runs once at load time.

---

## Example: New Trait in DLC

Base: `TRAIT_NAMES = [Comprehension, Constraint, ...]`  
DLC adds `Fortitude`.

- **Migration:** For existing entity, set `traits.Fortitude = 0`.
- **Schema:** Trait vector schema extends key set.
- **Content pack:** DLC pack lists `extends: "base-v1"`, adds `traitDefinitions: [{ id: "Fortitude", ... }]`.

---

## Example: New Equipment Slot

Base: no slots. DLC adds `equipped: { weapon?: string, ring1?: string, ... }`.

- **Migration:** `equipped = {}` for old entities.
- **Schema:** Entity state gains optional `equipped` block.
- **Content pack:** DLC defines slot schema; engine validates at runtime.

---

## Implementation Roadmap

| Phase | Scope |
|-------|-------|
| 1 | Add `schemaVersion` to each pack; loader validates. |
| 2 | Migration registry: `registerMigration(packId, fromVersion, fn)`. |
| 3 | DLC loader: load base + DLC packs, apply migrations in order. |
| 4 | Save format version: snapshot includes `contentPackVersions` for reproducibility. |

---

## Local Multi-Agent Authoring (Current Default)

### Runtime Contract

- `features` are the base labeled stats.
- `vectors` are sets of feature values attached to models (`entity`, `item`, `room`, `level`, `effect`).
- `traits` are grouped feature labels for authoring convenience, not a separate vector system.

### Authoring Flow

1. Other agents write patch JSON files into `docs-site/content-packs/inbox/`.
2. Run `pnpm --dir docs-site run content:ingest`.
3. Ingest updates `docs-site/public/game/content-pack.bundle.v1.json`.
4. KAPLAY consumes this bundle at runtime via `/game/content-pack.bundle.v1.json`.
5. Latest ingest report is published at `/reports/content-pack.latest.report.json`.

### Local-Only Dolt Stance (for now)

- Content versioning remains local-first.
- Dolt can be used as an optional local backing store for content snapshots and diffs.
- GitHub/content-pack publishing remains the external release channel.
- No remote Dolt sync is required in this phase.

### Open TODO: Semantic Axes

- Current semantic axes (`risk`, `pressure`, etc.) are treated as optional latent analysis dimensions.
- They are not required for feature/model authoring.
- Keep a TODO to refine, rename, or remove axes that do not improve balancing or explainability.

---

## Testing

- **Replay fixtures:** Golden traces use specific pack versions; migration must not change determinism for same seed + actions.
- **Regression:** Old snapshots load and produce same hash after migration.
- **New content:** DLC adds tests for new traits/skills; engine tests remain stable.

]]></content>
  </doc>
  <doc>
    <path>GAME-VALUE-CONCEPT.md</path>
    <content><![CDATA[
# Game Value Concept

**Purpose:** Define a measurable statistic for the *conceptual* worth of the game design. Implementation value varies; this doc frames the analysis loop.

---

## Conceptual vs Implementation Value

| Dimension | Conceptual value | Implementation value |
|-----------|------------------|----------------------|
| **Scope** | Design space, playthroughs, emergent metrics | Specific build (2D/3D, engine, monetization) |
| **Measurable** | Replayability, excitement, emergent behaviors, action coverage | Revenue, engagement, technical quality |
| **Implementation** | Varies | Varies |

The **Game Value** page analyzes spaces and playthroughs to derive metrics that approximate conceptual worth. The actual implementation (Escape the Dungeon vs DungeonBreak) affects real-world value but not the conceptual design quality.

---

## Implementations

| Implementation | Engine | Notes | Relative value |
|----------------|--------|------|----------------|
| **Escape the Dungeon** | Kaplay (basic 2D) | Current reference; no monetization; small scope | Low |
| **DungeonBreak** | Unreal Engine | 3D; more systems/behaviors; larger scope | High (target) |

Moving from 2D to 3D (DungeonBreak) will require defining additional systems and behaviors in the design space. The conceptual analysis informs that expansion.

---

## Measurable Metrics (Tooling PRD)

- **Replayability:** Action diversity entropy, coverage ratio, unique action–room pairs
- **Excitement:** Per-turn scores, rolling average, mean excitement
- **Emergent:** Novel event combinations, action-type entropy

These feed the Game Value page tabs and Space Explorer for visualizing content and playthrough structure.

---

## Planning Loop

1. Run playthroughs (browser or MCP agent).
2. Analyze reports (replayability, excitement, emergent).
3. Explore spaces (PCA, K-means, Reachability).
4. Adjust policies/content.
5. Re-run and compare.

Phase 30 delivers the Game Value page and browser-side analysis tools. Future phases extend the loop for DungeonBreak 3D expansion.

]]></content>
  </doc>
  <doc>
    <path>implementation-roadmap.md</path>
    <content><![CDATA[
# DungeonBreak Implementation Roadmap

Phased development with explicit acceptance criteria per Ralph Wiggum loop methodology. Each phase ends when its completion criteria are met.

**Narrative Engine (plugin):** State model (N-D basis vectors, Verlet, constraints), dialog/storylet selection by saliency and threshold, alignment and canon/subjective knowledge — see `.planning/DECISIONS.md` (Narrative Engine subsection) and `.planning/PROJECT.md` (Game and narrative scope). **First testable slice:** Verlet integration + constraint application (per-axis min/max, reproject old_p) implemented and unit-tested so narrative state stays within bounds. Later: dialog selection by position/Location, integration with DialogComponent and Yarn Spinner.

---

## Phase 1: Core Systems Foundation

**Scope**: Stabilize existing modules (Quaterra, Narrative, Combat, Economy) and establish integration patterns.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 1.1 | Interaction System | UInteractionSystem handles Friendly/Hostile toggle; FInteractionHandle lifecycle correct |
| 1.2 | Narrative Pipeline | Dialog flows from Narrative module through HUD; keywords and goals wired |
| 1.3 | Combat Baseline | GAS attributes, damage execution, auto-attack functional |
| 1.4 | Economy Baseline | Inventory, equipment slots, currency; UInventoryComponent operational |

**Done**: All four milestones pass; no regressions in existing tests.  
**Output**: `<promise>PHASE1_DONE</promise>`

---

## Phase 2: World & Linear Path

**Scope**: Implement linear world structure; towns and dungeons as POIs; basic world map navigation.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 2.1 | World Map | Linear path visible; player knows "where to go next" |
| 2.2 | POI Spawning | Towns and dungeons spawn along path; predicate-based placement |
| 2.3 | Dungeon Layout | Tile/grammar-based dungeon generation; no perceptual oatmeal in layouts |
| 2.4 | Travel Flow | Fast-forward time; towns safe for resting/training; risk on road |

**Done**: Player can traverse linear path; POIs feel distinct.  
**Output**: `<promise>PHASE2_DONE</promise>`

---

## Phase 3: Character Systems

**Scope**: Orthogonal traits, progression, training regimens, time-spend economy for characters.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 3.1 | Orthogonal Traits | Trait axes defined; no overlapping behavior (per DECISIONS) |
| 3.2 | Progression | Skills improve by use; attributes via items; combat metrics feed XP |
| 3.3 | Training Regimens | Trainers teach regimens; training grounds functional |
| 3.4 | Character Simulation | All characters simulate on time-spend; NPC growth independent of player |
| 3.5 | Aging & Death | Death frequent; respawn with retained experience/regimens; visible aging |

**Done**: Character system supports modular, perceptually unique NPCs.  
**Output**: `<promise>PHASE3_DONE</promise>`

---

## Phase 4: Ever Growing Threat

**Scope**: Dungeon breaks, emergent threats, society-driven crisis escalation.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 4.1 | Dungeon Gates | Gates spawn creatures (goblins, kobolds, etc.); threaten roads/towns |
| 4.2 | Threat Escalation | Frequency and severity scale with societal/crisis variables |
| 4.3 | Safe Zones | Towns safer than roads; "none safe forever" enforced |
| 4.4 | NPC Response | NPCs react to threats; some become allies, some competition |

**Done**: Threat loop feels dynamic; player understands "ever growing" pressure.  
**Output**: `<promise>PHASE4_DONE</promise>`

---

## Phase 5: Economy & Guild Hierarchy

**Scope**: In-depth economy; guild power structure; adventurer stratification.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 5.1 | Economy Loop | Shops, currencies, items; economy ties into core loops |
| 5.2 | Guilds | Guilds control adventuring means; top-down power structure |
| 5.3 | Stratification | Top adventurers have privilege; lower tiers struggle (streaming/livelihood theme) |
| 5.4 | Time-Spend Economy | Character growth and world state tied to game time; emergent competition/alliances |

**Done**: Economy and guild systems support narrative themes.  
**Output**: `<promise>PHASE5_DONE</promise>`

---

## Phase 6: Dialog & TTRPG Feel

**Scope**: Effort resource, alignment, dialog options with requirements/rewards; companion walk-and-talk.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 6.1 | Effort System | Effort (⚡) consumed by actions; Pause, Reconsider, Vow, etc. |
| 6.2 | Alignment | Actions affect alignment; +Alignment feedback visible |
| 6.3 | Dialog Requirements | Options have attribute/skill/alignment requirements and rewards |
| 6.4 | Companion Dialogue | Walk and talk with companions; light-hearted exchanges feed narrative |
| 6.5 | TTRPG Feel | Feels like playing in a TTRPG; DM-like pacing and discovery |

**Done**: Interaction loop matches dialog mockup; TTRPG feel achieved.  
**Output**: `<promise>PHASE6_DONE</promise>`

---

## Phase 7: Time Travel & Branching

**Scope**: Time travel mechanics; bifurcation from choices; retained vs. reset state.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 7.1 | Time Travel | Time travel possible; systemically discouraged but necessary at times |
| 7.2 | Reset Rules | Currency/items reset; experience, regimens, visual progression retained |
| 7.3 | Bifurcation | Choices create divergent outcomes within same world space |
| 7.4 | Future Trunks Arc | Guaranteed grim future; player can prepare via friends and choices |

**Done**: Time loop supports Re:Zero–style iteration with retained knowledge.  
**Output**: `<promise>PHASE7_DONE</promise>`

---

## Phase 8: Polish & Content

**Scope**: UI polish, content authoring, perceptual uniqueness pass.

**Milestones**:
| Milestone | Description | Acceptance criteria |
|-----------|-------------|---------------------|
| 8.1 | UI Polish | Party builder, loadouts, RTS controls match reference |
| 8.2 | Content Volume | Story volume populated; sufficient variety for perceptual uniqueness |
| 8.3 | Oatmeal Pass | No "10,000 bowls" feeling in characters, events, or locations |
| 8.4 | Exceptions | Hand-authored flourishes that break rules where appropriate |

**Done**: Game feels complete and distinct; no placeholder-only content.  
**Output**: `<promise>PHASE8_DONE</promise>`

---

## Ralph Wiggum Loop Application

For each phase:
1. Define scope and completion criteria (above).
2. Implement milestones; self-check against criteria.
3. If failing: note blockers, refine, retry.
4. Output `<promise>PHASE_N_DONE</promise>` when done.
5. Cap iterations per phase (e.g., max 2–3 planning refinements) to avoid over-planning.

]]></content>
  </doc>
  <doc>
    <path>escape-the-dungeon-teen-guide.md</path>
    <content><![CDATA[
# Escape the Dungeon Engine Guide (Simple Version)

This guide explains the game engine in plain language.

## 1. What the game is

- You play as **Kael**.
- You start at level 12 (deepest point).
- You want to reach the final gate on level 1.
- Every action is one turn.

## 2. Main objects

Think of the game like a set of boxes:

1. **Dungeon**
- Holds all levels.
- Knows total level count (12).

2. **Level**
- Holds 50 rooms.
- Has one start room and one exit room.

3. **Room**
- Has a feature (training, treasure, rest, etc.).
- Has items.
- Has a vector (meaning state).

4. **Entity**
- Kael and NPCs are entities.
- Entities have:
  - traits (vector values)
  - attributes (might/agility/insight/willpower)
  - health, energy, inventory

5. **Act / Chapter / Page**
- Each level = one chapter.
- 4 chapters = one act.
- Pages store logs of what happened.

## 3. How turns work

One turn = one action:

1. Player takes one action (`move`, `train`, `rest`, `talk`, `search`, `speak`, `choose_dialogue`).
2. Room vector influences the actor.
3. Event is logged to pages.
4. NPCs each take one action.

## 4. What vectors do

Vectors are just number maps like:

```text
Empathy: 0.4
Survival: 0.2
Direction: -0.1
```

We use vectors for:

- entity traits
- room mood/state
- item influence
- dialogue option positions

## 5. Dialogue ranges

Dialogue options are only available when:

1. The current vector is close enough to the option vector.
2. The room/item conditions are true.

Example:

- `loot_treasure` needs a treasure item to exist.
- If treasure is gone, that option disappears.
- A different option can appear, like:
  - `wish_something_else_was_here`

## 6. How to add new features safely

### Add a new room feature

1. Add a constant in `world/map.py`.
2. Add base vector behavior in `_feature_base_vector`.
3. Add generation rule for which rooms get it.

### Add a new dialogue option

1. Open `narrative/dialogue.py`.
2. Add a `DialogueOption` in a cluster.
3. Set:
  - `anchor_vector`
  - `radius`
  - room/item requirements
4. Test with `game.available_dialogue_options()`.

### Add a new trait

1. Add it to trait manifest.
2. Add anchor text in projector defaults.
3. Update any room/dialogue vectors that should use it.

## 7. CLI commands

Use:

```bash
escape-the-dungeon
```

Useful commands:

- `look`
- `status`
- `options`
- `choose <option_id>`
- `go north|south|east|west|up|down`
- `train`
- `rest`
- `talk`
- `search`
- `say <text>`
- `pages`

]]></content>
  </doc>
  <doc>
    <path>TODO-dungeonbreak-3d.md</path>
    <content><![CDATA[
# TODO: DungeonBreak 3D

**Status:** Discovery pending  
**Scope:** Future Unreal Engine 3D adaptation of Escape the Dungeon

---

## Context

DungeonBreak 3D will extend Escape the Dungeon (2D/Kaplay) into a 3D Unreal Engine experience. Actual implementation value will be higher than the current reference build; design requires more systems and behaviors to define as we move from 2D to 3D.

## Prerequisites

- Core systems (engine, formulas, content packs) must be stable
- Content and spaces framework documented
- Stakeholder discovery meeting to align scope and constraints

## Approach

1. **Planning loop** — Use existing planning loop (discuss → plan → execute → verify)
2. **Core systems** — Build on engine contracts, formula registry, space definitions
3. **Content** — Extend content packs for 3D; new room/entity/action definitions
4. **Discovery** — Stakeholder meeting to capture desired outcomes, constraints, non-goals

## Links

- [GRD](/planning/grd) — Current game requirements (Escape the Dungeon)
- [Formulas](/docs/formulas) — Calculations and spaces
- [Game Value](/play/reports/game-value) — Concept worth analysis

]]></content>
  </doc>
  <doc>
    <path>TODO-realtime-content-visualizer.md</path>
    <content><![CDATA[
# TODO: Realtime Content Visualizer

**Status:** Pending

## Context

Content spaces shift during a playthrough. The current Space Explorer shows a static PCA projection of catalog content (skills, archetypes, dialogue). During runtime, availability changes because of gates and state:

- **Progression gates:** Content can be on later strata of progression and therefore not currently reachable.
- **Branch locks:** Already selected branches or evolved skills can remove alternative paths.
- **Dynamic filters:** Room context, item tags, prerequisites, and depth all affect live reachability.

## Needed

1. **Realtime availability layer** - Show content available at the current turn, not just full-catalog positions.
2. **Space strata model** - Represent progression levels/strata and branch exclusions explicitly.
3. **State-coupled UX** - As turn selection changes, reflect current vector position and currently reachable regions immediately.
4. **Event Space panel** - Add first-class event-space view (trigger/context/outcome vectors, deterministic/emergent filters).
5. **Behavior Lens panel** - Add timeline controls for behavior/effect similarity:
   - window length `T`
   - sample interval `dt`
   - similarity metric (cosine / DTW)
   - baseline vs candidate comparison
6. **Effect model split** - Visual separation of `deltaEffect` vs `behaviorEffect` and their targets (entity/room/level/feature).
7. **Semantic-axis definition pass (experimental)** - Keep semantic axes optional until formally defined:
   - Current status: similarity/ranking defaults are feature-first (`content_features` + `power_features` only).
   - Open question: whether semantic axes should exist as first-class dimensions or just derived debug labels.
   - If retained, each semantic axis must have:
     - plain-language definition
     - source features list
     - deterministic mapping formula
     - examples and counterexamples
8. **Content-pack authoring flow in Space Explorer** - Completed first usable slice:
   - Build model/feature patch in UI
   - Save/load local drafts
   - Validate schema refs and summarize diff vs base bundle
   - Build and download full `content-pack.bundle.v1.json` via docs API route
   - Remaining: release publishing integration and generated docs report wiring

## References

- [Space Explorer](/play/reports/spaces) - current static visualization
- [Spaces doc](/docs/formulas/spaces) - canonical vector/space definitions

]]></content>
  </doc>
</planning-references>
